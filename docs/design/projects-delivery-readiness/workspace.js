/* global window, document, setTimeout, clearTimeout, FormData, Blob, URL */
/* One shared session, six views. All visible user text is escaped at the boundary. */
(() => {
  'use strict';
  const M=window.PPO;
  let s=M.seed(), decision=null, opener=null, panelOpener=null, toastTimer=null, assistantTimer=null, assistantRun=0, suspendedDecision=null;
  const draftBuffers={}, editorOpen={}, columnWidths={};
  const $=id=>document.getElementById(id);
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const dt=M.formats.date;
  const icons={overview:'<rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/>',readiness:'<path d="M9 5h11M9 12h11M9 19h11m-17-14 1 1 2-3m-3 9 1 1 2-3m-3 9 1 1 2-3"/>',programme:'<path d="M4 3v18h17M7 7h7v3H7zM12 14h8v3h-8z"/>',changes:'<path d="M5 3v12a5 5 0 0 0 5 5h9M14 15l5 5-5 4M5 10h8a5 5 0 0 0 5-5V3M15 6l3-3 3 3"/>',actions:'<rect x="4" y="4" width="16" height="17" rx="2"/><path d="M9 3h6v3H9zM8 12l2 2 6-6M8 18h8"/>',evidence:'<path d="M6 3h8l4 4v14H6zM14 3v5h4M9 12h6M9 16h6"/>',arrow:'<path d="M4 12h16m-6-6 6 6-6 6"/>',chevron:'<path d="m9 5 7 7-7 7"/>',down:'<path d="m5 9 7 7 7-7"/>',search:'<circle cx="10" cy="10" r="6"/><path d="m15 15 6 6"/>',clock:'<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/>',warning:'<path d="m12 3 10 18H2zM12 9v5M12 17h.01"/>',block:'<circle cx="12" cy="12" r="9"/><path d="m6 6 12 12"/>',check:'<path d="m5 12 4 4L19 6"/>',info:'<circle cx="12" cy="12" r="9"/><path d="M12 11v6M12 7h.01"/>',snapshot:'<rect x="3" y="4" width="18" height="16" rx="1"/><path d="M14 4v16M17 8h1M17 12h1"/>',assistant:'<path d="m12 3 2.8 6.2L21 12l-6.2 2.8L12 21l-2.8-6.2L3 12l6.2-2.8L12 3Z"/>',guide:'<path d="M3 4h7l2 2 2-2h7v16h-7l-2 2-2-2H3zM12 6v16"/>',plus:'<path d="M12 4v16M4 12h16"/>',filter:'<path d="M3 5h18M6 12h12M9 19h6"/>',download:'<path d="M12 3v12m-5-5 5 5 5-5M4 16v5h16v-5"/>',print:'<path d="M7 8V3h10v5M7 17H3V9h18v8h-4M7 14h10v7H7zM17 11h.01"/>',refresh:'<path d="M20 8a8 8 0 1 0 0 8M20 3v5h-5"/>',close:'<path d="m6 6 12 12M18 6 6 18"/>',person:'<circle cx="12" cy="7" r="4"/><path d="M4 21v-2a8 8 0 0 1 16 0v2"/>',location:'<path d="M19 10c0 5-7 12-7 12S5 15 5 10a7 7 0 0 1 14 0Z"/><circle cx="12" cy="10" r="2"/>',pump:'<path d="M4 8h12v12H4zM7 4h6v4M16 12h5v5h-5M7 12h6v5H7zM2 20h17"/>',calendar:'<rect x="3" y="5" width="18" height="16" rx="2"/><path d="M7 3v5M17 3v5M3 10h18M7 14h3M14 14h3M7 18h3"/>',settings:'<path d="M4 7h16M4 17h16"/><circle cx="9" cy="7" r="3" fill="white"/><circle cx="16" cy="17" r="3" fill="white"/>',lock:'<rect x="5" y="10" width="14" height="11" rx="2"/><path d="M8 10V6a4 4 0 0 1 8 0v4M12 14v3"/>',edit:'<path d="m15 3 6 6-12 12H3v-6zM12 6l6 6"/>',back:'<path d="M20 12H4m6-6-6 6 6 6"/>',list:'<path d="M8 5h13M8 12h13M8 19h13M3 5h.01M3 12h.01M3 19h.01"/>'};
  const icon=(name)=>`<svg class="icon" viewBox="0 0 24 24" aria-hidden="true" focusable="false">${icons[name]||icons.info}</svg>`;
  const btn=(label,action,ico='',cls='',extra='')=>`<button type="button" class="${cls}" data-action="${action}" ${extra}>${ico?icon(ico):''}${label}</button>`;
  const chip=(text,kind='neutral')=>`<span class="pill ${kind}">${esc(text)}</span>`;
  const stateKind=v=>v==='Blocked'||v==='Refused'?'bad':/Ready|Accepted|Completed|Resolved/.test(v)?'good':/risk|Returned|outdated|needed|Pending|Resubmitted|review/.test(v)?'risk':'neutral';
  const statePill=v=>chip(v,stateKind(v));
  const person=name=>`<span class="row"><span class="avatar" aria-hidden="true">${esc(name.split(' ').map(x=>x[0]).join(''))}</span><span>${esc(name)}</span></span>`;
  const options=(vals,current)=>vals.map(x=>{const v=Array.isArray(x)?x[0]:x,l=Array.isArray(x)?x[1]:x;return `<option value="${esc(v)}" ${v===current?'selected':''}>${esc(l)}</option>`;}).join('');
  const field=(label,name,value,type='text',extra='')=>`<div class="field"><label for="${name}">${esc(label)}</label><input id="${name}" name="${name}" type="${type}" value="${esc(value)}" ${extra}></div>`;
  const selectField=(label,name,values,current,extra='')=>`<div class="field"><label for="${name}">${esc(label)}</label><select id="${name}" name="${name}" ${extra}>${options(values,current)}</select></div>`;
  const notice=(kind,title,text,action='')=>`<div class="notice ${kind}">${icon(kind==='bad'?'block':kind==='risk'?'warning':kind==='good'?'check':'info')}<div class="grow"><strong>${esc(title)}</strong>${text?`<p class="small">${text}</p>`:''}${action}</div></div>`;
  const sourceChip=(id,rev)=>{const src=s.sources[id],n=rev||M.current(s,id).revision;return btn(esc(src.title)+' · r'+String(n).padStart(2,'0'),'open-source','evidence','source-chip',`data-id="${id}" data-rev="${n}"`);};
  const sourceCell=id=>{const src=s.sources[id],x=M.current(s,id);return `${btn(esc(src.title),'open-source','','link',`data-id="${id}" data-rev="${x.revision}"`)}<span class="cell-sub">r${String(x.revision).padStart(2,'0')} · ${esc(x.at)}${src.unavailable?' · unavailable':''}</span>`;};
  function announce(t){$('announcer').textContent=t;}
  function toast(t){clearTimeout(toastTimer);$('toast').innerHTML=`<span>${esc(t)}</span>${btn('','dismiss-toast','close','', 'aria-label="Dismiss message"')}`;$('toast').classList.add('show');toastTimer=setTimeout(()=>$('toast').classList.remove('show'),7000);}
  function error(t){toast(t);announce(t);}
  const currentProject=()=>s.projects.find(p=>p.id===s.project);
  const scopeLabel=v=>({all:'All work packages',pump:'WP01 · Shared pump replacement',components:'WP02 · Valve kit preparation',survey:'Controls survey'}[v]||v);
  const isLocked=()=>!M.canEdit(s);
  function renderContext(){const p=currentProject();$('project-context').innerHTML=`<section class="project-strip" aria-label="Selected project"><div class="project-title"><div class="row wrap"><span class="eyebrow">${esc(p.ref)}</span>${chip(s.project==='irrigation'?'Delivery coordination':'Discovery','neutral')}</div><h2>${esc(p.name)}</h2><div class="row small muted">${icon('location')}<span>${esc(p.customer)} <span aria-hidden="true"> / </span> ${esc(p.site)}</span></div></div><div class="project-controls">${selectField('Work package','scope',s.project==='irrigation'?[['all','All work packages'],['pump','WP01 · Shared pump'],['components','WP02 · Valve kit']]:[['all','All work packages'],['survey','Controls survey']],s.ui.scope,'data-filter="scope"')}${selectField('Affected area','area',[['all','All affected areas'],['greenhouse','Greenhouse 01'],['tunnel','Tunnel 01'],['propagation','Propagation House 01']],s.ui.area,'data-filter="area"')}</div></section><div class="context-meta"><span class="row">${person(p.coordinator)}<span>Project coordinator</span></span><span class="row">${icon('clock')}Sources as at 15 Sep 2026 · 09:00 AEST${s.seq?' · local revisions available':''}</span><span class="row">${icon('pump')}One pump · three served areas</span>${s.role!=='Coordinator'?chip(s.role,s.role==='Delivery viewer'?'risk':'info'):''}</div>`;
    const counts=M.count(s),views=[['overview','Overview'],['readiness','Readiness'],['programme','Programme'],['changes','Changes'],['actions','Actions'],['evidence','Evidence & history']];
    $('tabs').innerHTML=views.map(([id,label])=>`<button type="button" role="tab" id="tab-${id}" data-action="tab" data-tab="${id}" aria-controls="content" aria-selected="${s.ui.tab===id}" tabindex="${s.ui.tab===id?'0':'-1'}">${icon(id)}${label}${id==='changes'?`<span class="badge-count">${counts.changes}</span>`:''}</button>`).join('');
    $('content').setAttribute('aria-labelledby','tab-'+s.ui.tab);
    $('session-line').innerHTML=`<span class="session-status">${icon(s.ui.save.includes('failed')||s.ui.save.includes('not confirmed')?'warning':'check')}${esc(s.ui.save)}${s.pendingOperation?btn('Recover original receipt','recover','','link'):''}</span><span class="row wrap"><span>Synthetic data · r01</span>${btn('Demo controls','demo','settings','quiet')}${btn('Page guide','guide','guide','quiet')}</span>`;
  }
  function toolsBar(title,subtitle,primary,action,ico){return `<div class="toolbar"><div><h2>${title}</h2><p class="small muted">${subtitle}</p></div><div class="toolbar-actions">${primary?btn(primary,action,ico,'primary',isLocked()&&['new-change','preview'].includes(action)?'disabled title="Inspection only for this role"':''):''}${btn('<span class="optional-label">Snapshot</span>','snapshot','snapshot','quiet','aria-label="Open Snapshot"')}${btn('<span class="optional-label">Assistant</span>','assistant','assistant','quiet','aria-label="Open Assistant"')}${btn('','guide','guide','quiet icon-button','aria-label="Open Page guide"')}</div></div>`;}
  function render(){const active=document.activeElement,focused=active?.id,selection=active?.selectionStart,focusData=active?.dataset?.action?{...active.dataset}:null,inDock=$('dock').contains(active);renderContext();const view={overview:overview,readiness:readiness,programme:programme,changes:changes,actions:actions,evidence:evidence};$('content').innerHTML=(isLocked()?notice('info','Delivery viewer','You can inspect delivery context. Editing is unavailable for this role. Commercial details are restricted.')+'<div class="spacer"></div>':'')+view[s.ui.tab]();if(s.ui.panel)renderPanel();layoutPanel();if(focused&&$(focused)&&$(focused).tagName==='INPUT'){$(focused).focus({preventScroll:true});if(typeof selection==='number'&&$(focused).type==='search')$(focused).setSelectionRange(selection,selection);}else if(focusData){const target=[...(inDock?$('dock'):$('app')).querySelectorAll('[data-action]')].find(x=>Object.entries(focusData).every(([key,val])=>x.dataset[key]===val));target?.focus({preventScroll:true});} }
  function overview(){const n=M.count(s),rows=M.scopedRows(s),attention=rows.filter(r=>!['Ready for stated scope'].includes(r.state)).sort((a,b)=>M.states.indexOf(b.state)-M.states.indexOf(a.state)).slice(0,5);const all=s.project==='irrigation';return toolsBar('Delivery overview','What is stopping delivery, and who needs to act next?',all?'Review next blocker':'Review missing evidence','next-blocker','arrow')+`<section class="metric-grid" aria-label="Project summary">${metric('Blocking prerequisites',n.blocked,'Need a delivery response','block','metric-blocked','danger-text')}${metric('Evidence to review',n.evidence,'Unknown or incomplete basis','evidence','metric-evidence','risk-text')}${metric('Open change reviews',n.changes,'Coordinate affected work','changes','metric-changes','')}${metric('Next forecast milestone',all?'21':'—',all?'Sep 2026 · pump arrival baseline':'Date not recorded','calendar','metric-programme','',all?'Sep':'')}</section>${all?notice('bad','Pump delay crosses the agreed work window',`Arrival now expected <strong>5 Oct 2026</strong>. Installation is forecast for <strong>23–25 Sep 2026</strong>; the <strong>28 Sep 2026</strong> commissioning visit is still confirmed.`,btn('Open change review','select-change','arrow','link','data-id="DEMO-CHG-024001"')):notice('info','No blockers assessed yet','Source information is partial. A zero blocker count is not a readiness assessment.')}<div class="overview-columns"><div class="stack"><section class="card"><header class="card-head"><div><h2>Needs attention</h2><p class="small muted">Prioritised for ${esc(scopeLabel(s.ui.scope))}</p></div>${btn('View register','tab','arrow','quiet','data-tab="readiness"')}</header>${attention.length?attention.map(r=>`<div class="attention-row"><span class="priority-icon ${r.state==='Blocked'?'':'risk'}">${icon(r.state==='Blocked'?'block':'warning')}</span><div><h3>${btn(esc(r.title),'open-readiness','','link',`data-id="${r.id}"`)}</h3><p>${esc(r.reason)}</p>${statePill(r.state)} <span class="small muted">${esc(r.category)}</span></div><div class="attention-owner"><span class="small strong">${esc(r.owner)}</span><span class="small muted">${r.due?'Due '+dt(r.due):'Due date not recorded'}</span>${btn('Inspect','open-readiness','chevron','quiet',`data-id="${r.id}"`)}</div></div>`).join(''):`<div class="empty">${icon('check')}<h3>No attention items in this scope</h3><p>Ready assessments still apply only to each recorded prerequisite.</p></div>`}</section><section class="card"><header class="card-head"><div><h2>One shared pump, connected delivery</h2><p class="small muted">Installation location and served areas remain distinct.</p></div>${btn('Inspect relationship','open-source','snapshot','quiet','data-id="relationship"')}</header><div class="card-body"><div class="scope-diagram"><div class="pump-node">${icon('pump')}<div><strong>Irrigation pump 01</strong><p class="small muted">SYN-PPO-AST-000501 · installed in Irrigation Shed 01</p></div></div><div class="served-areas">${s.equipment.servedNames.map(x=>`<span>${esc(x)}</span>`).join('')}</div></div><p class="small muted" style="margin-top:12px">Staging any served area requires evidence of independent isolation. Selecting an area does not remove the shared pump prerequisites.</p></div></section></div><div class="stack overview-support"><section class="card"><header class="card-head"><h2>Upcoming dates</h2></header>${all?`<div class="milestone-item"><div class="date-tile">21<small>Sep 2026</small></div><div><strong class="small">Pump arrival baseline</strong><p class="small muted">Supplier now advises 5 Oct</p>${chip('Forecast at risk','risk')}</div></div><div class="milestone-item"><div class="date-tile">23<small>Sep 2026</small></div><div><strong class="small">Installation starts</strong><p class="small muted">Through 25 Sep 2026</p>${chip('Project forecast','neutral')}</div></div><div class="milestone-item"><div class="date-tile confirmed">${Number(M.current(s,'visit').date.slice(-2))}<small>${M.current(s,'visit').date.includes('-09-')?'Sep':'Oct'} 2026</small></div><div><strong class="small">Commissioning visit</strong><p class="small muted">${dt(M.current(s,'visit').date)} · Service crew A</p>${chip('Confirmed booking','info')}</div></div>`:'<div class="card-body"><p class="small muted">No survey appointment is recorded.</p></div>'}</section><section class="card"><header class="card-head"><h2>Source coverage</h2></header><div class="card-body coverage"><div class="coverage-line"><span>Prerequisites in scope</span><strong>${n.total}</strong></div><div class="coverage-line"><span>Ready for stated scope</span><strong>${n.ready}</strong></div><div class="coverage-line"><span>Evidence requiring review</span><strong>${n.evidence}</strong></div><div class="coverage-line"><span>Unavailable sources</span><strong>${M.sourceIds(s).filter(id=>s.sources[id].unavailable).length}</strong></div><p class="small muted">Readiness is assessed per prerequisite. These counts are not a project completion percentage.</p></div></section><section class="card"><header class="card-head"><h2>Customer arrangements</h2></header><div class="card-body stack-sm"><span class="small strong">${all?dt(M.current(s,'access').start)+' – '+dt(M.current(s,'access').end):'Window not recorded'}</span><p class="small muted">${all?'Agreed shutdown · 07:00–15:00<br>Nursery local time · Australia/Melbourne':'Coordinate with the nursery manager.'}</p><span class="small">Casey Taylor · nursery manager</span>${btn('View access evidence','open-source','arrow','link',`data-id="${all?'access':'survey'}"`)}</div></section></div></div>`;}
  function metric(label,value,sub,ico,action,tone,tail=''){return `<button type="button" class="metric" data-action="${action}"><span class="section-label">${esc(label)}</span>${icon(ico)}<span class="metric-value"><span class="count-big ${tone}">${esc(value)}</span>${tail?`<span class="metric-tail">${tail}</span>`:''}</span><span class="small muted">${esc(sub)}</span></button>`;}
  function readiness(){const u=s.ui,rows=M.visibleRows(s),full=M.scopedRows(s);let body='';for(const cat of M.categories){const rs=rows.filter(r=>r.category===cat);if(!rs.length)continue;const collapsed=u.collapsed.includes(cat);body+=`<tr class="group-row"><td colspan="6">${btn(`${icon(collapsed?'chevron':'down')}${esc(cat)} <span class="badge-count">${rs.length}</span>`,'collapse-group','','',`data-id="${esc(cat)}" aria-expanded="${!collapsed}"`)}</td></tr>`;if(!collapsed)body+=rs.map(r=>`<tr class="record ${u.selected?.kind==='readiness'&&u.selected.id===r.id?'is-selected':''}"><td><span class="mobile-label">Prerequisite</span>${btn(esc(r.title),'open-readiness','','link record-title',`data-id="${r.id}"`)}<span class="cell-sub">${esc(r.id==='pump-line'?'SYN-PPO-AST-000501':r.category)}</span></td><td><span class="mobile-label">Affected scope</span>${esc(scopeLabel(r.scope))}<span class="cell-sub">${r.scope==='pump'?'All 3 served areas':r.scope==='components'?'Separate component shipment':'Propagation House 01'}</span></td><td><span class="mobile-label">State / reason</span>${statePill(r.state)}<span class="cell-sub">${esc(r.reason)}</span></td><td><span class="mobile-label">Source / revision</span>${sourceCell(r.sourceId)}</td><td><span class="mobile-label">Owner</span>${esc(r.owner)}</td><td><span class="mobile-label">Next action / due</span>${btn(esc(r.action),'clarify','','link',`data-origin="${r.id}" data-owner="${esc(r.owner)}"`)}<span class="cell-sub">${r.due?dt(r.due):'Due date not recorded'}</span></td></tr>`).join('');}
    return toolsBar('Readiness register','Evidence, scope and the next owned action.','Review next blocker','next-blocker','arrow')+`<div class="filter-bar"><div class="search-field">${icon('search')}<input id="readiness-search" type="search" aria-label="Search readiness" placeholder="Search prerequisites, reasons or owners" value="${esc(u.search)}" data-filter="search"></div>${selectField('State','state',[['all','All states'],...M.states],u.state,'data-filter="state"')}${selectField('Category','category',[['all','All categories'],...M.categories],u.category,'data-filter="category"')}${selectField('Owner','owner',[['all','All owners'],...M.owners],u.owner,'data-filter="owner"')}</div><div class="row wrap between" style="margin-bottom:12px"><div class="row wrap"><label class="check"><input type="checkbox" data-filter="attention" ${u.attention?'checked':''}>Needs attention</label><label class="check"><input type="checkbox" data-filter="myActions" ${u.myActions?'checked':''}>Owned by me</label></div>${btn('Clear filters','clear-filters','refresh','quiet')}</div>${s.ui.evidenceOnly?notice('info','Evidence requiring review','Showing Not assessed and Evidence needed prerequisites from the summary.')+'<div class="spacer"></div>':''}<section class="card">${body?`<div class="table-scroll" tabindex="0" role="region" aria-label="Readiness register; scroll horizontally on intermediate widths"><table class="register card-register"><colgroup>${[21,15,23,18,10,13].map((w,i)=>`<col style="width:${columnWidths[i]||w+'%'}">`).join('')}</colgroup><thead><tr>${['Prerequisite','Affected scope','State / reason','Source / revision','Owner','Next action / due'].map((x,i)=>`<th scope="col">${x}${i<5?`<span class="column-resize" role="separator" tabindex="0" aria-orientation="vertical" aria-label="Resize ${x} column" data-col="${i}" title="Drag, or use left and right arrow keys"></span>`:''}</th>`).join('')}</tr></thead><tbody>${body}</tbody></table></div>`:`<div class="empty">${icon('search')}<h3>No matching prerequisites</h3><p>There are ${full.length} prerequisites in the selected work-package scope. Broaden the filters to see them.</p>${btn('Clear filters','clear-filters','refresh')}</div>`}<footer class="register-footer"><span>${rows.length} shown · ${full.length} prerequisites in scope</span><span>Filtered rows do not change the underlying readiness assessment.</span></footer></section>`;}
  function programme(){const c=M.activeChange(s),all=M.programme(s,c).filter(t=>(s.ui.scope==='all'||t.scope===s.ui.scope)),items=all.filter(t=>s.ui.programmeFilter==='all'||s.ui.programmeFilter==='conflict'&&t.conflict||s.ui.programmeFilter==='affected'&&t.proposedStart||s.ui.programmeFilter==='undated'&&!t.start),mode=s.ui.programmeMode;return toolsBar('Programme','Manual project forecasts and Service bookings, shown together.','Preview impact','preview','changes')+`<div class="card"><div class="timeline-toolbar"><div class="row wrap"><div class="segmented" role="group" aria-label="Programme display">${btn('Gantt','programme-mode','programme',mode==='gantt'?'selected':'','data-id="gantt" aria-pressed="'+(mode==='gantt')+'"')}${btn('List','programme-mode','list',mode==='list'?'selected':'','data-id="list" aria-pressed="'+(mode==='list')+'"')}</div><div class="segmented" role="group" aria-label="Timeline scale">${['fit','week','month'].map(x=>btn(x[0].toUpperCase()+x.slice(1),'programme-scale','',s.ui.programmeScale===x?'selected':'',`data-id="${x}" aria-pressed="${s.ui.programmeScale===x}"`)).join('')}</div>${btn('Today','today','calendar')}</div><div class="row wrap"><label class="check"><input type="checkbox" data-filter="compare" ${s.ui.compare?'checked':''}>Show proposed dates</label><select aria-label="Programme records" data-filter="programmeFilter">${options([['all','All tasks & visits'],['conflict','Conflicts'],['affected','Affected by change'],['undated','Undated']],s.ui.programmeFilter)}</select></div></div>${items.length?(mode==='gantt'?gantt(items):programmeList(items)):`<div class="empty">${icon('programme')}<h3>No matching programme records</h3><p>Change the filter to include recorded tasks or undated milestones.</p></div>`}<div class="legend"><span><i></i>Current forecast</span><span><i class="proposed-key"></i>Proposed dates</span><span><i class="visit-key"></i>Confirmed Service visit</span><span><i class="conflict-key"></i>Conflict / at risk</span></div>${items.filter(t=>t.conflict).map(t=>`<div class="timeline-conflict">${icon('warning')}<div><strong>${esc(t.title)}</strong><p>${esc(t.conflict)}</p></div></div>`).join('')}</div><p class="small muted" style="margin-top:12px">FS: successor starts no earlier than the next Monday–Friday working day after finish. SS: successor starts on or after start. No holiday calendar, date cascade or critical-path calculation. Confirmed visits are changed only by a scheduler response.</p>`;}
  function programmeList(items){return `<div class="table-scroll"><table class="register card-register"><thead><tr><th style="width:25%">Task / milestone</th><th>Current position</th><th>Proposed position</th><th>Dependency / owner</th></tr></thead><tbody>${items.map(t=>`<tr class="record"><td><span class="mobile-label">Task / milestone</span>${btn(esc(t.title),'open-task','','link record-title',`data-id="${t.id}"`)}<span class="cell-sub">${t.booking?'Confirmed Service appointment':t.milestone?'Milestone':'Project forecast'}</span></td><td><span class="mobile-label">Current position</span>${dt(t.start)}${t.end&&t.start!==t.end?'<br>to '+dt(t.end):''}</td><td><span class="mobile-label">Proposed position</span>${s.ui.compare&&t.proposedStart?dt(t.proposedStart)+(t.proposedEnd!==t.proposedStart?'<br>to '+dt(t.proposedEnd):''):'No proposal'}${t.conflict?`<span class="cell-sub danger-text">${esc(t.conflict)}</span>`:''}</td><td><span class="mobile-label">Dependency / owner</span>${esc(t.relation||'No predecessor')}<span class="cell-sub">${esc(t.owner)}</span></td></tr>`).join('')}</tbody></table></div>`;}
  function gantt(items){const times=[s.today,...items.flatMap(t=>[t.start,t.end,...(s.ui.compare?[t.proposedStart,t.proposedEnd]:[])]).filter(Boolean)].map(x=>new Date(x+'T12:00:00Z').getTime()),first=new Date(Math.min(...times));first.setUTCDate(first.getUTCDate()-((first.getUTCDay()+6)%7));const start=first.toISOString().slice(0,10),span=Math.max(35,Math.ceil((Math.max(...times)-first.getTime())/86400000/7)*7+7),pos=date=>100*(new Date(date+'T12:00:00Z')-first.getTime())/86400000/span;const dates=Array.from({length:5},(_,i)=>dt(new Date(first.getTime()+Math.round(span*i/5)*86400000).toISOString().slice(0,10))),width=s.ui.programmeScale==='week'?1800:s.ui.programmeScale==='month'?1050:1000;return `<div id="timeline-scroll" class="timeline-wrap" tabindex="0" role="region" aria-label="Gantt timeline starting ${esc(dt(start))}, ${span} calendar days; horizontal scrolling"><div class="gantt" style="min-width:${width}px"><div class="gantt-head"><div class="gantt-labels">Activity / work package</div><div class="gantt-dates">${dates.map(d=>`<div class="gantt-week">${d}<span>${span===35?'Mon &nbsp; Tue &nbsp; Wed &nbsp; Thu &nbsp; Fri':'Calendar dates · manual forecast'}</span></div>`).join('')}</div></div>${items.filter(t=>t.start).map(t=>{const left=pos(t.start),w=Math.max(2.1,pos(t.end)-left+100/span),proposed=s.ui.compare&&t.proposedStart;return `<div class="gantt-line"><div class="gantt-labels">${btn(esc(t.title),'open-task','','link',`data-id="${t.id}"`)}${t.conflict?icon('warning'):''}</div><div class="gantt-track">${t.id==='t-install'?`<span class="gantt-connector" style="left:${pos('2026-09-21')}%;width:${Math.max(1,pos(t.start)-pos('2026-09-21'))}%" aria-hidden="true"></span>`:''}<button type="button" class="bar ${t.state} ${t.milestone?'milestone':''}" style="left:${left}%;width:${w}%" data-action="open-task" data-id="${t.id}" aria-label="${esc(t.title+' · '+dt(t.start)+(t.end!==t.start?' to '+dt(t.end):''))}" title="${esc(t.title+' · '+dt(t.start))}"></button>${proposed?`<button type="button" class="bar proposed-bar" style="left:${pos(t.proposedStart)}%;width:${Math.max(2.1,pos(t.proposedEnd)-pos(t.proposedStart)+100/span)}%" data-action="open-task" data-id="${t.id}" aria-label="Proposed ${esc(t.title+' · '+dt(t.proposedStart))}"></button>`:''}</div></div>`;}).join('')}<div class="today-line" style="left:calc(240px + (100% - 240px)*${pos(s.today)/100})" aria-hidden="true"></div></div></div>${items.filter(t=>!t.start).map(t=>`<div class="undated">${icon('calendar')}<span>${esc(t.title)} · Date not recorded</span>${btn('Inspect','open-task','arrow','link',`data-id="${t.id}"`)}</div>`).join('')}`;}
  function changes(){const c=M.activeChange(s),queue=s.changes.filter(x=>M.changeInScope(s,x)&&x.title.toLowerCase().includes(s.ui.changeSearch.toLowerCase()));return toolsBar('Change reviews','Compare the recorded position, then coordinate a reviewed response.','New change review','new-change','plus')+`<div class="change-workspace ${s.ui.queueOnly?'show-queue':''}"><aside class="card change-queue" aria-label="Change review queue"><div class="queue-toolbar"><div class="search-field">${icon('search')}<input type="search" id="change-search" aria-label="Search change reviews" placeholder="Search reviews" data-filter="changeSearch" value="${esc(s.ui.changeSearch)}"></div></div>${queue.length?queue.map(x=>{const p=M.latestPreview(x);return `<button type="button" class="queue-item" data-action="select-change" data-id="${x.id}" aria-pressed="${c?.id===x.id}"><span class="small">${esc(x.id)}</span><strong>${esc(x.title)}</strong><div class="row wrap">${statePill(x.lifecycle)}${p&&M.isStale(s,p)?chip('Source changed','risk'):''}</div><span class="small">${esc(x.scope==='pump'?'Shared pump · 3 growing areas':'Valve kit · separate work')}</span></button>`;}).join(''):`<div class="empty"><h3>No change reviews</h3><p>${s.ui.changeSearch?'No matches for this search.':'Create a review when a proposed change needs investigation.'}</p></div>`}</aside><div class="change-detail">${btn('All change reviews','show-queue','back','back-to-queue')}${c?changeDetail(c):`<div class="card empty">${icon('changes')}<h2>No change selected</h2><p>Select a review or create one for the selected project.</p>${btn('New change review','new-change','plus','primary',isLocked()?'disabled':'')}</div>`}</div></div>`;}
  function changeDetail(c){const p=M.latestPreview(c),stale=p&&(p.proposalRevision!==c.revision||M.isStale(s,p)),locked=isLocked()||['Resolved','Withdrawn'].includes(c.lifecycle),proposal=draftBuffers[c.id]||c,steps=['Define','Preview impact','Compare responses','Obtain reviews','Request actions','Track outcomes'],step=!p?0:stale?1:c.lifecycle==='Actions requested'?5:M.validReviews(s,c)?4:3;return `<div class="change-heading"><div><span class="eyebrow">${esc(c.id)} · proposal r${String(c.revision).padStart(2,'0')}</span><h2>${esc(c.title)}</h2><div class="row wrap">${statePill(c.lifecycle)}${chip(scopeLabel(c.scope),'neutral')}${p?chip(p.id.split('-').slice(-1)[0],stale?'risk':'info'):''}</div></div>${btn('','change-menu','settings','icon-button',`data-id="${c.id}" aria-label="Change review options"`)}</div><ol class="progress-steps" aria-label="Review stages">${steps.map((x,i)=>`<li class="${step===i?'current':''}"><span class="step-number">${i+1}</span>${x}</li>`).join('')}</ol><div class="stack">${stale?notice('risk','Comparison outdated — confirmation is blocked','The proposal or a captured source has changed. Your draft and previous reviews remain available. Refresh the comparison, then obtain the affected responses.',btn('Refresh comparison','preview','refresh','link')):p?notice('info','Captured comparison · '+p.id.split('-').pop(),`Proposal r${p.proposalRevision} · ${p.sources.length} exact source revisions. Technical decisions, booking acceptance and document issue retain their own owners.`):notice('info','Start with a captured comparison','The proposed dates are a scenario. Preview the impact to retain the source revisions used for review.')}<details id="proposal-editor" ${editorOpen[c.id]?'open':''}><summary>1. Define the change <span class="muted">· ${esc(c.type)} · ${draftBuffers[c.id]?'editing':'saved in this session'}</span></summary><div class="details-body"><form id="proposal-form" data-change="${c.id}"><fieldset ${locked?'disabled':''} style="border:0;padding:0;margin:0"><div class="form-grid">${field('Change title','title',proposal.title,'text','maxlength="180"')}${selectField('Change type','type',['Supplier delay','Receipt / quarantine','Substitution','Site access','Scope / design'],proposal.type)}${selectField('Affected work package','scopeEdit',s.project==='controls'?[['survey','Controls survey']]:[['pump','WP01 · Shared pump'],['components','WP02 · Valve kit']],proposal.scope)}<div class="field span-2"><label for="reason">Reason and intended effect</label><textarea id="reason" name="reason" maxlength="2000">${esc(proposal.reason)}</textarea></div>${field('Proposed delivery','proposedDelivery',proposal.proposedDelivery,'date')}${field('Installation forecast start','installStart',proposal.installStart,'date')}${field('Installation forecast finish','installEnd',proposal.installEnd,'date')}${field('Requested commissioning visit','visitDate',proposal.visitDate,'date')}<div class="field span-2"><span class="field-label" id="sourceIds" tabindex="-1">Evidence linked to this proposal</span><div class="row wrap">${M.sourceIds(s).map(id=>`<label class="check"><input type="checkbox" name="sources" value="${id}" ${proposal.sourceIds.includes(id)?'checked':''}>${esc(s.sources[id].title)}</label>`).join('')}</div></div></div><div class="row wrap" style="margin-top:16px">${btn('Save proposal','save-proposal','check','primary')}${btn('Discard edits','discard-edits','','')}${btn('Request clarification','clarify','plus','',`data-origin="${c.id}" data-owner="Alex Morgan"`)}<span class="small muted">Closing this section preserves entered values.</span></div></fieldset></form></div></details><div class="comparison-grid"><section class="position current"><span class="eyebrow">Current recorded position</span><h3>${c.scope==='components'?'Shared component allocation':'Existing plan and commitments'}</h3>${c.scope==='components'?`<dl class="kv"><dt>Project demand / usable allocation</dt><dd>${M.quantities(s).projectDemand} EA / ${M.quantities(s).projectAllocation} EA</dd><dt>Service demand / usable allocation</dt><dd>${M.quantities(s).serviceDemand} EA / ${M.quantities(s).serviceAllocation} EA</dd><dt>Quarantined within receipt</dt><dd>${M.quantities(s).quarantine} of ${M.quantities(s).received} EA</dd></dl>`:`<dl class="kv"><dt>Original pump arrival forecast</dt><dd>21 Sep 2026</dd><dt>Installation forecast</dt><dd>${dt(s.tasks.find(t=>t.id==='t-install').start)} – ${dt(s.tasks.find(t=>t.id==='t-install').end)}</dd><dt>Confirmed commissioning</dt><dd>${dt(M.current(s,'visit').date)} ${chip('Service booking','info')}</dd><dt>Customer shutdown</dt><dd>${dt(M.current(s,'access').start)} – ${dt(M.current(s,'access').end)}</dd></dl>`}</section><section class="position proposed"><span class="eyebrow">Proposed position</span><h3>${c.response==='alternative'?'Investigate an alternative':c.response==='stage'?'Investigate staged work':'Delay the affected work'}</h3><dl class="kv"><dt>Proposed pump / component arrival</dt><dd>${dt(c.proposedDelivery)} ${c.scope==='pump'?`<span class="delta">${Math.round((new Date(c.proposedDelivery)-new Date('2026-09-21'))/86400000)} days from original promise</span>`:''}</dd><dt>Installation forecast scenario</dt><dd>${dt(c.installStart)} – ${dt(c.installEnd)}</dd><dt>Requested commissioning</dt><dd>${dt(c.visitDate)} ${chip('Unconfirmed','risk')}</dd><dt>Customer window and preparation</dt><dd>${M.current(s,'access').start<=c.installStart&&M.current(s,'access').end>=c.visitDate?'Current evidence covers these dates; inspect before requesting.':'New agreement and preparation review required.'}</dd></dl></section></div><section class="card"><header class="card-head"><div><h2>2. Impact preview</h2><p class="small muted">${p?esc(p.id)+' · captured proposal r'+p.proposalRevision:'Working scenario · capture before reviewing'}</p></div>${btn(p?'Refresh comparison':'Capture comparison','preview',p?'refresh':'changes','primary',locked?'disabled':'')}</header><div class="table-scroll" tabindex="0" role="region" aria-label="Impact comparison"><table class="register impact-table"><thead><tr><th>Affected item</th><th>Current position</th><th>Proposed effect</th><th>Required response</th></tr></thead><tbody>${(p?p.impacts:M.impacts(s,c)).map(x=>`<tr><td><strong>${esc(x.item)}</strong></td><td>${esc(x.current)}</td><td>${esc(x.proposed)}</td><td>${esc(x.response)}</td></tr>`).join('')}</tbody></table></div><footer class="card-foot"><div class="row wrap">${(p?p.sources:c.sourceIds.map(id=>({id,revision:M.current(s,id).revision}))).map(x=>sourceChip(x.id,x.revision)).join('')}</div></footer></section><section class="card"><header class="card-head"><div><h2>3. Compare response options</h2><p class="small muted">Select an option to revise the proposal. Earlier comparisons are retained.</p></div></header><div class="card-body"><div class="response-options">${[['delay','Delay affected work','Shift the forecast. Request a revised window, preparation and scheduler review.'],['alternative','Investigate alternative pump','Availability is indicative; compatibility and commissioning criteria are unknown.'],['stage','Investigate staged work','Independent isolation is not evidenced for any of the three served areas.']].map(([id,title,text])=>`<label class="response-option"><input type="radio" name="response" value="${id}" ${c.response===id?'checked':''} ${locked?'disabled':''}><div><strong>${title}</strong><span>${text}</span></div></label>`).join('')}</div>${c.response!=='delay'?`<div class="spacer"></div>${notice('risk','Investigation only',c.response==='alternative'?'The fixture does not establish technical compatibility. Create an Engineering clarification; execution requests remain blocked.':'All three growing areas share this pump. Staging cannot be confirmed without independent isolation evidence.',btn('Request investigation','clarify','plus','link',`data-origin="${c.id}" data-owner="Riley Chen"`))}`:''}</div></section>${commercial(c,p)}<section class="card"><header class="card-head"><div><h2>4. Specialist responses</h2><p class="small muted">Responses are bound to this proposal and their relevant source revisions.</p></div>${chip(M.validReviews(s,c)?'Required responses current':'Reviews required',M.validReviews(s,c)?'good':'risk')}</header><div class="review-list">${M.requiredReviews(c).map(role=>{const r=c.reviews.filter(x=>x.role===role).at(-1),valid=r&&M.reviewValid(s,c,r);return `<div class="review-row"><div><strong>${role}</strong><div class="small muted">${esc(M.roleOwner[role])}</div></div><div>${r?statePill(valid?r.outcome:'Earlier response · outdated'):chip('Awaiting response','neutral')}<p>${r?esc(role==='Commercial'&&!M.canCost(s)?'Commercial response details are restricted.':r.comment):'Review the exact captured proposal; record scope, reason and conditions.'}</p>${r?`<span class="small muted">Proposal r${r.proposalRevision} · ${esc(r.previewId.split('-').pop())}</span>`:''}</div>${btn('Record response','review','','',`data-role="${role}" ${locked?'disabled':''}`)}</div>`;}).join('')}</div></section><section class="card"><header class="card-head"><div><h2>5. Coordinate specific actions</h2><p class="small muted">Confirm the requests, owners and due dates before creating the local follow-up set.</p></div></header><div class="card-body stack">${c.requestIds.length?c.requestIds.map(id=>{const a=s.actions.find(x=>x.id===id);return `<div class="request-row"><div class="row between wrap"><strong>${esc(a.title)}</strong>${statePill(a.status)}</div><p class="small muted">${esc(a.id)} · ${esc(a.owner)} · ${dt(a.due)}</p>${btn('Inspect request and response','open-action','arrow','link',`data-id="${a.id}"`)}</div>`}).join(''):notice('info','No execution requests created','Clarifications can be opened while investigating. Coordination requests require current applicable reviews.')}<div class="row wrap">${btn('Review follow-up requests','confirm-requests','actions','primary',locked?'disabled':'')}${btn('Request clarification','clarify','plus','',`data-origin="${c.id}" data-owner="Alex Morgan" ${locked?'disabled':''}`)}${s.pendingOperation?btn('Recover original receipt','recover','refresh'):''}</div>${c.requestIds.length?'<p class="small muted">Request responses do not automatically resolve the parent change or declare delivery ready.</p>':''}</div></section>${p?`<section class="card"><header class="card-head"><div><h2>6. Review record</h2><p class="small muted">${c.previews.length} captured comparison${c.previews.length===1?'':'s'} · ${c.reviews.length} specialist response${c.reviews.length===1?'':'s'}</p></div><div class="row wrap">${btn('Download review','export','download','',`data-preview="${p.id}"`)}${btn('Print','print','print','',`data-preview="${p.id}"`)}</div></header><div class="card-body"><div class="row wrap">${c.previews.map(x=>btn(x.id.split('-').pop()+' · proposal r'+x.proposalRevision,'open-preview','evidence','',`data-id="${x.id}"`)).join('')}</div>${c.notes.filter(n=>M.canCost(s)||(n.role!=='Commercial'&&n.source?.id!=='cost')).map(n=>`<div class="revision-item" style="margin-top:12px"><strong class="small">Reviewed coordination note</strong><p class="small">${esc(n.text)}</p></div>`).join('')}${c.resolution?`<p class="small" style="margin-top:12px">${esc(c.resolution)}</p>`:''}</div></section>`:''}</div>`;}
  function commercial(){if(!M.canCost(s))return notice('info','Commercial detail restricted','This role can see that a Commercial review is required. Cost lines, subtotals, notes and export values are unavailable.');const x=M.current(s,'cost');return `<section class="card"><header class="card-head"><div><h2>Commercial context</h2><p class="small muted">Fictional internal coordination estimate · AUD, ex GST</p></div>${chip('Incomplete estimate','risk')}</header><div class="card-body"><dl class="kv"><dt>Additional freight</dt><dd>AUD ${x.freight.toFixed(2)} ex GST</dd><dt>Remobilisation</dt><dd>AUD ${x.remobilisation.toFixed(2)} ex GST</dd><dt>Known subtotal</dt><dd>AUD ${(x.freight+x.remobilisation).toFixed(2)} ex GST</dd><dt>Additional labour / alternative / crop effects</dt><dd>Unknown · not included in subtotal</dd><dt>Customer variation</dt><dd>Value and authority not recorded</dd></dl><p class="small muted" style="margin-top:12px">No spend approval, selling price or revenue recognition follows from this estimate.</p></div></section>`;}
  function actions(){const all=s.actions.filter(a=>M.actionInScope(s,a)),items=all.filter(a=>(s.ui.actionStatus==='all'||s.ui.actionStatus==='open'&&!['Completed','Accepted','Refused'].includes(a.status)||a.status===s.ui.actionStatus)&&[a.id,a.title,a.owner].join(' ').toLowerCase().includes(s.ui.actionSearch.toLowerCase()));return toolsBar('Owned actions','Clarification and coordination follow-up, with retained outcomes.','New clarification','clarify','plus')+`<div class="filter-bar"><div class="search-field">${icon('search')}<input type="search" id="action-search" aria-label="Search actions" placeholder="Search actions, references or owners" data-filter="actionSearch" value="${esc(s.ui.actionSearch)}"></div><select aria-label="Action status" data-filter="actionStatus">${options([['all','All action states'],['open','Open & awaiting response'],'Open','Pending response','Returned','Resubmitted','Accepted','Refused','Completed'],s.ui.actionStatus)}</select></div><section class="card">${items.length?`<div class="table-scroll"><table class="register card-register"><thead><tr><th style="width:31%">Action / originating record</th><th style="width:16%">Owner</th><th style="width:18%">Status / outcome</th><th style="width:17%">Due</th><th style="width:18%">Type</th></tr></thead><tbody>${items.map(a=>`<tr class="record"><td><span class="mobile-label">Action / origin</span>${btn(esc(a.title),'open-action','','link record-title',`data-id="${a.id}"`)}<span class="cell-sub">${esc(a.id)} · ${esc(a.changeId||a.origin)}</span></td><td><span class="mobile-label">Owner</span>${esc(a.owner)}</td><td><span class="mobile-label">Status / outcome</span>${statePill(a.status)}<span class="cell-sub">${esc(a.outcome||'Outcome not recorded')}</span></td><td><span class="mobile-label">Due</span>${dt(a.due)}</td><td><span class="mobile-label">Type</span>${esc(a.kind)}<span class="cell-sub">Submission / revision ${a.version}</span></td></tr>`).join('')}</tbody></table></div>`:`<div class="empty">${icon('actions')}<h3>${all.length?'No matching actions':'No actions recorded'}</h3><p>${all.length?'Broaden the search or status filter.':'Create an owned clarification when evidence or a response is needed.'}</p></div>`}<footer class="register-footer"><span>${items.length} shown · ${all.length} actions in this project</span><span>Completing an action does not clear its originating blocker.</span></footer></section>`;}
  function evidence(){const relevant=s.ui.scope==='all'&&s.ui.area==='all'?M.sourceIds(s):[...new Set([...M.scopedRows(s).map(r=>r.sourceId),...s.changes.filter(c=>M.changeInScope(s,c)).flatMap(c=>c.sourceIds)])];const ids=relevant.filter(id=>[s.sources[id].title,id].join(' ').toLowerCase().includes(s.ui.sourceSearch.toLowerCase())),history=s.history.filter(h=>h.project===s.project);return toolsBar('Evidence & history','Inspect exact revisions, captured comparisons and owned responses.','Download latest review','export','download')+`<div class="filter-bar"><div class="search-field">${icon('search')}<input type="search" id="source-search" aria-label="Search evidence" placeholder="Search source titles or references" data-filter="sourceSearch" value="${esc(s.ui.sourceSearch)}"></div><label class="check"><input type="checkbox" data-filter="showOld" ${s.ui.showOld?'checked':''}>Include superseded revisions</label></div><section class="card"><div class="table-scroll"><table class="register card-register"><thead><tr><th style="width:34%">Source</th><th style="width:17%">Revision / state</th><th style="width:25%">Captured as at</th><th>Owner</th></tr></thead><tbody>${ids.map(id=>(s.ui.showOld?s.sources[id].revisions:[M.current(s,id)]).map(x=>`<tr class="record"><td><span class="mobile-label">Source</span>${btn(esc(s.sources[id].title),'open-source','','link record-title',`data-id="${id}" data-rev="${x.revision}"`)}<span class="cell-sub">${esc(s.sources[id].kind)}</span></td><td><span class="mobile-label">Revision / state</span>r${String(x.revision).padStart(2,'0')}<span class="cell-sub">${s.sources[id].unavailable?'Current source unavailable':x.revision===M.current(s,id).revision?'Current recorded source':'Superseded · retained'}</span></td><td><span class="mobile-label">Captured as at</span>${esc(x.at)}</td><td><span class="mobile-label">Owner</span>${esc(s.sources[id].owner)}</td></tr>`).join('')).join('')}</tbody></table></div>${!ids.length?'<div class="empty"><h3>No matching evidence</h3><p>Clear the search to inspect available source records.</p></div>':''}</section><div class="spacer"></div><section class="card"><header class="card-head"><div><h2>Review history</h2><p class="small muted">Session events are chronological and preserve previous values.</p></div><span class="small muted">${history.length} events</span></header><div class="card-body">${history.map(h=>`<article class="history-item"><h3>${esc(h.kind)}</h3><p>${esc(h.sourceId==='cost'&&!M.canCost(s)?'Commercial evidence revised. Values and comparison are restricted for this role.':h.text)}</p><div class="small">${esc(h.actor)} · ${esc(h.at)}</div>${h.before&&!(h.sourceId==='cost'&&!M.canCost(s))?btn('Inspect before / after','open-history','evidence','link',`data-id="${h.id}"`):''}</article>`).join('')}</div></section>`;}
  function setPanel(type,selection){panelOpener=document.activeElement;s.ui.panel=type;if(selection)s.ui.selected=selection;renderPanel();layoutPanel();$('dock-title').focus({preventScroll:true});}
  function closePanel(){s.ui.panel=null;$('dock').className='dock';$('panel-backdrop').classList.remove('open');$('app').inert=false;document.body.classList.remove('docked');document.body.style.removeProperty('overflow');if(panelOpener?.isConnected)panelOpener.focus({preventScroll:true});else $('tab-'+s.ui.tab).focus({preventScroll:true});}
  function layoutPanel(){const active=!!s.ui.panel,width=s.ui.panel==='assistant'?480:448,modal=active&&window.innerWidth-width-48<440;document.documentElement.style.setProperty('--panel-width',width+'px');document.body.classList.toggle('docked',active&&!modal);$('dock').classList.toggle('open',active);$('dock').classList.toggle('assistant',s.ui.panel==='assistant');$('dock').setAttribute('role',modal?'dialog':'complementary');if(modal)$('dock').setAttribute('aria-modal','true');else $('dock').removeAttribute('aria-modal');$('app').inert=modal;$('panel-backdrop').classList.toggle('open',modal);document.body.style.overflow=modal?'hidden':'';}
  function renderPanel(){let content={title:'Snapshot',sub:'Selected project and scope',body:'',foot:''};const p=s.ui.panel;if(p==='snapshot')content=snapshot();if(p==='assistant')content=assistant();if(p==='guide')content=guide();if(p==='demo')content=demo();$('dock-kicker').textContent=p==='snapshot'?'Snapshot':p==='assistant'?'Powerplants One Assistant':p==='demo'?'Synthetic demonstration':'Page guide';$('dock-title').textContent=content.title;$('dock-subtitle').textContent=content.sub||'';$('dock-body').innerHTML=content.body;$('dock-foot').innerHTML=(content.foot||btn('Close','close-panel','',''))+(suspendedDecision?btn('Resume decision','resume-decision','back','primary'):'');}
  function snapshot(){const sel=s.ui.selected;if(!sel)return {title:'Project context',sub:currentProject().ref,body:`<div class="stack">${notice('info','Select a record to inspect its source','Readiness rows, materials, drawings and visits open their own record context.')}<dl class="kv"><dt>Project</dt><dd>${esc(currentProject().name)}</dd><dt>Work package</dt><dd>${esc(scopeLabel(s.ui.scope))}</dd><dt>Customer / site</dt><dd>${esc(currentProject().customer)}<br>${esc(currentProject().site)}</dd><dt>Coordinator</dt><dd>${esc(currentProject().coordinator)}</dd></dl>${sourceChip(s.project==='controls'?'survey':'relationship')}</div>`};
    if(sel.kind==='source')return sourceSnapshot(sel.id,sel.revision);
    if(sel.kind==='readiness'){const r=M.projectRows(s).find(x=>x.id===sel.id);if(!r)return {title:'Record no longer in context',body:notice('info','Select another record','The selected project has changed.')};const src=sourceSnapshot(r.sourceId);return {...src,title:r.title,sub:r.category+' · '+scopeLabel(r.scope),body:`<div class="stack">${notice(stateKind(r.state),r.state,esc(r.reason))}<dl class="kv"><dt>Assessment scope</dt><dd>${esc(scopeLabel(r.scope))}<br>${r.scope==='pump'?'Shared pump and all three served areas':'Separate work-package prerequisite'}</dd><dt>Owner</dt><dd>${esc(r.owner)}</dd><dt>Next action</dt><dd>${esc(r.action)}</dd><dt>Due</dt><dd>${dt(r.due)}</dd></dl>${src.body}</div>`,foot:btn('Request clarification','clarify','plus','primary',`data-origin="${r.id}" data-owner="${esc(r.owner)}" ${isLocked()?'disabled':''}`)+(r.sourceId==='components'?btn('Correct quantities','edit-quantities','edit','',isLocked()?'disabled':''):btn('Explain blocker','assistant','assistant'))};}
    if(sel.kind==='task'){const t=M.programme(s,M.activeChange(s)).find(x=>x.id===sel.id);if(!t)return {title:'Task unavailable',body:notice('info','Choose a task in this project','')};return {title:t.title,sub:t.booking?'Confirmed Service appointment':'Project forecast · '+scopeLabel(t.scope),body:`<div class="stack">${t.conflict?notice('risk','Programme conflict',esc(t.conflict)):''}<dl class="kv"><dt>Current start</dt><dd>${dt(t.start)}</dd><dt>Current finish</dt><dd>${dt(t.end)}</dd><dt>Proposed start</dt><dd>${dt(t.proposedStart)}</dd><dt>Proposed finish</dt><dd>${dt(t.proposedEnd)}</dd><dt>Dependency</dt><dd>${esc(t.dependency||'None recorded')} · ${esc(t.relation||'No relation')}</dd><dt>Owner</dt><dd>${esc(t.owner)}</dd></dl>${sourceChip(t.source)}${notice('info',t.booking?'Scheduler acceptance required':'Manual forecast',t.booking?'A proposal does not alter this booking. Inspect a Scheduling request to see its receiving response.':'Edit dates through the task form. Linked tasks will not move automatically.')}</div>`,foot:t.booking?btn('Open change review','select-change','changes','primary','data-id="DEMO-CHG-024001"'):btn('Edit forecast dates','edit-task','edit','primary',`data-id="${t.id}" ${isLocked()?'disabled':''}`)};}
    if(sel.kind==='action')return actionSnapshot(s.actions.find(a=>a.id===sel.id&&a.project===s.project));
    if(sel.kind==='preview'){const c=s.changes.find(x=>x.previews.some(p=>p.id===sel.id)),p=c?.previews.find(x=>x.id===sel.id);if(!p)return {title:'Comparison unavailable',body:''};const data=M.exportReview(s,c,p);return {title:'Captured comparison '+p.id.split('-').pop(),sub:c.id+' · proposal r'+p.proposalRevision,body:`<div class="stack">${notice(M.isStale(s,p)?'risk':'info',data.sourceState,'Captured proposal and sources below are preserved, including superseded evidence.')}<dl class="kv"><dt>Captured</dt><dd>${esc(p.at)}</dd><dt>Proposed delivery</dt><dd>${dt(p.proposal.proposedDelivery)}</dd><dt>Forecast</dt><dd>${dt(p.proposal.installStart)} – ${dt(p.proposal.installEnd)}</dd><dt>Requested visit</dt><dd>${dt(p.proposal.visitDate)}</dd><dt>Reason</dt><dd>${esc(p.proposal.reason)}</dd></dl><h3>Exact sources</h3><div class="revision-list">${p.sources.map(x=>`<div class="revision-item">${sourceChip(x.id,x.revision)}<p class="small">${esc(x.id==='cost'&&!M.canCost(s)?'Commercial source detail restricted.':x.snapshot.text)}</p></div>`).join('')}</div><h3>Responses recorded against this comparison</h3>${data.reviews.length?data.reviews.map(r=>`<div class="revision-item"><strong>${esc(r.role)} · ${esc(r.outcome)}</strong><p class="small">${esc(r.comment)}</p></div>`).join(''):'<p class="small muted">No specialist responses were recorded against this comparison.</p>'}</div>`,foot:btn('Download review','export','download','primary',`data-preview="${p.id}"`)+btn('Print','print','print','',`data-preview="${p.id}"`)};}
    if(sel.kind==='history'){const h=s.history.find(x=>x.id===Number(sel.id));return {title:h.kind,sub:h.at,body:`<div class="stack"><p>${esc(h.text)}</p><h3>Before</h3>${objectRows(h.before)}<h3>After</h3>${objectRows(h.after)}</div>`};}
    return {title:'Record context',body:notice('info','Choose a record','Inspect a prerequisite, source, task or action.')};
  }
  function objectRows(obj){return `<dl class="kv">${Object.entries(obj||{}).map(([key,val])=>`<dt>${esc(key)}</dt><dd>${esc(val===null?'Not recorded':typeof val==='object'?JSON.stringify(val):val)}</dd>`).join('')}</dl>`;}
  function sourceSnapshot(id,revision){const src=s.sources[id],x=revision?src?.revisions.find(x=>x.revision===revision):src&&M.current(s,id);if(!src||!x)return {title:'Source unavailable',body:notice('risk','Exact revision is unavailable','Keep the record reference and request clarification.')};const restricted=id==='cost'&&!M.canCost(s),old=x.revision!==M.current(s,id).revision;let body=`<div class="stack">${src.unavailable?notice('risk','Current source could not be loaded','The last captured revision is retained below. A new comparison cannot use this source until it is available.',btn('Retry source','retry-source','refresh','link',`data-id="${id}"`)):''}${old?notice('info','Superseded source — exact revision retained','Viewing a successor does not rewrite this earlier source or its reviewed comparison.'):''}<div class="row wrap">${chip('r'+String(x.revision).padStart(2,'0'),old?'neutral':'info')}${chip(src.kind,'neutral')}</div><p class="small muted">${esc(x.at)} · ${esc(src.owner)}</p>${restricted?notice('info','Commercial detail restricted','This source is available to Coordinator and Commercial demo roles.'): `<div class="document-sheet"><div class="eyebrow">Synthetic source extract</div><p>${esc(x.text)}</p></div>`}`;
    if(id==='components'){const q={...x,usable:x.received-x.quarantine,unreceived:x.required-x.received,projectUnmet:x.projectDemand-x.projectAllocation,serviceUnmet:x.serviceDemand-x.serviceAllocation};body+=`<section><h3>Receipt accounting</h3><div class="quantity-grid" style="margin-top:10px">${[['Demand',q.required],['Received',q.received],['Unreceived',q.unreceived],['Quarantined',q.quarantine],['Usable',q.usable],['Usable allocated',q.projectAllocation+q.serviceAllocation]].map(([l,v])=>`<div class="quantity-cell"><small>${l}</small><strong>${v} <span class="small">EA</span></strong></div>`).join('')}</div><p class="small muted" style="margin-top:10px">${q.received} received = ${q.quarantine} quarantined + ${q.usable} usable. Quarantine is counted once.</p></section><dl class="kv"><dt>Project demand / allocated / unmet</dt><dd>${q.projectDemand} / ${q.projectAllocation} / ${q.projectUnmet} EA</dd><dt>Service demand / allocated / unmet</dt><dd>${q.serviceDemand} / ${q.serviceAllocation} / ${q.serviceUnmet} EA</dd></dl>`;}
    if(id==='pump')body+=`<dl class="kv"><dt>Required</dt><dd>${x.required} ${x.unit}</dd><dt>Received</dt><dd>${x.received} ${x.unit}</dd><dt>Original promise</dt><dd>${dt(x.previousPromise)}</dd><dt>Current promise at this revision</dt><dd>${dt(x.promised)}</dd><dt>Usable allocation</dt><dd>0 EA · no receipt evidence</dd></dl>`;
    if(id==='visit')body+=`<dl class="kv"><dt>Visit reference</dt><dd>SYN-PPO-APT-024001</dd><dt>Recorded appointment</dt><dd>${dt(x.date)} · 07:00–15:00</dd><dt>Status</dt><dd>${esc(x.status)}</dd><dt>Scheduler</dt><dd>Taylor Brooks</dd><dt>Crew</dt><dd>${esc(x.crew)}</dd><dt>Local timezone</dt><dd>Australia/Melbourne</dd></dl>`;
    if(id==='drawing'||id==='pack')body+=objectRows(id==='drawing'?{drawing:x.drawingRevision,purpose:x.purpose,alternative:'Compatibility unassessed'}:{issue:x.packRevision,acknowledgement:x.acknowledgement,successorPrepared:x.ready?'Evidenced for stated scope':'Not recorded'});
    if(id==='cost'&&!restricted)body+=objectRows({freight:'AUD '+x.freight.toFixed(2)+' ex GST',remobilisation:'AUD '+x.remobilisation.toFixed(2)+' ex GST',knownSubtotal:'AUD '+(x.freight+x.remobilisation).toFixed(2)+' ex GST',labour:null,alternative:null,cropEffects:null,customerVariation:null});
    if(id==='relationship')body+=`<dl class="kv"><dt>Equipment identity</dt><dd>${esc(s.equipment.ref)}<br>${esc(s.equipment.id)}</dd><dt>Installed in</dt><dd>Irrigation Shed 01</dd><dt>Serves</dt><dd>${s.equipment.servedNames.map(esc).join('<br>')}</dd></dl>`;
    body+=`<h3>Source revision history</h3><div class="revision-list">${src.revisions.slice().reverse().map(r=>`<div class="revision-item"><div class="row between"><strong>r${String(r.revision).padStart(2,'0')}</strong>${btn('Inspect','open-source','arrow','link',`data-id="${id}" data-rev="${r.revision}"`)}</div><span class="small muted">${esc(r.at)}</span>${r.revision===x.revision?chip('Viewing this revision','neutral'):''}</div>`).join('')}</div></div>`;return {title:src.title,sub:src.kind,body,foot:btn('Request clarification','clarify','plus','primary',`data-origin="${id}" data-owner="${esc(src.owner)}" ${isLocked()?'disabled':''}`)+(id==='components'&&!old?btn('Correct quantities','edit-quantities','edit','',isLocked()?'disabled':''):'')};}
  function actionSnapshot(a){if(!a)return {title:'Action unavailable',body:''};const request=/request/.test(a.kind),pending=['Pending response','Resubmitted'].includes(a.status),canAct=!isLocked();return {title:a.title,sub:a.id+' · '+a.kind,body:`<div class="stack"><div>${statePill(a.status)}</div><dl class="kv"><dt>Responsible person</dt><dd>${esc(a.owner)}</dd><dt>Due</dt><dd>${dt(a.due)}</dd><dt>Originating record</dt><dd>${esc(a.changeId||a.origin)}</dd><dt>Request revision</dt><dd>${a.version}</dd><dt>Captured comparison</dt><dd>${a.previewId?btn(esc(a.previewId),'open-preview','','link',`data-id="${a.previewId}"`):'Not an execution request'}</dd><dt>Outcome</dt><dd>${esc(a.outcome||'Not yet recorded')}</dd></dl>${a.proposal?notice('info','Exact requested effect',`Installation ${dt(a.proposal.installStart)} – ${dt(a.proposal.installEnd)}; requested visit ${dt(a.proposal.visitDate)}. This is a coordination request, not a completed operational change.`):''}${a.kind==='Scheduling request'?notice('risk','Service scheduler response','Accepting requires current customer access, crew and preparation evidence against this exact request. Return or refuse it with a reason if these are missing.'):''}<div class="row wrap">${a.changeId?btn('Open originating change','select-change','changes','',`data-id="${a.changeId}"`):btn('Inspect originating evidence','origin','evidence','',`data-origin="${a.origin}"`)}</div><h3>Action history</h3>${a.history.length?a.history.slice().reverse().map(h=>`<article class="history-item"><h3>${esc(h.kind)}</h3><p>${esc(h.text)}</p><p>${esc(h.outcome||'')}</p><div class="small">${esc(h.actor)} · ${esc(h.at)}</div></article>`).join(''):'<p class="small muted">No response or reassignment recorded.</p>'}</div>`,foot:pending?btn('Record response','respond','actions','primary',`data-id="${a.id}" ${canAct?'':'disabled'}`)+btn('Reassign','reassign','person','',`data-id="${a.id}" ${canAct?'':'disabled'}`):a.status==='Returned'?btn('Resubmit reviewed request','resubmit','refresh','primary',`data-id="${a.id}" ${canAct?'':'disabled'}`)+btn('Reassign','reassign','person','',`data-id="${a.id}" ${canAct?'':'disabled'}`):!request&&a.status!=='Completed'?btn('Complete action','complete','check','primary',`data-id="${a.id}" ${canAct?'':'disabled'}`)+btn('Reassign','reassign','person','',`data-id="${a.id}" ${canAct?'':'disabled'}`):btn('Close','close-panel')};}
  function contextSource(){const x=s.ui.selected;if(x?.kind==='source')return x.id;if(x?.kind==='readiness')return M.projectRows(s).find(r=>r.id===x.id)?.sourceId||'pump';if(x?.kind==='task')return M.programme(s,M.activeChange(s)).find(t=>t.id===x.id)?.source||'pump';if(x?.kind==='action')return s.actions.find(a=>a.id===x.id)?.target==='visit'?'visit':'scope';return s.project==='controls'?'survey':'pump';}
  function assistant(){const id=contextSource(),src=s.sources[id],context=s.ui.assistantContext,stale=context&&context.revision!==M.current(s,context.id).revision;return {title:'Project coordination',sub:'Scripted assistance · review every suggestion',body:`<div class="stack"><div class="small muted">Context: ${esc(src.title)} · r${String(M.current(s,id).revision).padStart(2,'0')}</div><div class="segmented assistant-tabs" role="group" aria-label="Assistant sections">${['conversation','drafts','activity'].map(x=>btn(x[0].toUpperCase()+x.slice(1),'assistant-tab','',s.ui.assistantTab===x?'selected':'',`data-id="${x}"`)).join('')}</div>${stale?notice('risk','Source context changed','Refresh the response before using this suggestion. Its earlier source reference is retained.'):''}${s.ui.assistantTab==='conversation'?`<div class="prompt-list">${['Explain this blocker','What would this delay affect?','What evidence is missing?','Draft an internal coordination note'].map(x=>btn(esc(x),'assistant-prompt','arrow','',`data-prompt="${esc(x)}"`)).join('')}</div>${s.ui.assistantStatus==='loading'?`<div class="notice info"><span class="pulse"></span><div class="grow">Preparing a scripted response…</div>${btn('Stop','assistant-stop')}</div>`:s.ui.assistantStatus==='failed'?notice('risk','Response unavailable','No suggestion or action was applied. Retry when ready.',btn('Retry','assistant-retry','refresh','link')):s.ui.assistantStatus==='stopped'?notice('info','Response stopped','No action was created.',btn('Retry','assistant-retry','refresh','link')):s.ui.assistantText?`<div class="assistant-message">${esc(s.ui.assistantText)}</div><div class="row wrap">${sourceChip(context?.id||id,context?.revision)}${btn('Prepare editable note','assistant-draft','edit','',stale?'disabled':'')}</div>`:notice('info','Ask about the selected record','Responses use the synthetic source shown above. They cannot establish an unknown quantity, approve a substitution or move a booking.')}`:s.ui.assistantTab==='drafts'?`<div class="field"><label for="assistant-note">Internal coordination note</label><textarea id="assistant-note" rows="9" placeholder="Prepare a note from a response first.">${esc(s.ui.assistantDraft)}</textarea></div><p class="small muted">Editable draft only. Review its scope, uncertainties and source before adding it to this change.</p>${btn('Review note before adding','review-note','check','primary',(!s.ui.assistantDraft||stale||isLocked())?'disabled':'')}`:`${s.ui.assistantActivity.length?s.ui.assistantActivity.map(x=>`<article class="history-item"><p>${esc(x)}</p></article>`).join(''):'<p class="small muted">No assistance has been requested in this session.</p>'}`}</div>`,foot:btn('Open source','open-source','evidence','',`data-id="${id}"`)+btn('Close','close-panel')};}
  function guide(){return {title:'Using this workspace',sub:'Projects · Delivery Readiness & Change Control',body:`<div class="stack">${notice('info','Three questions','What is stopping delivery? What would a proposed change affect? Who needs to decide or act next?')}<ol class="guide-list"><li>Open a blocker in <strong>Readiness</strong> and inspect its exact source.</li><li>Open <strong>Changes</strong>. Edit the dates or select a response, then <strong>Capture comparison</strong>.</li><li>In <strong>Demo controls</strong>, choose the specialist role. Record each response against that proposal. A return requires a revision and a refreshed comparison.</li><li>As Coordinator, review the request set, owners and due dates; confirm creation in this session.</li><li>In <strong>Actions</strong>, the scheduler can return a request when access, crew or preparation is missing.</li><li>Use <strong>Add compatible October evidence</strong> in Demo controls, refresh the comparison and renew only affected reviews. Resubmit the same returned request, then record the scheduler response.</li><li>Inspect earlier sources, request outcomes and captured comparisons in <strong>Evidence & history</strong>.</li></ol><h3>Context stays connected</h3><p>Work-package and area selections carry between views. Shared-pump prerequisites apply to all served growing areas. Search filters never change the underlying assessment.</p><h3>Authority stays with its owner</h3><p>Project forecast, Service booking, Engineering assessment, document preparation and commercial authority are separate. A coordination request does not itself release equipment, issue a pack or approve spend.</p><h3>Local demonstration</h3><p><strong>Reload resets this demonstration.</strong> Edits and receipts exist only in page-session memory. Use review download or print to retain a visible review record. DEMO-CHG and DEMO-ACT labels are local review references, not allocated platform record types. Demo roles illustrate presentation; all fixtures are embedded and this file is not a security boundary.</p><h3>Keyboard and layout</h3><p>Use arrow keys, Home and End across tabs. Escape closes a panel or decision. Table column separators support left/right arrow keys. At phone width, registers become cards and Programme starts in List. Side panels become modal when less than 440 px would remain for the workspace.</p><h3>Source and revision</h3><p class="small muted">Review r01 · BP-06 / Projects J1 / F05 change impact.<br>Repository basis: bc1dcf2.<br>Theme: PPO r20, SHA-256 c68a499e…f19b617.<br>Existing customer/site/pump identities are reused. The project-specific shutdown agreement and all delivery, quantity, review and cost records are new synthetic fixtures.</p>${btn('Restore preview context header','show-header','overview')}${btn('Open demonstration controls','demo','settings')}</div>`};}
  function demo(){return {title:'Demonstration controls',sub:'Separate from the operational workspace',body:`<div class="stack">${notice('info','Session-only synthetic preview','Reload resets this demonstration. No live requests, messages, bookings or financial transactions are sent.')}<div class="demo-group">${selectField('Project scenario','demo-project',s.projects.map(p=>[p.id,p.name]),s.project,'data-demo="project"')}${selectField('Viewing / acting role','demo-role',M.roles,s.role,'data-demo="role"')}<p class="small muted">Each specialist records their own response. Coordinator confirms the request set. Delivery viewer has no editing or commercial detail.</p></div><div class="demo-group"><h3>Source and recovery scenarios</h3>${btn('Advance pump source revision','advance-source','refresh','',isLocked()?'disabled':'')}<p class="small muted">Adds a revised supplier note with the same 5 Oct promise. Captured comparisons become outdated; only affected specialist reviews lapse.</p>${btn('Add compatible October evidence','success-evidence','evidence','',isLocked()?'disabled':'')}<p class="small muted">Adds customer access for 6–9 Oct, crew evidence and preparation-owner successor r03. Refresh and review before resubmitting.</p>${btn('Make pump source unavailable','source-fail','warning','',s.project==='controls'?'disabled':'')}${btn('Simulate source loading','source-loading','refresh','',s.project==='controls'?'disabled':'')}</div><div class="demo-group">${selectField('Next request confirmation','demo-fault',[['none','Normal response'],['before','Fail before acceptance'],['after','Lose response after acceptance']],s.fault,'data-demo="fault"')}<p class="small muted">A lost response requires recovery of the original receipt. Retrying never creates a second request set.</p>${s.pendingOperation?btn('Recover original receipt','recover','refresh','primary'):''}${selectField('Next Assistant response','assistant-fault',[['none','Normal scripted response'],['fail','Response unavailable']],s.assistantFault,'data-demo="assistantFault"')}</div><div class="demo-group"><h3>Review aids</h3>${btn('Load long-text example','long-text','edit','',isLocked()?'disabled':'')}${btn('Reset complete demonstration','reset','refresh','danger')}<p class="small muted">Reset clears all local drafts, receipts, source revisions, actions and role changes after confirmation.</p></div></div>`};}
  function openDecision(config){opener=document.activeElement;decision=config;$('decision-title').textContent=config.title;$('decision-subtitle').textContent=config.sub||'';$('decision-body').innerHTML=config.body;$('decision-foot').innerHTML=`<span class="small muted">${esc(config.effect||'Local synthetic demonstration only')}</span>${btn(config.cancel||'Cancel','close-decision')}<button type="submit" class="primary">${esc(config.submit||'Save in this session')}</button>`;if(!$('decision').open)$('decision').showModal();setTimeout(()=>{const input=$('decision-body').querySelector('input:not([type=checkbox]),textarea,select');(input||$('decision-title')).focus();},0);}
  function closeDecision(){if(!$('decision').open)return;$('decision').close();decision=null;if(opener?.isConnected)opener.focus({preventScroll:true});else if(s.ui.panel)$('dock-title').focus({preventScroll:true});else $('tab-'+s.ui.tab).focus({preventScroll:true});}
  function decisionError(err){const old=$('decision-body').querySelector('.error-summary');if(old)old.remove();const div=document.createElement('div');div.className='error-summary';div.setAttribute('role','alert');div.tabIndex=-1;div.innerHTML=`<strong>Review the following before continuing</strong><p>${esc(err.message||err)}</p>`;$('decision-body').prepend(div);div.focus();}
  function proposalFromForm(){const form=$('proposal-form');if(!form)return null;const data=new FormData(form);return {title:String(data.get('title')||''),type:String(data.get('type')||''),scope:String(data.get('scopeEdit')||''),reason:String(data.get('reason')||''),proposedDelivery:String(data.get('proposedDelivery')||''),installStart:String(data.get('installStart')||''),installEnd:String(data.get('installEnd')||''),visitDate:String(data.get('visitDate')||''),sourceIds:data.getAll('sources')};}
  function saveProposal(){const c=M.activeChange(s),patch=proposalFromForm()||draftBuffers[c.id];if(!patch)return c;if(JSON.stringify(patch)===JSON.stringify(Object.fromEntries(Object.keys(patch).map(k=>[k,c[k]])))){delete draftBuffers[c.id];return c;}try{M.updateChange(s,c,patch);delete draftBuffers[c.id];s.ui.save='Saved in this session';render();toast('Proposal saved. Capture a new comparison before requesting execution.');return c;}catch(err){const form=$('proposal-form');form?.querySelector('.error-summary')?.remove();if(form){const div=document.createElement('div');div.className='error-summary';div.tabIndex=-1;div.setAttribute('role','alert');div.innerHTML='<strong>Check the proposal</strong><ul>'+Object.entries(err.fields||{reason:err.message}).map(([k,v])=>`<li><a href="#${k==='scope'?'scopeEdit':k}">${esc(v)}</a></li>`).join('')+'</ul>';form.prepend(div);for(const k of Object.keys(err.fields||{}))$(k==='scope'?'scopeEdit':k)?.setAttribute('aria-invalid','true');div.focus();}throw err;}}
  function confirmRequests(){const c=M.activeChange(s);if(!c)throw Error('Select a change review first.');if(s.pendingOperation){M.recover(s);render();toast('Original receipt recovered. No duplicate actions created.');return;}M.executionGuard(s,c);const p=M.latestPreview(c),items=M.requests(s,c),existing=s.receipts[p.id+'-requests'];if(existing){toast('This comparison already has '+existing.ids.length+' requests. Opening the original action.');setPanel('snapshot',{kind:'action',id:existing.ids[0]});return;}openDecision({title:'Confirm coordination requests',sub:c.id+' · '+p.id.split('-').pop()+' · proposal r'+c.revision,effect:'Creates '+items.length+' owned requests. Does not move a booking.',submit:'Create '+items.length+' requests',body:`<div class="stack">${notice('info','Review the exact follow-up set','These requests go to the synthetic receiving owners shown below. Their responses remain separate from this change review.')}<div>${items.map((x,i)=>`<div class="request-form-row"><div><strong>${esc(x.title)}</strong><p class="small muted">${esc(x.kind)} · target ${esc(x.target)}</p></div>${selectField('Owner','request-owner-'+i,M.owners,x.owner)}${field('Due date','request-due-'+i,x.due,'date','required')}</div>`).join('')}</div><label class="check"><input type="checkbox" name="confirmEffect" required>I have reviewed the scope, owners and requested effects.</label></div>`,onSubmit:data=>{const updated=items.map((x,i)=>({...x,owner:String(data.get('request-owner-'+i)),due:String(data.get('request-due-'+i))}));const result=M.confirmRequests(s,c,updated);closeDecision();render();if(result.uncertain)openDecision({title:'Save not confirmed',sub:result.key,effect:'Recover the accepted operation before trying a new effect.',submit:'Recover original receipt',body:notice('risk','Response lost after acceptance','The demonstration accepted the request set, but its response was lost. Recover the original receipt to see the created requests; do not start a second set.'),onSubmit:()=>{const r=M.recover(s);closeDecision();render();toast(r.ids.length+' original requests recovered. No duplicates.');}});else toast(result.ids.length+' requests created. Receiving owners must respond.');}});}
  function reviewDialog(role){const c=M.activeChange(s);if(s.role!==role)throw Error('Choose the '+role+' role in Demo controls to record this specialist response.');const p=M.latestPreview(c);if(!p||p.proposalRevision!==c.revision||M.isStale(s,p))throw Error('Capture a current comparison before recording a review.');openDecision({title:role+' response',sub:c.id+' · '+p.id.split('-').pop()+' · proposal r'+c.revision,submit:'Record response',effect:'Applies to this exact proposal and relevant sources.',body:`<div class="stack">${notice('info','Purpose of this response',role==='Engineering'?'Assess whether this coordination response can proceed. Supporting coordination does not technically release an alternative pump.':role==='Commercial'?'Assess the coordination basis and unresolved effects. This does not approve spend, customer price or revenue.':'Assess the coordination proposal and record conditions. Operational acceptance remains a separate receiving action.')}${selectField('Outcome','review-outcome',['Support coordination','Return for revision','Further evidence needed'], 'Support coordination')}<div class="field"><label for="review-comment">Scope, reason and conditions</label><textarea id="review-comment" name="review-comment" required placeholder="State what was reviewed, what is supported and what remains open."></textarea></div><div class="row wrap">${p.sources.map(x=>sourceChip(x.id,x.revision)).join('')}</div></div>`,onSubmit:data=>{M.review(s,c,role,String(data.get('review-outcome')),String(data.get('review-comment')));closeDecision();render();toast(role+' response recorded against proposal r'+c.revision+'.');}});}
  function clarification(origin,owner){if(isLocked())throw Error('This role can inspect records but cannot create actions.');const c=M.activeChange(s);origin=origin||s.ui.selected?.id||c?.id||currentProject().ref;const r=M.projectRows(s).find(x=>x.id===origin);openDecision({title:'New clarification',sub:'Origin: '+origin,effect:'Creates one owned local action; no message is sent.',submit:'Create clarification',body:`<div class="stack">${field('Action title','action-title',r?.action||'Clarify the evidence needed for this scope','text','required maxlength="240"')}<div class="form-grid">${selectField('Responsible person','action-owner',M.owners,owner||r?.owner||'Alex Morgan')}${field('Due date (optional)','action-due','2026-09-16','date')}</div><p class="small muted">If the date is missing, the action explicitly retains “Date not recorded”.</p></div>`,onSubmit:data=>{const a=M.createAction(s,{title:String(data.get('action-title')),owner:String(data.get('action-owner')),due:String(data.get('action-due')),origin,changeId:s.changes.some(x=>x.id===origin)?origin:null});closeDecision();render();setPanel('snapshot',{kind:'action',id:a.id});toast('Clarification saved as '+a.id+'.');}});}
  function actionDialog(action,id){const a=s.actions.find(x=>x.id===id&&x.project===s.project);if(!a)throw Error('Action not found in this project.');if(action==='reassign'){openDecision({title:'Reassign action',sub:a.id,submit:'Save reassignment',body:`<div class="stack">${selectField('Responsible person','reassign-owner',M.owners,a.owner)}${field('Due date (optional)','reassign-due',a.due,'date')}<div class="field"><label for="reassign-reason">Reason for reassignment</label><textarea id="reassign-reason" name="reassign-reason" required></textarea></div></div>`,onSubmit:data=>{M.reassign(s,a,String(data.get('reassign-owner')),String(data.get('reassign-due')),String(data.get('reassign-reason')));closeDecision();render();toast('Owner and due-date history retained.');}});return;}
    if(action==='resubmit'){const c=s.changes.find(x=>x.id===a.changeId);M.executionGuard(s,c);openDecision({title:'Resubmit the same request',sub:a.id+' · revised comparison '+M.latestPreview(c).id,effect:'Retains the action identity and earlier returned response.',submit:'Resubmit request',body:`<div class="stack">${notice('info','Previous response',esc(a.outcome))}<div class="field"><label for="response-reason">What changed and why this now addresses the return</label><textarea id="response-reason" name="response-reason" required></textarea></div></div>`,onSubmit:data=>{M.resubmit(s,a,c,String(data.get('response-reason')));closeDecision();render();toast('Same request resubmitted; earlier response retained.');}});return;}
    openDecision({title:action==='complete'?'Complete clarification':'Receiving owner response',sub:a.id+' · '+a.owner,submit:action==='complete'?'Record completion':'Record response',effect:action==='complete'?'Retains the outcome; the originating readiness state stays evidence-based.':'Acceptance applies only to the stated receiving request.',body:`<div class="stack">${action==='complete'?'':selectField('Response','response-status',['Accepted','Returned','Refused'],'Returned')}${a.kind==='Scheduling request'?notice('risk','Scheduler authority','Accept checks current request sources, customer access, crew and preparation. Missing evidence must be addressed before booking acceptance.'):''}<div class="field"><label for="response-reason">${action==='complete'?'Completion outcome':'Response reason and conditions'}</label><textarea id="response-reason" name="response-reason" required></textarea></div></div>`,onSubmit:data=>{M.respond(s,a,action==='complete'?'Completed':String(data.get('response-status')),String(data.get('response-reason')));closeDecision();render();toast('Response recorded. '+(a.kind==='Scheduling request'&&a.status==='Accepted'?'The synthetic booking now reflects scheduler acceptance.':'The parent change remains separate.'));}});}
  function editQuantities(){const q=M.current(s,'components');openDecision({title:'Correct component evidence',sub:'Valve kit receipt & allocation · common unit EA',submit:'Record source revision',effect:'Adds a new source revision; earlier receipt evidence is retained.',body:`<div class="stack">${notice('info','Conserve receipt and allocation','Usable = received − quarantined. Project and Service allocations cannot exceed usable stock or their respective demand.')}<div class="form-grid">${[['required','Total demand'],['projectDemand','Project demand'],['serviceDemand','Service demand'],['received','Received'],['quarantine','Quarantined within receipt'],['projectAllocation','Usable project allocation'],['serviceAllocation','Usable Service allocation']].map(([k,l])=>field(l+' (EA)',k,q[k],'number','min="0" step="1" required')).join('')}</div><div class="field"><label for="quantity-reason">Evidence / correction reason</label><textarea id="quantity-reason" name="quantity-reason" required></textarea></div></div>`,onSubmit:data=>{const patch={};for(const key of ['required','projectDemand','serviceDemand','received','quarantine','projectAllocation','serviceAllocation'])patch[key]=Number(data.get(key));M.updateQuantities(s,patch,String(data.get('quantity-reason')));closeDecision();render();toast('New component revision recorded. Affected comparisons need refresh.');}});}
  function editTask(id){const t=s.tasks.find(x=>x.id===id);if(!t||t.booking)throw Error('Confirmed visits require a receiving scheduler response.');if(s.role!=='Coordinator')throw Error('Choose the Coordinator role to edit project forecast dates.');openDecision({title:'Edit project forecast',sub:t.title,effect:'Changes this task only. Dependent tasks and bookings do not move.',body:`<div class="stack"><div class="form-grid">${field('Forecast start','task-start',t.start,'date')}${field('Forecast finish','task-end',t.end,'date')}</div><p class="small muted">Leave both dates empty for an undated task. Dates on weekends are allowed but flagged for review.</p><div class="field"><label for="task-reason">Forecast reason</label><textarea id="task-reason" name="task-reason" required></textarea></div></div>`,onSubmit:data=>{const start=String(data.get('task-start')),end=String(data.get('task-end'));if((start&&!end)||(!start&&end)||end<start)throw Error('Provide both dates in start-to-finish order, or leave both empty.');const before={start:t.start,end:t.end};t.start=start||null;t.end=end||null;M.record(s,'Forecast edited',t.title+' · '+String(data.get('task-reason')),{before,after:{start:t.start,end:t.end}});M.advance(s,'programme',t.id==='t-install'?{installStart:t.start,installEnd:t.end}:{},'Manual forecast revision: '+t.title+' · '+dt(t.start)+' to '+dt(t.end));closeDecision();render();toast('Forecast saved. Dependencies were checked without moving other dates.');}});}
  function changeOptions(){const c=M.activeChange(s);if(!c)throw Error('Select a change first.');openDecision({title:'Change review state',sub:c.id,submit:'Record state',effect:'Existing receiving requests keep their current status.',body:`<div class="stack">${selectField('State','change-state',['Withdrawn','Resolved'],'Withdrawn')}<div class="field"><label for="state-reason">Reason / resolution outcome</label><textarea id="state-reason" name="state-reason" required></textarea></div>${notice('info','Separate change and delivery states','Resolve requires final responses to all requests. Resolving or withdrawing a review does not declare the project ready or silently cancel requests.')}</div>`,onSubmit:data=>{M.closeChange(s,c,String(data.get('change-state')),String(data.get('state-reason')));closeDecision();render();toast('Review state recorded with its reason.');}});}
  function newChange(){if(isLocked())throw Error('This role cannot create a review.');openDecision({title:'New change review',sub:currentProject().name,submit:'Create draft review',body:`<div class="stack">${field('Change title','new-title','','text','required maxlength="180"')}${selectField('Type','new-type',['Supplier delay','Receipt / quarantine','Substitution','Site access','Scope / design'],'Supplier delay')}${selectField('Work package','new-scope',s.project==='controls'?[['survey','Controls survey']]:[['pump','WP01 · Shared pump'],['components','WP02 · Valve kit']],s.project==='controls'?'survey':'pump')}<div class="field"><label for="new-reason">Reason and evidence to investigate</label><textarea id="new-reason" name="new-reason" required></textarea></div></div>`,onSubmit:data=>{const scope=String(data.get('new-scope')),type=String(data.get('new-type')),c=M.newChange('DEMO-CHG-'+String(24001+s.changes.length).padStart(6,'0'),{title:String(data.get('new-title')),reason:String(data.get('new-reason')),type,scope,project:s.project,sourceIds:scope==='survey'?['survey']:scope==='components'?['components','scope','cost']:['pump','access','visit','drawing','pack','crew','cost','relationship','programme',...(type==='Substitution'?['alternate']:[])],response:type==='Substitution'?'alternative':'delay'});s.changes.push(c);s.ui.selectedChange=c.id;s.ui.queueOnly=false;editorOpen[c.id]=true;s.ui.tab='changes';M.record(s,'Draft created',c.id+' · '+c.title);closeDecision();closePanel();render();toast('Draft review created. Define the proposed dates before capturing.');}});}
  function preview(){const c=M.activeChange(s);if(!c){newChange();return;}if(draftBuffers[c.id])saveProposal();const p=M.capture(s,c);s.ui.tab='changes';s.ui.queueOnly=false;render();toast(p.id.split('-').pop()+' captured. Current relevant specialist responses are retained.');}
  function exportData(previewId){let c=M.activeChange(s),p;if(previewId){c=s.changes.find(x=>x.project===s.project&&x.previews.some(y=>y.id===previewId));p=c?.previews.find(x=>x.id===previewId);}else p=c&&M.latestPreview(c);if(!p)throw Error('Capture a comparison in Changes before exporting a review.');return {c,p,data:M.exportReview(s,c,p)};}
  function reviewMarkup(d){return `<h1>Projects — Delivery Readiness &amp; Change Control</h1><p><strong>${esc(d.designation)}</strong></p><p>${esc(d.project)} · ${esc(d.site)}<br>${esc(d.changeId)} · ${esc(d.comparisonId)} · proposal r${d.proposalRevision}<br>Captured: ${esc(d.captured)} · visible role: ${esc(d.exportRole)}</p><p><strong>${esc(d.sourceState)}</strong></p><h2>Proposed scope and dates</h2>${objectRows(d.proposal)}<h2>Impact comparison</h2><table><thead><tr><th>Affected item</th><th>Current</th><th>Proposed</th><th>Required response</th></tr></thead><tbody>${d.impacts.map(x=>`<tr><td>${esc(x.item)}</td><td>${esc(x.current)}</td><td>${esc(x.proposed)}</td><td>${esc(x.response)}</td></tr>`).join('')}</tbody></table><h2>Exact source basis</h2>${d.sources.map(x=>`<div class="source-line"><strong>${esc(x.title)} · r${x.revision} · ${esc(x.at)}</strong><p>${esc(x.text)}</p></div>`).join('')}<h2>Specialist responses</h2>${d.reviews.length?d.reviews.map(x=>`<p><strong>${esc(x.role)} · ${esc(x.owner)} · ${esc(x.outcome)}</strong><br>${esc(x.comment)}</p>`).join(''):'<p>No responses recorded for this comparison.</p>'}${d.commercial?`<h2>Commercial basis</h2><p>Known subtotal: AUD ${d.commercial.knownSubtotal.toFixed(2)} ex GST. Labour, alternative, crop effects and customer variation: unknown. Estimate incomplete.</p>`:''}<h2>Reviewed internal notes</h2>${d.notes.length?d.notes.map(n=>`<p>${esc(n.text)}</p>`).join(''):'<p>No notes recorded.</p>'}<h2>Open follow-up</h2>${d.openItems.length?d.openItems.map(x=>`<p>${esc(x.id)} · ${esc(x.title)} · ${esc(x.owner)} · due ${dt(x.due)} · ${esc(x.status)}</p>`).join(''):'<p>No open requests linked to this review.</p>'}<p>${esc(d.limitations)}</p>`;}
  function exportReview(id,print=false){const {data,p}=exportData(id);$('print-review').innerHTML=reviewMarkup(data);if(print){window.print();return;}const text='# '+data.designation+'\n\n'+JSON.stringify(data,null,2),blob=new Blob([text],{type:'text/plain;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=p.id+'-internal-review.txt';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);toast('Exact captured review downloaded with role-visible information.');}
  function runAssistant(prompt){clearTimeout(assistantTimer);const run=++assistantRun,id=contextSource(),revision=M.current(s,id).revision;s.ui.assistantContext={id,revision,prompt,project:s.project,role:s.role};s.ui.assistantStatus='loading';s.ui.assistantTab='conversation';s.ui.assistantActivity.unshift('Requested: '+prompt+' · '+s.sources[id].title+' r'+revision);renderPanel();assistantTimer=setTimeout(()=>{if(run!==assistantRun)return;if(s.assistantFault==='fail'){s.assistantFault='none';s.ui.assistantStatus='failed';renderPanel();return;}const q=M.quantities(s),restricted=id==='cost'&&!M.canCost(s);let answer;if(restricted)answer='Commercial detail is unavailable for this role. A Commercial review is required; no amount or estimate can be inferred from this view.';else if(id==='components')answer=`The separate valve kit line has ${q.required} EA demand and ${q.received} EA received. ${q.quarantine} EA of that receipt is quarantined, leaving ${q.usable} EA usable. Project demand is ${q.projectDemand} EA with ${q.projectAllocation} EA allocated and ${q.projectUnmet} EA unmet. Service demand is ${q.serviceDemand} EA with ${q.serviceAllocation} EA allocated and ${q.serviceUnmet} EA unmet.\n\nMorgan Ellis needs release or replacement evidence. No quantity can be assumed available just because the shipment arrived.`;else if(id==='survey')answer='The controls survey brief is present, but controller revision and customer attendance are not evidenced. No delivery blocker has yet been assessed. Ask Riley Chen for the revision and Casey Taylor for attendance; do not infer readiness from zero blockers.';else if(id==='cost')answer='The known fictional coordination subtotal is AUD 1,200.00 ex GST: AUD 480.00 freight plus AUD 720.00 remobilisation. Labour, alternative pump difference, crop effects and customer variation remain unknown. This is an incomplete estimate and does not authorise spend or revenue.';else answer=`The pump promise is now ${dt(M.current(s,'pump').promised)}. The recorded installation forecast is ${dt(s.tasks.find(t=>t.id==='t-install').start)} to ${dt(s.tasks.find(t=>t.id==='t-install').end)}, while the commissioning visit remains ${dt(M.current(s,'visit').date)}.\n\nThe shared pump serves Greenhouse 01, Tunnel 01 and Propagation House 01. Independent staging is not evidenced. Review the customer shutdown window, crew availability, installation drawing and job-pack successor. Alex Morgan coordinates; Taylor Brooks decides the scheduling request.\n\nUncertainty: a supplier promise is not a usable receipt, and the alternative pump has no compatibility assessment.`;s.ui.assistantText=(prompt==='Draft an internal coordination note'?'Draft for internal review:\n\n':'')+answer;s.ui.assistantStatus='ready';s.ui.assistantActivity.unshift('Scripted response prepared; no actions applied. Source r'+revision+'.');renderPanel();announce('Scripted response ready for review.');},850);}
  function changeProject(value){s.ui.scroll[s.ui.tab]=window.scrollY;s.project=value;s.ui.scope='all';s.ui.area='all';s.ui.selected=null;s.ui.selectedChange=s.changes.find(c=>c.project===value)?.id||null;s.ui.search='';s.ui.state='all';s.ui.category='all';s.ui.owner='all';s.ui.changeSearch='';s.ui.actionSearch='';s.ui.sourceSearch='';s.ui.actionStatus='all';s.ui.attention=false;s.ui.myActions=false;s.ui.evidenceOnly=false;s.ui.queueOnly=false;s.ui.assistantText='';s.ui.assistantDraft='';s.ui.assistantContext=null;s.ui.assistantStatus='idle';clearTimeout(assistantTimer);assistantRun++;render();toast('Project context changed. Previous project drafts remain in this session.');}
  function tab(id){s.ui.scroll[s.ui.tab]=window.scrollY;s.ui.tab=id;render();window.scrollTo({top:s.ui.scroll[id]||0,behavior:'instant'});}
  function resetFilters(){Object.assign(s.ui,{search:'',state:'all',category:'all',owner:'all',attention:false,myActions:false,collapsed:[],evidenceOnly:false});}
  async function handle(action,el){const id=el?.dataset.id;switch(action){
    case 'tab':tab(el.dataset.tab);break;
    case 'hide-header':$('context-header').hidden=true;break;
    case 'show-header':$('context-header').hidden=false;toast('Preview context header restored.');break;
    case 'dismiss-toast':$('toast').classList.remove('show');break;
    case 'snapshot':setPanel('snapshot');break;
    case 'assistant':setPanel('assistant');break;
    case 'guide':setPanel('guide');break;
    case 'demo':setPanel('demo');break;
    case 'close-panel':closePanel();break;
    case 'close-decision':closeDecision();break;
    case 'metric-blocked':resetFilters();s.ui.state='Blocked';tab('readiness');break;
    case 'metric-evidence':resetFilters();s.ui.attention=true;s.ui.state='all';s.ui.evidenceOnly=true;tab('readiness');break;
    case 'metric-changes':s.ui.changeSearch='';s.ui.queueOnly=true;tab('changes');break;
    case 'metric-programme':tab('programme');break;
    case 'next-blocker':{const r=M.scopedRows(s).find(x=>x.state==='Blocked')||M.scopedRows(s).find(x=>x.state!=='Ready for stated scope');if(r)setPanel('snapshot',{kind:'readiness',id:r.id});else toast('No attention items in the selected scope.');break;}
    case 'open-readiness':setPanel('snapshot',{kind:'readiness',id});break;
    case 'open-source':if($('decision').open){suspendedDecision={config:decision,values:[...new FormData($('decision-form'))]};closeDecision();}setPanel('snapshot',{kind:'source',id,revision:Number(el.dataset.rev)||M.current(s,id).revision});break;
    case 'resume-decision':{const saved=suspendedDecision;suspendedDecision=null;closePanel();openDecision(saved.config);for(const input of $('decision-form').querySelectorAll('[name]')){const vals=saved.values.filter(([name])=>name===input.name).map(([,value])=>value);if(input.type==='checkbox'||input.type==='radio')input.checked=vals.includes(input.value);else if(vals.length)input.value=vals[0];}break;}
    case 'open-task':setPanel('snapshot',{kind:'task',id});break;
    case 'open-action':setPanel('snapshot',{kind:'action',id});break;
    case 'open-preview':setPanel('snapshot',{kind:'preview',id});break;
    case 'open-history':setPanel('snapshot',{kind:'history',id});break;
    case 'origin':{const r=M.projectRows(s).find(x=>x.id===el.dataset.origin);if(r)setPanel('snapshot',{kind:'readiness',id:r.id});else if(s.sources[el.dataset.origin])setPanel('snapshot',{kind:'source',id:el.dataset.origin});else toast('Origin reference: '+el.dataset.origin);break;}
    case 'clear-filters':resetFilters();s.ui.evidenceOnly=false;render();break;
    case 'collapse-group':s.ui.collapsed=s.ui.collapsed.includes(id)?s.ui.collapsed.filter(x=>x!==id):[...s.ui.collapsed,id];render();break;
    case 'select-change':if(!s.changes.some(c=>c.id===id&&c.project===s.project))throw Error('This change is outside the selected project.');s.ui.selectedChange=id;s.ui.queueOnly=false;if(s.ui.panel)closePanel();tab('changes');break;
    case 'show-queue':s.ui.queueOnly=true;render();break;
    case 'new-change':newChange();break;
    case 'save-proposal':saveProposal();break;
    case 'discard-edits':{const c=M.activeChange(s);delete draftBuffers[c.id];s.ui.save='Saved in this session';render();toast('Unsaved form edits discarded. Recorded proposal retained.');break;}
    case 'preview':preview();break;
    case 'review':reviewDialog(el.dataset.role);break;
    case 'confirm-requests':confirmRequests();break;
    case 'recover':{const r=M.recover(s);render();toast(r.ids.length+' original requests recovered; no duplicate set created.');break;}
    case 'clarify':clarification(el?.dataset.origin,el?.dataset.owner);break;
    case 'complete':case 'respond':case 'reassign':case 'resubmit':actionDialog(action,id);break;
    case 'edit-task':editTask(id);break;
    case 'edit-quantities':editQuantities();break;
    case 'change-menu':changeOptions();break;
    case 'programme-mode':s.ui.programmeMode=id;render();break;
    case 'programme-scale':s.ui.programmeScale=id;s.ui.programmeMode='gantt';render();break;
    case 'today':s.ui.programmeMode='gantt';render();$('timeline-scroll')?.scrollTo({left:0,behavior:'smooth'});toast('Timeline positioned at the demonstration date: 15 Sep 2026.');break;
    case 'advance-source':M.advance(s,'pump',{},'Supplier clarification r'+(M.current(s,'pump').revision+1)+': 5 Oct 2026 remains the promise; transport evidence is still pending. This is a source revision, not a receipt.');render();toast('Pump source revised. Captured comparisons are outdated.');break;
    case 'success-evidence':M.addSuccessEvidence(s);render();toast('Compatible October evidence added. Refresh the comparison and obtain affected reviews.');break;
    case 'source-fail':s.sources.pump.unavailable=true;render();toast('Pump source unavailable. Captured evidence and drafts retained.');break;
    case 'source-loading':s.loadState='loading';setPanel('snapshot',{kind:'source',id:'pump'});$('dock-body').innerHTML='<div role="status"><p>Loading current pump evidence…</p><div class="loading-placeholder"></div><div class="loading-placeholder"></div><div class="loading-placeholder"></div></div>';setTimeout(()=>{s.loadState='ready';if(s.ui.panel==='snapshot')renderPanel();announce('Pump evidence loaded.');},1200);break;
    case 'retry-source':s.sources[id].unavailable=false;render();toast('Current source available again. Recheck the comparison basis.');break;
    case 'long-text':{const c=M.activeChange(s);if(!c)throw Error('Select a change first.');M.updateChange(s,c,{title:'Review the shared irrigation pump delivery change across the nursery, protected cropping tunnels and propagation production areas',reason:'Synthetic long-content review: the nursery manager requires an explicit account of all affected growing areas, the access window, isolation assumptions and the limits of the supplier promise. Preserve the installation location separately from the three served-area relationships. Unresolved effects include customer attendance, the sequence of preparation and commissioning, and specialist availability. No independent staging or alternate compatibility is evidenced.'});render();toast('Long-text example loaded into the current proposal.');break;}
    case 'reset':openDecision({title:'Reset the whole demonstration?',submit:'Reset demonstration',effect:'Clears local work in all six views.',body:notice('risk','Local changes will be cleared','This resets drafts, source revisions, reviews, requests, history, role and filters to the original synthetic fixture. Cancel keeps everything.'),onSubmit:()=>{closeDecision();closePanel();s=M.seed();for(const key of Object.keys(draftBuffers))delete draftBuffers[key];for(const key of Object.keys(editorOpen))delete editorOpen[key];suspendedDecision=null;clearTimeout(assistantTimer);assistantRun++;if(window.innerWidth<700)s.ui.programmeMode='list';render();toast('Complete synthetic fixture restored.');}});break;
    case 'assistant-tab':s.ui.assistantTab=id;renderPanel();break;
    case 'assistant-prompt':runAssistant(el.dataset.prompt);break;
    case 'assistant-stop':clearTimeout(assistantTimer);assistantRun++;s.ui.assistantStatus='stopped';s.ui.assistantActivity.unshift('Response stopped; no action applied.');renderPanel();break;
    case 'assistant-retry':runAssistant(s.ui.assistantContext?.prompt||'Explain this blocker');break;
    case 'assistant-draft':s.ui.assistantDraft=s.ui.assistantText;s.ui.assistantTab='drafts';renderPanel();break;
    case 'review-note':{const c=M.activeChange(s),context=s.ui.assistantContext;if(!c)throw Error('Select a change to receive the coordination note.');if(context&&M.current(s,context.id).revision!==context.revision)throw Error('Source changed; refresh the assistant response first.');openDecision({title:'Review internal coordination note',sub:c.id,submit:'Add note to this review',effect:'Adds a local note only. No action or message is sent.',body:`<div class="stack"><div class="field"><label for="review-note-text">Editable note</label><textarea id="review-note-text" name="review-note-text" rows="10" required>${esc(s.ui.assistantDraft)}</textarea></div>${context?sourceChip(context.id,context.revision):''}</div>`,onSubmit:data=>{const text=String(data.get('review-note-text'));c.notes.push({text,source:context,role:s.role});M.record(s,'Coordination note added',c.id+' · editable Assistant draft reviewed before adding.');s.ui.assistantActivity.unshift('Reviewed note added to '+c.id+'.');closeDecision();render();toast('Reviewed internal note added. No action was executed.');}});break;}
    case 'export':exportReview(el?.dataset.preview);break;
    case 'print':exportReview(el?.dataset.preview,true);break;
  }}
  document.addEventListener('click',event=>{const el=event.target.closest('[data-action]');if(!el||el.disabled)return;event.preventDefault();try{const result=handle(el.dataset.action,el);if(result?.catch)result.catch(err=>error(err.message));}catch(err){error(err.message);}});
  $('decision-form').addEventListener('submit',event=>{event.preventDefault();try{decision?.onSubmit(new FormData(event.target));}catch(err){decisionError(err);}});
  $('decision').addEventListener('cancel',event=>{event.preventDefault();closeDecision();});
  document.addEventListener('submit',event=>{if(event.target.id==='proposal-form'){event.preventDefault();try{saveProposal();}catch(err){announce(err.message);}}});
  document.addEventListener('toggle',event=>{if(event.target.id==='proposal-editor'){const c=M.activeChange(s);if(c)editorOpen[c.id]=event.target.open;}},true);
  function changed(event){const el=event.target;if(el.id==='assistant-note'){s.ui.assistantDraft=el.value;return;}if(el.closest('#proposal-form')){const c=M.activeChange(s);if(c){draftBuffers[c.id]=proposalFromForm();s.ui.save='Editing';$('session-line').querySelector('.session-status').innerHTML=icon('edit')+'Editing · proposal draft retained';}return;}
    if(el.dataset.filter){if(el.tagName==='INPUT'&&['search','text'].includes(el.type)&&event.type==='change')return;if(el.tagName!=='INPUT'&&event.type==='input')return;const key=el.dataset.filter;s.ui[key]=el.type==='checkbox'?el.checked:el.value;if(['scope','area'].includes(key)){s.ui.selected=null;const c=M.activeChange(s);if(c&&!M.changeInScope(s,c))s.ui.selectedChange=s.changes.find(x=>M.changeInScope(s,x))?.id||null;announce('Scope changed. Hidden record selection cleared; shared pump prerequisites remain included.');}if(key==='changeSearch'&&M.activeChange(s)&&!M.activeChange(s).title.toLowerCase().includes(s.ui.changeSearch.toLowerCase())){s.ui.selectedChange=null;s.ui.queueOnly=true;}if(key==='state')s.ui.evidenceOnly=false;if(s.ui.tab==='readiness'&&s.ui.selected?.kind==='readiness'&&!M.visibleRows(s).some(r=>r.id===s.ui.selected.id)){s.ui.selected=null;announce('Selected prerequisite is hidden by the filter. Selection cleared.');}render();return;}
    if(el.dataset.demo&&event.type==='change'){const key=el.dataset.demo;if(key==='project')changeProject(el.value);else if(key==='role'){s.role=el.value;s.ui.assistantText='';s.ui.assistantDraft='';s.ui.assistantContext=null;s.ui.assistantStatus='idle';clearTimeout(assistantTimer);assistantRun++;$('print-review').innerHTML='';render();toast('Demo role: '+s.role);}else s[key]=el.value;return;}
    if(el.name==='response'&&event.type==='change'){try{const c=M.activeChange(s);M.updateChange(s,c,{response:el.value,sourceIds:el.value==='alternative'?[...new Set([...c.sourceIds,'alternate'])]:c.sourceIds});render();toast('Response option revised. Capture the updated comparison.');}catch(err){error(err.message);}}
  }
  document.addEventListener('input',changed);document.addEventListener('change',changed);
  document.addEventListener('keydown',event=>{const el=event.target;if(el.matches('[role=tab]')&&['ArrowLeft','ArrowRight','Home','End'].includes(event.key)){event.preventDefault();const tabs=[...$('tabs').querySelectorAll('[role=tab]')],i=tabs.indexOf(el),next=event.key==='Home'?0:event.key==='End'?tabs.length-1:(i+(event.key==='ArrowRight'?1:-1)+tabs.length)%tabs.length;tab(tabs[next].dataset.tab);$('tab-'+s.ui.tab).focus();}
    if(el.matches('.column-resize')&&['ArrowLeft','ArrowRight'].includes(event.key)){event.preventDefault();const index=Number(el.dataset.col),th=el.closest('th'),width=Math.max(96,th.getBoundingClientRect().width+(event.key==='ArrowRight'?16:-16));columnWidths[index]=width+'px';const col=$('content').querySelectorAll('col')[index];col.style.width=width+'px';announce('Column width '+Math.round(width)+' pixels.');}
    if(event.key==='Escape'&&s.ui.panel&&!$('decision').open){event.preventDefault();closePanel();}
    if(event.key==='Tab'&&s.ui.panel&&$('dock').getAttribute('aria-modal')==='true'&&!$('decision').open){const list=[...$('dock').querySelectorAll('button,input,select,textarea,a[href],[tabindex="0"]')].filter(x=>!x.disabled&&x.getClientRects().length);if(!list.length)return;const first=list[0],last=list.at(-1);if(event.shiftKey&&(document.activeElement===first||!list.includes(document.activeElement))){event.preventDefault();last.focus();}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus();}}
  });
  document.addEventListener('pointerdown',event=>{const el=event.target.closest('.column-resize');if(!el)return;const index=Number(el.dataset.col),th=el.closest('th'),start=event.clientX,width=th.getBoundingClientRect().width;el.setPointerCapture(event.pointerId);const move=e=>{const w=Math.max(96,width+e.clientX-start);columnWidths[index]=w+'px';$('content').querySelectorAll('col')[index].style.width=w+'px';};const up=()=>{el.removeEventListener('pointermove',move);el.removeEventListener('pointerup',up);};el.addEventListener('pointermove',move);el.addEventListener('pointerup',up);});
  window.addEventListener('resize',layoutPanel);
  window.addEventListener('beforeunload',event=>{if(s.dirty||s.pendingOperation||Object.keys(draftBuffers).length){event.preventDefault();event.returnValue='';}});
  window.addEventListener('beforeprint',()=>{if(!$('print-review').innerHTML){try{const {data}=exportData();$('print-review').innerHTML=reviewMarkup(data);}catch{$('print-review').innerHTML='<h1>PPO · Synthetic review r01</h1><p>No captured comparison selected. Capture a comparison in Changes before exporting an internal review.</p>';}}});
  if(window.innerWidth<700)s.ui.programmeMode='list';
  render();
})();
