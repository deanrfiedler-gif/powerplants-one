# PR #270 merge-readiness repair — 21 September 2026

EN-08 head `6dc7f43e` now incorporates the repaired EN-07 `30984721`, which includes main `8ed8b0c5`. A merge commit retains both histories, including the previously rebased EN-07 contribution.

The EN-07 r03 components, receiving-owner readiness fix, deterministic previews, shifted future fixtures and current reference/evidence files now have one source across the dependent branches. Earlier evidence remains in Git history. EN-08 retains its complete commissioning implementation, source adapters, migration/seed 0031, four additional capabilities, generated access-review catalogue, exact grants and upgrade tests. Existing migration/seed bytes and permission/receipt code match its original head.

The shared header retains semantic route-derived breadcrumbs and centred global controls. EN-06 retains its new grid; the Engineering review workspaces retain their separately scoped flex layouts and reachable phone inspector. Both Engineering review menus use main's current shared 240px menu and disclosure controls; their tests check those dimensions and EN-07 still compares its complete computed menu geometry against My Work.

No checks are disabled, no workflow gate is relaxed and no approval or migration-ledger entry is rewritten. Local TypeScript, lint, all 184 unit tests, foundation, prototype and naming checks pass on Node 24.21.0/npm 11.19.0. Published-head browser/database CI remains the merge gate and its results are reported on the PR. Owner/device/business acceptance remains separate.

Merge order: #266 → #270 → #269, using merge commits to preserve the reconciled dependency ancestry. This contribution does not merge to main or deploy.
