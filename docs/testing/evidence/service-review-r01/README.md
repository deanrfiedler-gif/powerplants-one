# Service Review r01 verification

Source baseline: `07ade644de06ff3b13e5af6ae9c299ca7c78c535`.

The first local model run passed **22 groups**, covering factual correction, original preservation, all-entry review, explicit recipient, exact issue bytes, customer-safe projection, non-acceptance ownership, replay, stale versions, permissions, missing evidence/contact, partial Finance limits, multiple attendances, changed source/template, tampered bytes and escaped customer text.

Native browser and screenshot results are pending the contribution's UI design baseline workflow. That suite writes exact source/hash/browser metadata and originals to the `Service-review-design-evidence` artifact. The PR is the authoritative run/commit publication record. No native result is claimed before its original evidence is inspected.

Local Chromium installation timed out; the cloud preview browser rejected the local HTTP URL. No native viewport, focus, print, screen-reader or physical-device result follows from the local model checks.

The [handover](../../../decisions/service-review-reports-workspace-design.md) defines the scope, source authority, guided scenario and implementation boundaries.
