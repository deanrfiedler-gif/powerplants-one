# Job Pack I6 application evidence

Owner: Dean Fiedler. Codex capture/inspection on 24 September 2026. Compiled I6 working source based on `899dd6e`; the committing change contains the corresponding UI and fixture. Later server-only projection tightening did not change these rendered UI bytes. Synthetic retained API fixture and a real local synthetic session; no persisted save is implied by these images.

## Captures

`structured-scope-{1440,1024,820,770,390,320}.png` shows the saved approved scope, tasks and limits. `exact-text-{1440,1024,820,770,390,320}.png` shows its opened saved-wording disclosure. Viewport heights are respectively 960, 768, 800, 900, 844 and 800 CSS px. These are viewport views; long phone sections continue in the normal module scroll.

Inspected the full-width scope element, the 1024 and 320 px viewport scope, and the 390 px exact-text view. Tasks and limits remain readable, long prose wraps, and the disclosure stays reachable by keyboard. The 320 px section naturally exceeds a viewport; it is not cropped by a fixed-height section. Native shell navigation and the r03 paper structure remain. Whole-page paired reference comparison, 200% zoom, physical devices and owner acceptance remain I5 work; no review fingerprint is adopted here.

## Actual proof

Compiled I6/I4/r03 browser suite: 22 passed, six deliberate duplicate mobile-project skips. The new cases cover verified fields and exact saved disclosure; unavailable and mismatched fallback; six widths and keyboard toggling. Six unit cases retain pre-I6 wording. Three focused database cases verify original snapshot digest, recipient old issue during a staff successor, history hashes and revoked permissions. A separate pre-I6-created revision was checked under I6 without changing snapshot or digest. See the [handover](../../../delivery/job-pack-i6-handover.md) for the local timeout and CI limits.
