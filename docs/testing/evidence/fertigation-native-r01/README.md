# Native fertigation candidate evidence

22 September 2026. Local synthetic proof, separate from CI, owner acceptance, manufacturer evidence and hosted deployment. Candidate source is on `feat/priva-fertigation-native`, based on main `0c95c5af776c97374997623bd1070d9de480cf82`. The source manifest records exact code and reference hashes; the PR records the commit identity.

## Environment and source controls

Windows 11 build 26100 host; Intel Core Ultra 5 225U, 14 logical CPUs, approximately 15 GiB memory reported by Node; Node 24.21.0, npm 11.19.0, PostgreSQL 16.15, existing Playwright 1.63.0 and approved Chrome 153.0.8010.53. A separate PostgreSQL cluster listens on loopback port 55442 and contains only disposable `ppo_synthetic_test`. Local credentials and document storage remain outside Git. The compiled application proof uses loopback port 3042. No hosted migration, reset, deployment, identity change or supplier/customer communication was performed.

Exact r02 HTML SHA-256: `b51bf2cab7cb1af54e33a0ec739921c4ca1a08da918165d615ead347d6463ab9`. The reference is retained unchanged. The absent starter-package source/builder/test bundle is not represented by generated replacements. Its reported reference-suite count is not a native test result.

## Executed local checks

| Layer | Command / evidence | Result and boundary |
|---|---|---|
| All native pure contracts | Native-file subset of `node --import tsx --test --test-concurrency=1 tests/unit/*.test.ts` | 98/98 passed: independent arithmetic, strict JSON/legacy/CSV mappings, reports, history, scenarios, candidate traces, bounded HTTP streams and conservative local orphan maintenance |
| Output and copy contract (subset above) | `node --import tsx --test tests/unit/fertigation-output.test.ts tests/unit/fertigation-history.test.ts` | 6/6 passed: customer allowlist, escaping/CSV formula defence, exact deterministic HTML, bounded graphs and semantic identity remapping |
| Core PostgreSQL | `node --env-file=.env.fertigation --import tsx --test --test-concurrency=1 tests/database/fertigation.test.ts` | 8/8 passed after final authority changes; real concurrency/replay, cross-source denial, immutable revisions, populated pre-0026 upgrade through 0042, reseed, copy/restore and terminal operation recovery |
| Evidence, receiving, reports and imports | Complete `fertigation-artifacts.test.ts` and `fertigation-import.test.ts`, sequential | 8/8 passed in 55.7 seconds: authenticated review, prepared PNG saved into a successor, exact retained HTML/PDF, receiving immutability/stale context, native JSON and strict schema1/2/CSV atomic imports |
| Report retention | Focused `FN-T45/T46/T47` in `fertigation-artifacts.test.ts` | Customer and 64-valve internal PDFs generated through the existing server renderer; exact private bytes/hash and original-operation replay passed. Final layout inspection recorded separately below |
| Compiled required journey | `playwright.fertigation.config.ts`, `tests/browser/fertigation.spec.ts` | Final 12/12 desktop/phone cases passed in 1.5 minutes, including exact saved Discovery source → source/evidence/master/area/irrigation valve → saved r2 → sign out → fresh authenticated context → exact saved revision/hash and valve values. Neutral nursery capture retains unknown numeric input |
| Capacity through real API/UI | Same compiled suite, `FN-T56` | Final desktop/phone cases passed with 100 areas, 1,000 valves, 100 groups and 300 events; bounded 25-row register. Individual API/engine/UI timings are retained rather than interpreting whole-test duration as response latency |
| Documentation assurance | `python scripts/check_foundation.py`, `check_prototype.py`, `check_naming.py` | Passed after register reconciliation: 78 parents, 17 issued source hashes and maintained instruction size under 8,000 characters. Documentation consistency is not business acceptance |

The final **twelve-case** compiled browser suite passed, including restore/copy/archive, lost-response recovery, scenario/candidate/receiving review and CSV mapping journeys. A further **two-case** capture run passed after changing only the candidate screenshot target; application code and assertions were unchanged. [browser-source-manifest.json](browser-source-manifest.json) records both test hashes, the exact tested application bytes, nine captures and compiled build `lvKYfTdLUzPlRDJHoINjr`. This was a pre-commit local build; CI records its checkout and build independently. The native bounded-drain fix also passed six focused unit/real-transport cases and the four-case compiled HTTP proof.

Four adjacent database regressions passed: E2 persistence and revoked historical receipts, plus ES-08 manual-edit/deletion and later-source adoption. Fifteen existing Discovery/manual-cost unit cases passed. No estimate writer changed.

[Capacity measurements](capacity-measurements.json) retain both desktop and phone final samples: proposal 1,077,332 bytes; read response 3,627,900 bytes; validation 30–31 ms; engine 32 ms; API create 1.78–1.80 s, save 1.67–1.70 s, read 363–390 ms; UI navigation 1.00–1.02 s, next page 58–61 ms and search/open 100–107 ms. No build, database suite or unit suite ran concurrently. These individual samples are not a production throughput or percentile guarantee. The separate [service measurements](service-measurements.json) record SQL calls including authority/transaction/receipt work: create 338, read 350 and save 429 for the large graph; the typical one-valve read also uses 350. That run measured large create 1.41 s, save 1.53 s, warm read 354 ms and a newly opened pool read 520 ms. Rejected oversize used zero SQL calls. These exclude HTTP identity resolution and do not claim a cold operating-system/PostgreSQL cache.

The final frozen serial unit suite returned **318/322**, with exactly the four reproduced Windows baseline failures described below. Full lint and the final changed-domain/UI targeted lint passed. The release build includes TypeScript checking. [Restart proof](restart-proof.json) records a real app process replacement and PostgreSQL fast restart on port 55442; a fresh authenticated HTTP session recovered the same scope/revision, master/valve links, operation receipt, original PNG bytes and retained HTML hash. The final compiled direct HTTP suite passed **4/4** in 6.65 seconds, including complete 422 JSON for oversized ordinary/import bodies. Required CI remains a separate PR checkpoint.

## Failed checks and corrections

The first browser run failed on test timing before the workspace toolbar rendered and on ambiguous label selectors. The harness now waits for the real native toolbar and uses exact accessible combobox names. Subsequent four-case proof passed. A real asynchronous preview defect found during review could leave Calculate disabled after a draft edit superseded an in-flight response; an independent request token now clears the busy state while rejecting the stale result. The final browser case exercises a delayed real API response and a newer edit.

Oversized HTTP uploads initially returned status 422 but could truncate their response body because rejection cancelled an unread request. The native reader now retains only the accepted 2/8 MiB cap, drains rejected bytes within a separate 16 MiB/10-second transport bound and returns a complete actionable response; shared domain limits are unchanged. The final compiled four-case HTTP run passed.

Two artifact tests initially failed during unchanged seed setup (`pg_timezone_names`, 10-second statement timeout) while full compilation ran concurrently. Sequential retries passed in 10.6 and 13.3 seconds, without increasing timeouts or weakening checks. Heavy suites now run serially. New import DB tests caught producer/consumer drift when native exports gained the truthful `synthetic: true` field; the strict importer contract and round-trip tests were corrected before rerun.

The broad unit run has four Windows/environment failures: two private document-store path cases, one recovery path case, and one slash-sensitive route-list case. All four reproduce in an untouched detached main checkout at `0c95c5a` using the same dependency/runtime environment. The retained baseline command is `node --import tsx --test tests/unit/document-store.test.ts tests/unit/recovery.test.ts tests/unit/warm-routes.test.ts`. They remain failures in the broad run; no claim of a wholly green Windows unit suite is made.

## Visual, print and accessibility evidence

Browser screenshots are actual compiled-app captures, not images of the standalone reference. The reference helper independently loads the exact r02 and shared r22 reference to compare component tokens and includes a negative control. Root inspected desktop saved valves; the UI implementer inspected desktop, phone 390 px and narrow 320 px views. The proof covers 1440, 1280, 1024, 768, 390 and 320 px widths, prominent valve/master actions and exact read-only reopening. A CSS 200% reflow check is explicitly an emulation, not a claim of native browser zoom or physical-device testing.

Keyboard editor/focus behaviour and error retention are tested where identified by the matrix. Screen-reader operation, every contrast combination and a complete WCAG conformance audit remain open. No blanket accessibility compliance is claimed.

PDFs are rendered with the existing isolated Chrome pipeline, all external requests blocked. Customer projection excludes free-text notes, original file metadata and calculation dependency reasons before HTML/PDF serialisation. PDF extraction checks the draft warning and absence of private canaries; internal output checks the last of 64 valve labels. Page-by-page visual QA checks repeated headers, units, wrapping, page numbering and clipping. Tagging is requested; PDF/UA conformance is not established.

The final **database-retained** customer and 64-valve internal outputs produced **one customer page and six internal pages**; all seven pages were inspected after exact-byte replay. The [report inspection record](report-inspection.json) pins the output source and PDF hashes. Metadata includes scenario/phase, an authenticated revision author and exact review evidence when recorded, while review notes remain excluded from customer output. Print spacing prevents an orphaned boundary bullet without reducing the 13 px print text. Representative captures: [customer report](customer-report.png) and [internal valve report](internal-report-valves.png).

Representative compiled captures: [desktop valve register](desktop-valves.png), [phone valve register](phone-valves.png), [clean-context exact reopening](phone-exact-reopen.png), [320 px neutral context](narrow-neutral-context.png), [archived history after original-operation recovery](phone-archived-history.png), [scenario comparison](scenario-comparison.png), [candidate constraints](candidate-constraints.png), [receiving quantity changes](receiving-effect-preview.png) and [CSV identity mapping](csv-import-preview.png). All nine were visually inspected; the final four were also inspected by the integrating reviewer.

## Review boundaries

See the [236-condition matrix](../../priva-fertigation-native-matrix.csv) for each implemented, partial or deferred condition. Fully representable schema1/2 graphs and the explicit 17-column valve CSV projection can be confirmed as new drafts. Legacy imports remain held when technical semantics/history/file bytes cannot be mapped safely. Original planted-hectare meaning and separate measurement provenance remain intact; unsupported analogue ranges retain their declaration without inferring compatible native signals. Advanced nutrient chemistry, bulk graph split/clone, independent physical scenarios and advanced process models remain deferred as instructed. PNG evidence is bounded validated content, not general file ingestion or malware scanning. The [read-only orphan reference probe](orphan-reference-probe.json) checked 378 current shared-schema reference columns against a 512 ceiling and protected a retained output; local cleanup tests use temporary files only. Committed preparations/history remain retained. Supplier Q01–Q26 remains unanswered; no manufacturer envelope is invented. Original technical ZIP bytes and executables are not published or executed.

Owner design/business acceptance, manual costing adoption, engineering/supplier approval, production readiness and deployment are independent decisions. The PR must retain draft status while its required verification or source reconciliation remains incomplete.
