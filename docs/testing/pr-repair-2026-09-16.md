# Open pull request repair — 16 September 2026

Scope: repair the eight open Powerplants One PRs #211–#218. No merge to main or deployment is included. Main `d565a9d` is retained throughout.

| PR | Repair | Verification and remaining boundary |
|---|---|---|
| #211 | Merge current main; retain both notification and Customer 360 records | Foundation, prototype and naming passed; new GitHub notification model/native workflow passed |
| #212 | Merge main; correct CommonJS built-in module loading; identify named cross-fragment bindings for lint | Full lint and the 23/24/24 model groups passed; HTML remains byte-identical; native review remains separate |
| #213 | Carry #212 repair through the stacked dependency; correct ES-07 built-in imports and model lint binding | Full lint, 33 model groups and documentation checks passed; HTML unchanged |
| #214 | Restore original Site Access and Knowledge packages; include original notification and quotation/conversion ancestry | Original restored HTML/report bytes match the uploaded aliases. Full lint, deterministic builds, model checks and foundation/prototype/naming passed. Native CI is separate; optional local DOM harness lacked jsdom |
| #215 | Resolve shared-file conflicts; remove seven unused controller bindings; regenerate the working draft and add focused CI | Full lint, 25 model groups, deterministic build and documentation checks passed. Fresh native CI output is authoritative; earlier visual evidence stays attached to its original hash |
| #216 | Reconcile main and record incomplete upload | Missing original Order Fulfilment source CSS/JavaScript, HTML, report and tests. Builder and foundation still fail; draft until recovered |
| #217 | Reconcile main and record incomplete upload | Missing original Sales Aftercare source CSS/JavaScript, HTML, report and tests. Historical result files alone cannot prove the absent package; draft until recovered |
| #218 | Reconcile main and record incomplete upload | Missing original Estimate-to-Actual source CSS/JavaScript, HTML, report and tests. Builder and foundation still fail; draft until recovered |

## Recovered package provenance

The original saved `PPO-Site-Access-and-Horticultural-Readiness-Review-Package-r01.zip` and `PPO-Knowledge-Repository-Changes-r01.patch` supplied the missing maintainable sources, build and check scripts, decisions and evidence. Only the package additions were restored; stale shared-file snapshots were not copied over current main. Their original register/index additions were integrated explicitly. The uploaded `sites` and `knowlege-base` aliases are retained; canonical build output lives in `site-access` and `knowledge`.

Notification #211 and the ES-05/06/07 chain #212/#213 already supplied the other missing evidence linked by the bulk HTML upload. This repair retains those commits as parents, so subsequent integration recognises the shared history. Merge those component PRs before the bulk upload PR; no duplicate business implementation is introduced.

All recovered fixtures are synthetic. No source acceptance, owner visual approval, operational access, ERP write or production readiness is inferred. All 78 parent requirement IDs and issued foundation sources remain unchanged.

## Incomplete uploads

For #216–#218, fetched branch history and available saved-file searches yielded no copy of the missing packages. Do not fabricate replacement source or verification to turn these checks green. Upload the original complete module packages, verify their recorded HTML hashes, then rerun build/model/native/repository assurance. Per-PR recovery inventories are committed on the respective branches.

## Runtime and CI evidence

Local full lint uses Node 24.21.0, npm 11.19.0 and the existing lockfile. Model/repository checks use the available Node/Python tools and no live services. Application source and database migrations are unchanged. Full remote application workflows must be read on each published head; this note does not claim they have finished. New focused workflows use the existing pinned runtime and browser policy without changing required-check settings.
