# ADR-0037 — Native Facility details and service memberships

**Date:** 21 September 2026. **State:** Adopted for this authorised synthetic implementation; owner visual acceptance pending.

The r02 CS-05 contract extends canonical Facility identities without replacing the protected identity registry. Use the existing Next/React/TypeScript/PostgreSQL stack, shared transaction/authority/receipts, native shell and controls. No dependency or provider is added. A separate growing-area store, custom-field engine and browser-authoritative persistence would duplicate identity or weaken the adopted field/authority contracts, so they are rejected.

Use additive typed Facility columns, immutable Facility-owned reported sources and automatic before/after audit. Unconstrained numeric columns with finite/range/scale checks reject excess precision before storage rounding. Existing parent graph guards and workspace lock order remain. Asset service memberships have explicit add/end operations and retained source/history, versioning the Asset once without changing Facility content. Protected Asset site movement remains disabled.

The [handover](../delivery/facilities-growing-areas-handover.md) records CS05-D01–D05, migration reservation, API/UI reuse, receiving boundaries and actual evidence. Legacy reads/create and hash canonicalisation are unchanged. No production or business acceptance is inferred.

## Combined-source assurance, 22 September 2026

Dean now authorises repair and merge of all current PRs after successful checks. CS-05 integrates ES-08 `0148514` and merged ES-02 main `460cf0b`; unchanged SQL remains ordered 0039, 0040, 0041, with seed 40 before seed 41. The hosted upgrade review retains each module's restrictions, including deferred identity-event flushing and specialist policy-write revocation. Registry assertions include all three migrations and both seeds. No additional application dependency, grant or service is introduced.

The retained CRM upgrade proofs must account explicitly for seed 41: one Organisation, two Sites, nine Facilities, one Asset, two SiteParties and one AssetLocationEvent. Every earlier identity remains exact; only the existing ORG/SITE/AST counters advance by 1/2/1. Authored test expectations enumerate all 16 new identity rows and their exact references. Counter queries now order by their complete key because ordering only by the shared workspace ID was ambiguous after inserts. The proofs still assert the whole identity/counter tables and repeat-seed stability.

The Contacts contract-source pin is refreshed only after reviewing the two Facility-only projection additions in `src/shared/reads.ts`. Person projection/visibility, directory rules and the issued Contacts HTML remain unchanged; the retained model/browser verifiers check their fidelity. The shared menu now has 21 destinations, including the explicit `/facilities` link. Native workspace matching and shared CSS retain Facilities, ES-02, ES-08 and PJ-09 together; all 39 CS-05 selector additions are preserved without changing rule bodies. The dedicated Facility browser proof runs once in the required browser lane, and the full compiled suite retains its cases.

The prior cancelled/development-readiness failures remain in GitHub history. Current combined-source CI inherits the four mandatory retained lanes and compiled module configuration established by ES-02. Exact fresh results belong in PR #274; a component pass does not establish owner, physical-device or business acceptance.

Original `94feea9` failure evidence is retained in [Application run 35615468635](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/35615468635), [CRM run 35615468617](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/35615468617) and [Contacts run 35615468620](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/35615468620). The local combined-source upgrade cases now both pass; the version-9 proof also explicitly retains the six earlier unnumbered CRM/Finance/Email seed identities. The menu proof distinguishes 20 module entries, Help (the 21st counted destination) and separate installation actions, and checks the exact Facilities & areas link.
