# Service Cases & Triage workspace

<!-- versioning: git; committed history is authoritative -->

**Owner:** Dean Fiedler · **Date:** 15 September 2026 · **State:** Requested standalone design delivered for review; native visual/device acceptance and application integration pending.

**Native refinement, 23 September 2026:** a proposed refinement of r02 for the shared application shell, covering the SV-01 register and SV-02 record, is recorded in [Service requests native refinement](service-requests-native-refinement.md). It changes nothing in r02 or below; D1, D2, D5 and D6 are applied under Dean's delegation, and D3 and D4 are open.

The [interactive HTML r02](../reference/ui/service-cases/PPO-Service-Cases-and-Triage-Workspace-r02.html) is the internal support front door requested by Dean: a Powerplants employee records a customer's issue; the Service Manager reviews its impact, selects a response and assigns an owned action; a technician or specialist records evidence; the manager reviews resolution and customer contact before closure. It uses the r20 theme with only the module's own header, context and local views. There is no left navigation rail, corporate masthead, customer login or external messaging action.

## r02 Board / List refinement

Dean requested the board as the next refinement before the Work Orders HTML. This revision extends the unchanged [r01 source](../reference/ui/service-cases/PPO-Service-Cases-and-Triage-Workspace-r01.html) on draft [PR #197](https://github.com/deanrfiedler-gif/powerplants-one/pull/197), whose source head was `d99dfe3f26e42a416221c6db8f781e3470041c3d`; repository main was `bc1dcf21490dda37979227f5fc13224183853d5a`. SC-04 intake, BP-07 state/ownership boundaries and the shared UI specification were reviewed. The r01 file retains SHA-256 `c05c0ea51e6bcceca8a35edbf606fdebdd452780f7c9c41432a51a2cb669c6a7`.

The attached `ppo-deal-pipeline_r35.html` has SHA-256 `9d7c0fc858a7a6114200fcc17f6c2ba09e0b2da8eaffad4e480e61ea8d696431`. Its final card cascade, including the r28 owner stack and r30 font-relative geometry, is the card source. Cases retains the 7px border radius, white surface, quiet border, hover shadow, two-line title, customer/site metadata slots, 16.5rem standard height, 11px padding, overlapping 23px avatars and compact footer controls. Source-specific card colours are scoped to the board; the r20 theme, embedded Roboto, shared choice menus and module-only layout remain around it. Case reference/priority occupy the value row; site occupies the contact row. The extra action-owner text and full snapshot make service accountability explicit.

Both presentations derive their permitted records from one collection and the same search, customer, site, case-owner, priority, category, attention queue and case-scope filters. Switching view preserves selection. Filter changes clear a selected record that becomes hidden. Queue counts reflect the current scope and non-queue criteria. Open cases are the default; Include closed adds the seventh lane and Closed only shows that lane alone. No separate board records or copied work identities exist. The original six fictional cases are retained, so Triaged and Resolved begin empty and become populated through saved decisions.

Cards show the case reference, issue, customer/site, priority, accountable case owner, next-action owner, earliest open action and due time, plus waiting/update/review signals. The displayed next step falls back to the manager decision when there is no open action; it does not invent a technical assignment. A snapshot exposes full text, all attention signals and waiting ownership/review details. Urgent cases sort first, followed by overdue commitments (oldest first), then remaining priority, received time and reference. This order is shared with List. The resolution proposal is a separate **Awaiting resolution review** badge in its current lane.

| Requested move | Required workflow and result |
|---|---|
| New / Triaged / Active → Needs information | Existing clarification form requires a question, owner, due time and reason. |
| New / Needs information / Active → Triaged | Existing triage form reviews impact, priority rationale, route, case owner and a new owned action. |
| Triaged → Active | New local `start` command selects an existing open action and requires a reason. It changes only case coordination state; the action remains open and no booking or work completion is fabricated. |
| Triaged / Active → Waiting | Existing waiting form requires dependency, detail, follow-up owner and review time. The customer-update commitment continues. Missing intake information uses clarification first; Waiting cannot bypass triage. |
| Waiting → Active | Existing reasoned resume preserves the previous waiting decision in history. |
| Triaged / Active → Resolved | Outstanding actions, waiting or work block the move. Without a proposal, the evidence-backed review form creates a proposal and leaves the case in its current lane. With a proposal, manager acceptance displays inspectable evidence and records Resolved. |
| Resolved → Closed | Available in Move case; existing closure guard requires successful customer contact after resolution review. |
| Resolved / Closed → Active | Existing reopening form requires an owned next action, due time, reason and customer-update commitment; earlier closure/evidence remain. |
| Back to New, or another unsupported move | Disabled with an explanation. No direct status assignment or stage skipping. |

Dragging and the **Move case** button use the same transition lookup and required forms. No state changes during dragging, and cancellation retains the original record. The initial drag/move basis and saved case version are checked; cross-tab changes refuse the save and preserve entered proposals. Only the manager preview can move cases; the model separately enforces this rule. Case-card Enter opens the workspace and Space selects the card. List also exposes Move case for keyboard and touch use.

At desktop widths the six lanes retain a 240px minimum width within one horizontal/vertical scroll surface, with sticky headings and no per-lane scrollers. Below 1024px the module presents readable labelled List records, including 390/320px CSS treatment; returning to desktop restores the preference. `ppo-service-cases-register-view` stores only Board/List preference. Case data uses the separate r02 key `ppo-service-cases-triage-r02`; a validated r01 backup can be explicitly restored through Workspace information. Opening r02 does not overwrite r01 storage or issued HTML. Native desktop geometry, zoom and physical-device behaviour still require review.

## Sources and explicit design decisions

The Service review used repository main `76a732a9520d28fd59d0fd990c13b6e7cdf415d1`, particularly [SC-04 and the Service blueprint](../blueprints/BP-07-service-operations.md), [intake implementation](../../src/service/intake.ts), [intake screens](../../src/components/intake-screens.tsx), [data dictionary](../contracts/service-data-dictionary.md) and [naming standard](../standards/naming-conventions.md). Existing P03 intake and later work-order/planner contracts remain authoritative for the running application.

The read-only Willowbank organisation, two sites, nine facilities and single pump context are copied from the [Customers, Sites & Growing Areas r03 source at b6066d1](https://github.com/deanrfiedler-gif/powerplants-one/blob/b6066d18602b7b544a1883b8beb3a12ed0ecf09c/docs/reference/ui/customers/PPO-Customers-Sites-and-Growing-Areas-Workspace-r03.html). This file runs independently of that design. Fernbank Flower Farm, the six case examples, staff roles, work and visit examples are new fictional design data. They are not migration seeds or operational records.

The attached r20 board has SHA-256 `c68a499e857b1705d47bd6598d4b2e89526bf0861a0d234394c3da023f19b617`. Its 41 Intake tokens resolve to 47 shared/workspace values. The three original embedded Roboto font faces retain combined SHA-256 `a3c83afa45da8ccb05686729f4cfdd2f4694e6771334b025f00207ef381185e9`. The navy/green palette, control shapes, shared rounded choice menus, dialog treatment and responsive spacing follow that source. The existing plain HTML/CSS/JavaScript design approach is reused; no new technology, framework or dependency is selected.

| User decision | r01 treatment |
|---|---|
| Case is the consistent interface term | Existing `Ticket` concept and `SYN-PPO-TKT-…` reference pattern remain. No database entity, reference allocator or API is renamed. |
| Staff receive and log the customer issue | Caller, channel and received time are distinct from the internal logger. Initial site/equipment/impact uncertainty can be retained and corrected before triage, with original capture history. |
| Service Manager reviews and assigns | Manager preview owns triage, response route, priority rationale, case ownership, escalation instruction, waiting and closure decisions. Technician actions and proposed visit assignments are separate from case ownership. |
| Every issue stays owned | An accountable manager/delegate and customer-update commitment are required while the case remains open. Open actions have responsible people and due times. Waiting also has its own owner and next review. |
| Customer support includes non-technical issues | Engineering, Sales and Finance routes select the matching fictional receiving person. Supplier advice remains internally owned. The Finance example contains no restricted account balances or invoice contents. |
| Reviewed resolution and reopening | Evidence-backed proposals, manager return with an owned correction, accepted resolution, customer contact, closure and reopening are distinct commands. Prior outcomes and evidence are retained. |

## Six connected views

1. **Case register:** six queue tiles for the current case scope, new intake, urgent open cases, waiting/information, overdue commitments and resolution review; Board and List share these criteria. Search and customer/site/case-owner/priority/category filters operate together. Filtered-out selection is cleared, and unavailable record actions are disabled. Mobile CSS presents register rows as labelled cards.
2. **Case workspace:** original symptoms, reported crop/production impact, temporary arrangements, customer/site/facility/equipment context, Google Maps address link, next actions, related-case candidates and history. Quick capture and captured-detail correction open as focused forms.
3. **Triage & actions:** reviewed priority, response route, case owner, escalation instruction, action assignment, outcomes and explicit transfer references/receiving owners. Waiting reasons cover customer information, parts, supplier advice, quotation approval and site access.
4. **Work & visits:** prepare a local Draft work order, link an existing work order for the same customer/site without duplicating its identity, propose a technician visit and inspect an exact preparation brief. These actions do not authorise work, reserve time or confirm attendance. Source work records retain their own scope and visits.
5. **Evidence & updates:** Reported symptom, Suspected cause, Attempted fix, Verified finding and Work completed remain typed and attributable. Original image attachments have exact context and source notes. Corrections append an observation retaining the original. Contact records distinguish Contacted, Message left and No response; an internal discussion cannot satisfy customer-contact closure requirements.
6. **Resolution & review:** open actions, waiting and unfinished/proposed work block resolution. A proposal identifies supporting current evidence. Manager return creates an owned action and preserves the proposal in history. Accepted resolution is retained separately from closure; closing requires successful non-internal customer contact after the resolution review. Reopening retains the case reference and earlier closed outcomes.

The main demonstration starts with an urgent intermittent fault involving the one Willowbank pump installed in Irrigation Shed 01 and serving Greenhouse 01, Tunnel 01 and Propagation House 01. An earlier closed case preserves an attempted restart and an unverified underlying cause. Other examples demonstrate unknown sensor identity, a completed diagnostic visit with outstanding parts, an invoice referral and remote support awaiting resolution review. A visit's completion never changes the case to Closed.

## Local behaviour and boundaries

Manager, Employee, Assigned technician and Observer are local preview roles. The technician preview is restricted to cases with its recorded involvement; it cannot export the full workspace, triage, assign case ownership or close cases. The observer can inspect but cannot mutate. These controls demonstrate the intended workflow; they are not authentication or server security. Real employee permissions, manager delegation and all related-record/file access must be enforced through existing domain services during implementation.

Expanded case states use the dictionary's existing New, NeedsInformation, Triaged, Active, Waiting, Resolved and Closed values. **Awaiting review** is the display of a separate resolution proposal, not a new canonical Ticket state. The current application still has its bounded P03 New/NeedsInformation/Triaged command contract. Organisation-level remote/departmental triage without a site is demonstrated here; it is a proposed extension to the current known-site runtime gate. On-site routing and work preparation in this design require a known site. This contribution does not silently lift a running-app guard.

Work-order links resolve to one retained source record. Draft scope and Proposed visits are intentionally held for receiving work-order/planner decisions. Full work approval, calendar capacity, confirmed assignment, dispatch, technician job packs and reports continue in their existing modules. This HTML does not simulate their completion by changing a case status. An outstanding action can be explicitly transferred with a receiving reference and responsible person; it cannot transfer back to the same case. Receiving-record existence/acceptance must be checked by the future integration.

Contextual assistance is scripted from the selected case, current evidence and actions. It offers a summary, missing-information prompts, technician brief and customer-update draft, with inspectable exact sources. Stale source data blocks use of a draft. It performs no model inference, technical diagnosis, email delivery, supplier contact or external handover. MYOB case mappings remain unconfigured; a future linked reference must retain company context and source evidence without replacing the existing PPO identity.

This is a single embedded file with no network-loaded fonts or assets. Google Maps opens only an explicitly selected fictional address through a standard address search link. The local key is `ppo-service-cases-triage-r02`. Validated JSON backup/restore, explicit reset, stale-write refusal and session-only storage failure messaging are included. The queue clock is visibly fixed at 15 September 2026, 10:00 AEST for repeatable examples; entered times use Australia/Melbourne and history retains recorded timestamps. No SLA response target, warranty decision or live urgency/dispatch authority is implied by a priority label.

Original JPG/PNG/WebP uploads are limited to 1 MiB each, twelve original files per workspace, dimensions up to 10,000 pixels and 6,000,000 JSON characters overall. File bytes are stored once and referenced by observations/history. Browser quotas may be smaller. Original metadata is not stripped. Upload decoding and file signatures are checked; restored-file structural checks are not cryptographic authenticity or controlled document storage. SharePoint remains the intended business-document authority and MYOB the ERP authority.

## Verification and next step

The preserved [r01 check](../../scripts/check-service-cases-design.mjs) and [r01 evidence](../testing/evidence/service-cases-workspace-r01.json) record 44 model/DOM checks. The current [r02 check](../../scripts/check-service-cases-r02-design.mjs) and [hash-bound r02 evidence](../testing/evidence/service-cases-workspace-r02.json) record **71 model/DOM checks**, including the original journeys and Board/List equivalence, all queues/scopes, role boundaries, source-derived card styling, movement cancellation/validation, guarded start, resolution proposals/acceptance, stale drags, ordering and preference/mobile presentation. They cover role separation, compound filters and hidden selection, exact pump context, owned triage/waiting, original photo preservation and correction, proposal/return/review/contact/closure/reopening, unknown-site routing refusal, same-work identity, capture correction, stale-source/write conflicts, backups and storage failures.

Foundation, prototype and naming documentation checks passed for r02 on integration base `bc1dcf21490dda37979227f5fc13224183853d5a`: 78 parent requirements retained, 2,119 local links resolved and 196 document records checked. Copy-ready project instructions remain at 7,994 characters. The Service contracts and intake source were unchanged between the design source and this integration base. The check script passed Node syntax validation; a full application build, lint or backend run is not claimed.

The check uses optional scratch jsdom 27.0.1 through `PPO_DESIGN_JSDOM_MODULE` pointing to its absolute `lib/api.js` entry. No application dependency is added. Run `node scripts/check-service-cases-r02-design.mjs --write-evidence` with that variable set. Native dialogs, scroll and image decoding are stubbed in the check. The earlier Browser preview was rejected under its local-URL security policy; no alternate renderer or hosting workaround was attempted. Native menu geometry, focus/top-layer interaction, 320/390px physical-device behaviour, camera capture, clipboard/downloads, zoom and print remain unverified. Code-level theme checks do not establish visual acceptance.

SC-04 and SVC-01/SVC-02/SVC-06 provide the primary traceability; CRM-01, DAT-01–DAT-03 and DOC-01/DOC-02 cover related identity and evidence. The existing 78 parent requirements and acceptance procedure statuses remain intact. This is a design PR, with no application routes, migration, operational communication, deployment or merge. The next design module is **Work Orders**, after native review of Cases r02. Runtime case/work/planner integration and independent acceptance remain separate; Service Agreements & Maintenance follows the reactive-support foundation.
