# Native Estimating and Quotation programme authority

<!-- versioning: git; committed history is authoritative -->

Owner: Dean Fiedler. Decision date: 24 September 2026. Status: implementation authorised; business policy and acceptance remain separate.

Dean supplied an execution prompt for ES-01–ES-10 and the Excel estimate import workflow. It authorises native implementation, refinement, synthetic tests, maintained guides/design records and dependency-ordered reviewable pull requests. It explicitly preserves the existing ES-02 wizard, ES-08 workbench, E1 Draft quotations, immutable source identities and current permissions. Historical implementation gates do not require reconfirmation of this repository work.

The prompt also supplies authority to implement the accepted ES-08 board after current-main WP-G00 reconciliation. No migration number is reserved, and any concurrent geometry implementation must be reconciled first. No such concurrent branch/worktree or open PR was found in the initial refresh.

Missing commercial policy is represented as Not configured; it does not authorise invented approval thresholds, self-approval, routing rules, FX, landed costs or financial definitions. Native surrounding workflows may proceed without enabling those actions. MYOB, SharePoint and native CAD retain their documented responsibilities. No live transactions, customer/supplier messages, Azure deployment or production cutover are authorised.

Use the [programme handover](../delivery/estimating-programme-handover.md) for the refreshed repository, actual implementation increments, tests and outstanding dependencies. Parent requirements EST-01–EST-09 and all 78 parent IDs are unchanged. Code delivery, component evidence, owner acceptance and production readiness remain separate claims.
