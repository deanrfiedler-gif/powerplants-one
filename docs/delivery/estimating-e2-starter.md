---
document_id: PPO-010-E2-START
revision: r03
date: 2026-09-14
owner: Dean Fiedler - private prototype
status: Adopted options and questions reconciled; runtime pending
---

# BP-04 E2 — Routing, options and scoped discovery starter

The initial r01 prepared the next increment. The later [audit policy package](../decisions/audit-follow-through-policy-package.md) adopts E2-D02/D03 and DR-01/02; Dean’s continuing instruction authorises their repository implementation. The r02 [receiving contract](../contracts/estimating-e2-design.md) now reconciles the older route prerequisites. Numeric routing rules and the five estimating-container propositions remain unresolved; no repeat adoption of the recorded choices is required. [E1's completed publication](https://github.com/deanrfiedler-gif/powerplants-one/pull/49#issuecomment-5562344440) satisfies its historical verification dependency; it is not a fresh check of current code. E2 remains local to the [BP-04 sequence](estimating-implementation-plan.md), under PPO-010, with no change to the 78 master parent IDs or P01–P12 service order.

## Authorised bounded runtime objective

Implement only a concretely adopted synthetic E2 route/option/scoping journey: a permitted estimator records separate Full/Express effort evidence or an owned unknown, alternatives and one explicit selected basis, scopes supported facilities/systems, and answers a bounded versioned questionnaire. Save immutable scope/answer snapshots that can feed a later manual estimate without changing any E1 estimate version or draft quotation. Do not build specialist formulas, costing approvals, formal issue, customer responses or live integrations in E2.

Before code, use the adopted G03/G04 synthetic subset and reconciled receiving contract. G02 delivery classification remains Not configured until a concrete rule set is adopted; that missing policy does not block drafting an option. DR-01/02 category and equipment changes have separate compatibility obligations in E1/E2. Existing screenshots and manuals are evidence of observed CREMS behaviour, not permission to invent operational thresholds, route precedence or lock rules. If accepted source material remains unavailable, label each candidate rule fictional and obtain adoption of that concrete synthetic policy within the invocation. Useful design and fixtures can proceed while a business choice is unresolved; never mark that choice resolved by implementation.

| Required decision | Smallest useful evidence or synthetic design result |
|---|---|
| G02 — future advisory routing | Not configured in the first persisted option/questionnaire slice. Do not execute historical R01–R13 or invent DR-03–DR-06. Later adopted rules require exact observations, boundaries and override authority before implementation. Separate effort, scope readiness and receiving confirmation. |
| G03 — alternatives and locks | Option/estimation/Estimate/Quote state-and-lock table; exactly one declared selected basis; predecessor/inheritance rules; conflicting selections, branch/reopen and terminal cases. A selection is local estimating context, not a new CRM probability or forecast amount. |
| G04 — questionnaire and scope | One bounded published definition with stable question/choice IDs, required/conditional logic, facility/system membership, supported answer types/ranges and accepted definition-change/re-snapshot examples. Incompatible answers remain visible as unresolved history. |

## Fresh dependency and work checks

Read AGENTS, README, STATUS, CONTRIBUTING, PPO-STD-001, current project instructions, full BP-04/evidence/screens/acceptance, E1's physical/API contract and final publication record, existing Opportunity/Activity/permissions/receipts and current Finance/CRM work. Verify the repository’s actual visibility (public at this audit; application data remain private), main SHA/tree, E1 merged status and actual-main checks, other active issues/PRs and migration/ADR reservations. E1 used 0012 / ADR-0017; never assume the next number. Use one bounded issue, branch and review PR, preserving unrelated work and issued source bytes.

Require actual E1 final-head and merged-main runtime/visual/restart evidence. Recheck current controls, requested reviews and unresolved threads before any normal expected-head merge authorised by the future invocation. Never copy a historical pass forward to changed code or treat unavailable detailed rules as an invitation to bypass them.

## Implementation contracts to retain

1. Reuse canonical Organisation/Opportunity/company/site/person/Activity IDs. Current scope and all related-target visibility govern commands, options/selectors, counts, snapshots and original receipt replay. Ownership does not confer access; no CRM stage, money, next-action or probability change is inferred.
2. Preserve E1's embedded option A and estimation revision r01 UUIDs when extracting formal records. Distinguish option, estimation revision, Estimate identity/version, scope/answer snapshot and Quote identity/revision. Prove old UUIDs, content hashes, reasons, receipts and exact draft HTML/PDF remain unchanged through upgrade, reseed and process restart.
3. Adopt explicit expected-version commands for effort/selection/branch/snapshot operations, with original operation identity, canonical payload hash, audit/outbox and one-effect replay. Concurrent selection cannot create two selected bases. Unknown outcomes reconcile the original before another intent.
4. Store versioned scope/question definition and input evidence. Effort is separately declared; no delivery-route outcome exists while rules are Not configured. Unknown information never silently resolves to Express or a zero quantity. Definition changes create a deliberate comparison/new snapshot; they do not mutate accepted answers or reprice old estimates.
5. Facilities, direct same-site equipment and systems use the reconciled subset of current shared scope contracts. No competing asset/customer/product master or native CAD authoring is introduced. Questionnaire visibility follows explicit scoped membership and supported conditional predicates.
6. Continue the accepted brand, compact phone context, keyboard access, meaningful loading/empty/dirty/saved/conflict/unknown/denied states and natural 320px reflow. Display selected versus proposed basis clearly. No offline-save claim is made without a separately authorised offline contract.

## Verification and stop boundary

Add meaningful database, HTTP and browser tests for effort/unknown separation and every adopted option/question boundary, cross-company and related-record denial, option selection races, exact inheritance, locked edits, definition compatibility and original-operation recovery. Prove the accepted E1 originals survive fresh/upgrade/reseed and distinct application/PostgreSQL restarts. Keep current P01–P09, CRM, Finance and E1 checks enabled under their pinned versions. Inspect desktop/390px/320px output; retain source/checkout/tree/run/attempt and content hashes.

Record only exercised components of relevant EA cases; broader EA/master AT and operational policy remain open. Deliver the scoped implementation, actual verification, limitations and publication record authorised by the ongoing invocation. Prepare only the next justified starter, then stop. Do not begin E3, operational source access, ERP writes, migration, hosting or customer communication.
