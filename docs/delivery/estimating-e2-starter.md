---
document_id: PPO-010-E2-START
revision: r01
date: 2026-09-06
owner: Dean Fiedler - private prototype
status: Prepared only; E1 verification and a later explicit invocation required
---

# BP-04 E2 — Routing, options and scoped discovery starter

This file prepares the next increment. Its creation does not start E2 or resolve a source/policy question. Verify [E1's actual publication](estimating-e1-handover.md) before invoking it. E2 remains local to the [BP-04 sequence](estimating-implementation-plan.md), under PPO-010, with no change to the 78 master parent IDs or P01–P12 service order.

## Objective on a later invocation

Implement only a concretely adopted synthetic E2 route/option/scoping journey: a permitted estimator determines a versioned Full/Express route, records alternatives and one explicit selected basis, scopes supported facilities/systems, and answers a bounded versioned questionnaire. Save immutable scope/answer snapshots that can feed a later manual estimate without changing any E1 estimate version or draft quotation. Do not build specialist formulas, costing approvals, formal issue, customer responses or live integrations in E2.

Before code, turn G02/G03/G04 into one reviewable policy package. Existing screenshots and manuals are evidence of observed CREMS behaviour, not permission to invent operational thresholds, route precedence or lock rules. If accepted source material remains unavailable, label each candidate rule fictional and obtain adoption of that concrete synthetic policy within the invocation. Useful design and fixtures can proceed while a business choice is unresolved; never mark that choice resolved by implementation.

| Required decision | Smallest useful evidence or synthetic design result |
|---|---|
| G02 — route precedence and unknowns | Ordered predicates, required inputs, first-match rule, explicit unknown/incomplete result, supported override reason/authority, version and complete boundary/overlap fixtures. Separate routing from E3 pricing/approval policy. |
| G03 — alternatives and locks | Option/estimation/Estimate/Quote state-and-lock table; exactly one declared selected basis; predecessor/inheritance rules; conflicting selections, branch/reopen and terminal cases. A selection is local estimating context, not a new CRM probability or forecast amount. |
| G04 — questionnaire and scope | One bounded published definition with stable question/choice IDs, required/conditional logic, facility/system membership, supported answer types/ranges and accepted definition-change/re-snapshot examples. Incompatible answers remain visible as unresolved history. |

## Fresh dependency and work checks

Read AGENTS, README, STATUS, CONTRIBUTING, PPO-STD-001, current project instructions, full BP-04/evidence/screens/acceptance, E1's physical/API contract and final publication record, existing Opportunity/Activity/permissions/receipts and current Finance/CRM work. Verify current private main SHA/tree, E1 merged status and actual-main checks, other active issues/PRs and migration/ADR reservations. E1 used 0012 / ADR-0017; never assume the next number. Use one bounded issue, branch and review PR, preserving unrelated work and issued source bytes.

Require actual E1 final-head and merged-main runtime/visual/restart evidence. Recheck current controls, requested reviews and unresolved threads before any normal expected-head merge authorised by the future invocation. Never copy a historical pass forward to changed code or treat unavailable detailed rules as an invitation to bypass them.

## Implementation contracts to retain

1. Reuse canonical Organisation/Opportunity/company/site/person/Activity IDs. Current scope and all related-target visibility govern commands, options/selectors, counts, snapshots and original receipt replay. Ownership does not confer access; no CRM stage, money, next-action or probability change is inferred.
2. Preserve E1's embedded option A and estimation revision r01 UUIDs when extracting formal records. Distinguish option, estimation revision, Estimate identity/version, scope/answer snapshot and Quote identity/revision. Prove old UUIDs, content hashes, reasons, receipts and exact draft HTML/PDF remain unchanged through upgrade, reseed and process restart.
3. Adopt explicit expected-version commands for route/selection/branch/snapshot operations, with original operation identity, canonical payload hash, audit/outbox and one-effect replay. Concurrent selection cannot create two selected bases. Unknown outcomes reconcile the original before another intent.
4. Store versioned definition and input evidence with route outcomes. Unknown information never silently resolves to Express or a zero quantity. Definition changes create a deliberate comparison/new snapshot; they do not mutate accepted answers or reprice old estimates.
5. Facilities and systems must use a reviewed subset of current shared scope contracts. No competing asset/customer/product master or native CAD authoring is introduced. Questionnaire visibility follows explicit scoped membership and supported conditional predicates.
6. Continue the accepted brand, compact phone context, keyboard access, meaningful loading/empty/dirty/saved/conflict/unknown/denied states and natural 320px reflow. Display selected versus proposed basis clearly. No offline-save claim is made without a separately authorised offline contract.

## Verification and stop boundary

Add meaningful database, HTTP and browser tests for every adopted route boundary/overlap/unknown case, cross-company and related-record denial, option selection races, exact inheritance, locked edits, definition compatibility and original-operation recovery. Prove the accepted E1 originals survive fresh/upgrade/reseed and distinct application/PostgreSQL restarts. Keep current P01–P09, CRM, Finance and E1 checks enabled under their pinned versions. Inspect desktop/390px/320px output; retain source/checkout/tree/run/attempt and content hashes.

Record only exercised components of relevant EA cases; broader EA/master AT and operational policy remain open. Deliver the scoped implementation, actual verification, limitations and publication record authorised by that later invocation. Prepare only the next justified starter, then stop. Do not begin E3, operational source access, ERP writes, migration, hosting or customer communication.
