# P11 — Integrated quality, access and usability handover

**Revision:** r01 · **Date:** 7 September 2026 · **State:** implementation checkpoint; P11 incomplete. **Owner:** Dean Fiedler, personal private synthetic prototype.

[Issue #54](https://github.com/deanrfiedler-gif/powerplants-one/issues/54) · [Authoritative external publication](https://github.com/deanrfiedler-gif/powerplants-one/issues/54#issuecomment-5567364667) · [P11 starter](p11-starter-prompt.md) · [Ordered plan](prototype-implementation-plan.md).

## Authority and verified starting point

Dean invoked the maintained P11 starter. Scope covers only P11 implementation, dependencies, additive/non-destructive fixtures when needed, verification, documentation, focused issue/branch/PR and normal expected-head merge after applicable checks/review, followed by P12 starter preparation. No hosting, live integration, production migration, communication, paid change or access/rule change is included.

Connected account `deanrfiedler-gif` (231005545), private repository, admin/maintain/push/triage/pull reported true. Starting main is `744b6e6b89e69abdd0fbd4bfdd6ef5ae51e154f3`, tree `48ddac2931df66828aef42be5c78b772d9267167`. Local checkout reconstructs the exact signed commit and matches that tree; no newer main or unrelated branch is reset. Branch: `feature/p11-integrated-quality`.

P10 [publication](https://github.com/deanrfiedler-gif/powerplants-one/issues/45#issuecomment-5562750221) is explicitly COMPLETE. Issue #45 is closed completed; PR #48 is merged. Exact P10 contribution `216102e39969aa7480bcc11898b7fe03a6d6a671` and actual merge `655f17cfc7e68036939eeecc85b75069ccde8a5c` share tree `d151081ba6bc99d0cfa387332bed74aa0f502aaa`. All three contribution runs 34077653041/34077653129/34077653140 and all three merge runs 34088289930/34088289905/34088289950 independently read completed/success, attempt 1. Current main's Application/Documentation/CRM design/E1 runs 34088912272/34088912277/34088912289/34088912253 also read completed/success, attempt 1.

Accepted CRM I1/I2, E1, unchanged issued references and design-only portal direction are preserved. During this work Projects design PR #52 merged as `ebbcac5c9e210e7742bb619da3c8518592d19f31`, tree `3deb75000c13c8eea013ff3b654745768a2015ca`. The checkpoint incorporates that exact tree and its accepted documents, including PPO-012's unchanged P01–P12 boundaries; Projects J1 remains separately prepared. Its Documentation/E1 runs 34097388155/34097388159 passed; Application 34097388077 was still running at the read. No earlier focused P11 issue/branch/PR was found. Newer decisions and main must be checked again before publication.

## Implemented checkpoint

- Shared business and diagnostic tabs unmount their displayed records and unsaved forms when another tab starts an identity switch or sign-out. A same-origin channel carries only `Lock`, never identity, role, data or credentials. Reload remounts the ordinary server-backed view. The signal can only remove context; all existing server authority remains required. It is not a new grant or offline persistence mechanism.
- Shared error summaries link matched server field errors to actual controls and move keyboard focus. Shared customer creation supplies its explicit display-name/company/owner mappings and linked validation state. Unmatched errors stay textual; no nonexistent field link is fabricated.
- Required company/owner selectors show their actual loading/error state; record capture waits for pending required selector reads. Successful recovery reviews retain a visible server-save confirmation through reload. A denied recovery command clears original details and form contents even when no browser identity signal was received.
- `Observed` distinguishes Complete, Partial, BoundedWindow and unknown completeness. A bounded report window is no longer labelled a complete filtered result.
- SC-15 `/admin` connects existing document, Finance and personal offline workflows, and presents only the existing current-owner P08 recovery list. Counts describe its permitted returned window, whose existing source examines at most 100 recent owned cases. There is no whole-backlog denominator or invented business metric.
- `/admin/recovery/:id` reads the original existing owner-scoped case, source envelope and review history; records only RetainedForReview or ClarificationRequired through the existing disposition command. Current permission governs the exact-byte image route and retry. An uncertain save retains the exact command for unchanged retry. It cannot approve evidence, change an original, restore normal access, allocate quantities or execute a financial effect. Raw original detail is confined to the authorised service-owner review disclosure.

The earlier UI checkpoint introduced no schema or grant changes. The approved Travel continuation below adds migration 0013 and seed 13; no new dependency, source/template edit, policy publication command or server aggregation endpoint is introduced. Earlier immutable SQL/seed/issued bytes and existing operation hashes remain untouched. Normal business and restricted recovery remain separate.

## Verification and limitations

Current pins remain Node 24.20.0 / npm 11.19.0 / PostgreSQL 16.15 / Next.js 16.3.4 / React 19.2.8 / TypeScript 6.0.3 / Playwright 1.63.0. Exact Node/npm and locked dependencies were verified locally. Ordinary shell cloning lacked credentials; connected GitHub reads and an existing checkout supplied a hash-verified current tree. No credentials were copied into Git. Local PostgreSQL/HTTP/browser proof is not assumed from surviving files; disposable CI is required for those gates.

The first local implementation checkpoint passed lint, typecheck, 20 unit cases and build; all three Python checks passed. These results are intermediate-source checks, not the final contribution or P11 acceptance. Eight authored desktop/phone browser cases in `tests/browser/quality.spec.ts` cover cross-tab business/diagnostic clearing, real current-authority reload, mapped validation focus/input retention, real recovery persistence/unchanged uncertain retry and loading/failure/empty/denied states with 320px recovery reflow. They require actual execution. Injected validation/query errors are UI component proof, not server-policy proof.

Pre-publication self-review replaced separate sender/listener channel objects with one shared instance per tab, preventing a tab from locking itself during its own identity switch. An initial lint rejection of synchronous effect state was resolved with the error-summary callback ref. These dispositions are implementation review, not independent review or executed browser acceptance.

Timing self-review also identified an early reload while another tab's switch request remains pending. Business and diagnostic identity adapters signal both at initiation and at completion, so a view reloaded under the still-current old session is cleared again when the switch settles. The cross-tab test deliberately delays the real session command and exercises that race. A case with no owner disposition displays ReviewRequired; it never presents an unrecorded RetainedForReview decision as saved history.

Original capture review also found that the previous successful-save confirmation remained beside a later denied command. Starting another review save now clears the previous confirmation; the server-denial browser assertion checks its absence while preserving the original earlier disposition in persistence.

First draft contribution `ae2f34c090325eed776812616a01527bcd61ca50`, tree `aed2470db431be42bcdc969ee29e96e99cbcc7c4`, is retained in [draft PR #57](https://github.com/deanrfiedler-gif/powerplants-one/pull/57). Application run 34099551465, attempt 1, passed setup, static/unit/build and fresh PostgreSQL migration/seed, then its focused P11 gate failed 3/8 cases (5 passed). Desktop validation submitted while its required owner selector was still loading; original screenshot and failure context show native focus on that selector. The form now declares/waits for required selector loading and the test explicitly selects its permitted owner. Both recovery cases reached one saved unchanged retry, then incorrectly compared the stored envelope to the wire object including its separate hash. The corrected assertion checks every original envelope field and the separate exact hash, preserving both guarantees. No existing accepted assertion is removed. Later baseline gates did not run in this failed job. Original archive artifact 10010023684 is 2,797,247 bytes, SHA-256 `86c54dba72542e2fd88fe6b20266266da45bb9ee3050d582fa016080784babd2`; original captures/proofs and failure contexts are preserved without raw traces.

The [retained original failed-run archive](https://chatgpt.com/api/library/files/libfile_ce2cec3814848191925fd855a827f3e6/download) contains 34 P11 PNG/proof pairs whose exact byte counts and SHA-256 were independently checked. They identify executed PR merge checkout `2ab1c9a18fa062723b2091aacec9504761e99546`, whose tree equals the contribution tree above, source head `ae2f34c090325eed776812616a01527bcd61ca50`, run 34099551465/attempt 1 and actual viewports. Documentation 34099551609 and E1 34099551610 passed on that contribution. Passing the separate E1 workflow does not resolve the failed application gate.

Second draft `e4038caf8c4aa74d6643c4f25a4db72134c64dd7`, tree `dd7ca458be981c6c518ce1ac38a474c3da251d55`: Application 34100372839/attempt 1 completed static/unit/build, fresh migration/seed and the focused eight-case P11 gate successfully, then continued its retained P10 gates. Documentation 34100372828 passed; the complete application workflow was not yet concluded. A follow-up expands the original-envelope disclosure during desktop/phone/320px capture and wraps long operation/hash text. Focused original evidence now uploads immediately with raw Playwright result JSON, independently of the later full-suite artifact. This is not a reduction of any baseline gate.

Preserve P10's 434 unique cases and all newer accepted tests, including full database/HTTP/browser, focused repeated gates, fresh/upgrade/repeat seed/guarded reset and real P08/P09/P10/CRM/E1 restart proofs. Original PNG/HTML/PDF/proof records must retain exact source/checkout/tree/run/attempt/hash/size/viewport/page provenance. Final-head and actual-main runs, visual inspection and failed-run dispositions belong in the external record. Raw session traces, profiles and credentials must remain outside durable review artifacts.

| Procedure | Current disposition |
|---|---|
| PT-01 | P11 cross-tab/recovery tests authored; retained direct access/receipt/file/search isolation gates still required on final source. No full promotion. |
| PT-18/PT-23 | Existing exact-byte/source/template/storage recovery controls preserved. Full integrated source-movement and all-page output review remain open. |
| PT-27 | Keyboard/validation/reflow cases authored. Declared 1,000-customer/5,000-asset/10,000-appointment/10-user benchmark, raw cold/warm timing/p95 and representative screen-reader/device evidence remain open. Viewport emulation is not real-device proof. |
| PT-29 | Added bounded SC-15 and shared validation/window cases. Complete 15-screen state matrix remains open. |
| PT-30 | Blocked by its written completed-P01–P12 precondition; no shorter journey is promoted. |
| Retained full PT-06/PT-15/PT-16/PT-17/PT-19/PT-20/PT-21 | Prior full synthetic statuses remain exactly governed by P09/P10 publications. They are not erased or relabelled by this checkpoint. |

## Approved bounded Travel treatment — 7 September 2026

[Scope narrative](../prototype/scope-and-journey.md) step 8 captures **Travel and Labour**, then step 11 sends reviewed time/material evidence to Finance. The current [Finance contract](../contracts/finance-handoff.md), ADR-0016 and `finance/context.ts` deliberately accept **Labour only**. Any Travel/Break/Waiting/Other source entry blocks readiness with UnsupportedTimeBasis; whole-source conservation prevents omitting it or calling it Labour. `tests/database/finance-boundaries.test.ts` preserves this refusal.

Dean approved the professional recommendation with “OK. Proceed on that basis.” [ADR-0018](../decisions/ADR-0018-p11-travel-and-integrated-quality.md) records the selected synthetic treatment: whole-minute Travel remains separate, its entire quantity is explicitly NonBillable with a reason and no posting, and separate Finance review binds the exact evidence. NonBillable does not establish zero cost or unpaid staff time. Labour F-06 remains unchanged; other time kinds and fractional minutes remain blocked. A future agreed policy requires a successor preserving originals.

Migration 0013 adds the direction/no-posting/source-definition guards. Seed 13 appends immutable definition v2, preserves v1 and advances only the expected original synthetic policy. Existing exact-policy checks block stale unprocessed commands; original lookup and issued-byte reads retain their current-authority recovery boundary. These changes are in progress and require actual database/upgrade/browser proof; approval itself is not acceptance.

## Remaining delivery

Verify the approved bounded treatment and complete the assigned dashboard/state/load/access/output work and evidence, then final review, normal expected-head merge and actual merged-main verification/publication. P12 starter preparation is maintained separately and grants no implementation authority. No independent review, owner acceptance, production readiness or completed P11 publication is claimed.

Newer main reconciliation: CRM handover design PR #56 merged as `4a9c27d5580edc236ff22b15a65ae441c0bc69a4`, tree `ca8b1d5760072a5b19263b93b81ffff19d5038f4`. All 16 changed documents are preserved. Decision index and document-register append conflicts retain both contributions. H-01–H-03 remain unaccepted proposals; no CRM transfer is implemented. The preceding Travel head `b241209d140dcf188bb20575f2cc2baf1710f65f` had no checks because this newer-main conflict prevented the PR test merge; it is not a passing source.

Travel checkpoint `6a91ef7c2d481dca0f041c75b3d0d82ffc5624a9`, tree `968075d65e596085c18384fa2cb43ec3412246d5`: Application 34105904946/attempt 1 passed static/unit/build and fresh migration/seed, then failed 5 of 6 new focused database cases (1 passed, 0 skipped; 28.698 s). The new SQL guard variable `d` conflicted with its entry-decision alias; forward migration 0014 repairs the name while preserving executed 0013 bytes. Three upgrade cases passed original-row/definition/migration/output preservation before their receipt assertion expected RecordUnavailable instead of the actual existing Forbidden capability refusal; the exact expectation is corrected without changing permission behaviour. Later baseline/browser gates were skipped and remain unproved on this source.

P11 screen-state continuation: failed pack/report/Finance/My Jobs lists do not render prior empty results; Draft packs explicitly remain Not issued. Retained detail reads label current status unconfirmed, and Finance actions wait for a successful read. An earlier Finance receipt confirmation clears before another command; current denied Finance/pack/report actions hide the affected context. `quality-states.spec.ts` adds a fifteen-screen loading/failure/recovery/real Systems-denial matrix, explicit empty list or unavailable detail treatment, and pack/report queue regressions. The new cases are authored pending execution, with original viewport/full-page provenance required. The passing prior eight P11 browser cases remain.

Application 34106635691/attempt 1 on `7fe7186603864454a062ac17efdc83f94d3f20ac` passed all six Travel/upgrade cases (27.530 s, zero skips) and all eight P11 browser cases (84.064 s, zero skips/flakes). Its retained P10 browser gate then failed one of eight cases: the broad alert locator matched both the correct permission error and Next.js's empty accessibility route announcer. The original phone capture confirms denial with no account balance. Three P10 generic alert assertions now target the existing business-error alert; every negative amount/account/state assertion and all test identities/counts remain. The framework announcer is preserved. This is a business-equivalent selector correction, not removal of a failing policy assertion. Later baseline gates were skipped and cannot be counted as passed. Original focused and failed-run archives are retained in the external record.

Further screen-state runs retained: 34107410636/attempt 1 (head `2ead1660e0420d62ac6077caba09b6de5d337b86`) passed 10/12 browser cases; the new matrix still counted Next.js's announcer. Run 34107820724/attempt 1 (head `b36aa7d9d7f0ad1fbce5c637f258e7f80e1e6fcb`) also passed 10/12, reaching SC-04 before its English-word matcher missed the correct “This identity cannot read service requests” refusal. The matrix now compares the visible business alert to the actual denied API message and independently asserts 403/404, private/no-store and absence of restricted source identity. No permission is changed to satisfy the test.

The selected integrated UI journey is now authored in `quality-journey.spec.ts` with bounded helpers: UI-created intake, linked scope and readiness evidence, authorisation, full-crew booking, contact failure/agreement, keyboard move, original/successor issued packs, personal acknowledgements and actual capture, distinct 30 MIN Travel/90 MIN Labour, 2 EA material, reading/checklist, owner-bound offline observation/PNG, page reload and exact uncertain-receipt retry, factual return/correction, partial attendance/owned return proposal, exact report/reserved response, and same-source Finance split/review/claim/unknown lookup/reconciliation/OUT-14. This is authored work pending actual execution, not a full PT claim. It uses bounded SyntheticApi for the unknown-outcome branch; retained SyntheticManual and actual process-restart gates remain separately required. PT-30's completed-P12 precondition remains blocked.


Retained full-suite conclusions now read: Application runs 34100372839, 34100924154, 34101367814 and 34102010920 (attempt 1) completed their database, HTTP and restart stages, then each failed one of 104 final browser cases (103 passed). All four failures reached P09 phone report issuance; the original 34100924154 phone image shows the correct Reviewed report and an output still Queued with zero attempts. Its helper clicked the asynchronous render command and immediately allowed only the ordinary five-second UI assertion. The retained P09 helper now awaits the actual worker response and additionally requires HTTP 200, private/no-store, Issued state, an issue ID, available output and a positive attempt count before the unchanged Issued-heading assertion. The report screen shows active processing and disables duplicate processing/issue clicks. This is a pending correction, not a retrospectively passing run; the 240-second overall P09 case budget remains unchanged. The original full failed-run archive is 123,066,780 bytes, SHA-256 `0e5bbf98f8309422051596d35fc82c63d52ca8e5b387ced621810a011e02de76`, GitHub artifact 10012881147.

Activities and Finance queues now show summaries derived only from the successfully loaded permitted page. Activity open/due-needed counts use the current filters; Finance ReadyForReview, OutcomeUnknown and ReconciliationRequired counts use the current state filter. Each denominator is the same returned page length, and later pages/currently inaccessible records are explicitly excluded. Loading, failed and denied reads do not present stale counts as current. No balance, cost, margin, utilisation or overdue policy is inferred. The home screen links the current delivered workflow and accurately keeps P11 verification/P12 acceptance incomplete.

The PT-27 measurement script is authored pending CI execution. `quality-load-fixture.ts` adds synthetic records only to the explicitly guarded disposable `ppo_synthetic_test` database, reaching 1,000 current-workspace organisations, 5,000 assets and 10,000 appointments. Its 1,000 new draft work orders and unassigned proposals confer no work/Finance authority; all existing records and constraints remain. `quality-performance-proof.ts` records 10 independent browser contexts/sessions (two role-appropriate existing identities, not ten distinct staff), four core views, desktop/phone viewports, a 40 ms / 10 Mbps down / 5 Mbps up emulated network, actual hardware/browser/build, one browser-cold and three warm waves, all 320 raw timing/error samples, nearest-rank p95 and the candidate 3,000 ms comparison. The guarded development build and shared server/OS caches are explicit. Candidate misses remain findings, not silently dropped samples; HTTP/UI errors fail the gate. It does not establish real-device, screen-reader, production-build or whole-product conformance.

Exact additional disposable commands, after ordinary documented migration/seed and browser installation:

```sh
PPO_ALLOW_RESET=dispose-synthetic PPO_RESET_DATABASE=ppo_synthetic_test npm run db:reset
PPO_BENCHMARK_FIXTURE=add-synthetic-load node --env-file=.env.local --import tsx scripts/quality-performance-proof.ts
```

The script waits for its application process to exit. Its separate disposable CI job preserves the declared load evidence independently of journey failures, while the retained regression job uses its own unchanged database service and resets. No maintained seed includes the load records.


Checkpoint `0c5293c4d6b7cadfe9be3a57cde8bb7423998882`, tree `eae53b63a2b09d0c04c161f10ce79df9dc7c76be`: Application 34111406973/attempt 1's independent load job 101708262403 passed exact setup/static/unit/build/migrations, then failed the fixture commit with the existing `check_location_chain` guard: “Current site needs matching location evidence.” The first fixture omitted the matching initial asset-location event; the transaction rolled back. The correction adds synthetic initial-location evidence for each newly added asset, preserving the guard and original assets. No timing samples were produced by that failed job, and its empty output directory produced no artifact. Future runs write provenance before fixture execution and record whether fixture identities remain after failure. The regression job was still running at this read. Documentation 34111407436 passed; E1 34111406887 required a completed-conclusion reread.

The journey continuation also reads the existing customer/site/asset history, including the original unsuccessful cable replacement, author and SyntheticLegacy source, and separately owned OEM follow-up. Both crew members acknowledge the first issue before a controlled move and materially changed technical instruction. A second isolated browser context retains the original offline pack with its stale-reference label while the coordinator issues the successor; original and successor HTML/PDF/manifests are saved with byte/hash/source/run/page provenance. The next assigned technician's history view is exercised independently. These new checks remain authored pending actual execution.


Application 34109279993/attempt 1 completed with 12 of 14 focused P11 browser cases passed (zero skips/flakes; 1,560.553 s). This includes both complete fifteen-screen state matrices and the retained eight P11 cases. Both newly authored journeys failed at the same scope editor: the script toggled a fresh order's already-open empty editor closed. The correction opens it by keyboard only when currently collapsed; no form control or visibility assertion is bypassed. Original artifact 10014641523 is 34,640,996 bytes, SHA-256 `d1adacb82a5b27301bd506f898e61587792c23368ab13e65b5724c0bbeb24bd7`, with 368 independently hash/size-verified PNG/proof pairs and two original failure screenshots. Executed checkout `b8123d1dbcbced34c0a6c6ddea5eaaad1faaae3e` has the contribution's exact tree `6ae170bff5ddc6f7b348038a4b42a25ef84582b5`. The [original archive](https://chatgpt.com/api/library/files/libfile_2a377a9a9a9c8191916816acb7c367b4/download) retains raw result JSON and six passing Travel/upgrade cases. Later regression gates were skipped on this failed source.

One local `npm run check` subsequently failed the unchanged startup-refusal unit test's five-second child-process limit: the launcher built offline assets and imported Next before checking its environment. The launcher now validates configuration first, before either costly operation or any socket, preserving the exact refusal conditions/assertions/budget. The subsequent complete local check passed all 20 unit cases, lint/typecheck and build; the refusal test completed in 1.664 s. This is local proof; final-head disposable CI remains required.


Checkpoint `60868c2240e1eaeb17865b8e32f0075ab86fcc0c`, tree `bfa2a24c0d2e4d6be6206d1ec5b1dcb76bc12731`: Application 34112379840/attempt 1 failed both jobs. Regression job 101711310019 passed 12/14 P11 browser cases and the six Travel/upgrade database cases, then both selected journeys received the correct VersionConflict when proposing their first visit. The untouched visit form had captured a work-order version when hidden, before scope/readiness/authorisation updates. It now captures the version on first input; an edited proposal retains its version and still requires explicit conflict comparison. No stale-command guard changes. The journey is split into an earlier focused step for diagnosis; all 14 cases and the complete final suite remain required.

The independent load job 101711309814 committed the exact declared fixture, then retained 160 desktop samples. Twenty-six of forty planner samples returned 503 under ten concurrent contexts; the other 120 desktop core reads succeeded. A response waiter's unhandled timeout interrupted phone collection. The measurement now attaches handlers to navigation and response together and saves representative original failure screenshots; neither HTTP errors nor failed timing samples are excluded. Planner latency and phone completion remain unresolved findings. Original artifact 10015137950 is 2,936,619 bytes, SHA-256 `b98527725b91d11e7606e184ca0b86a72321d4ba4c1eb0a466a22fcdd7d53acb`. No p95 or full PT-27 pass is claimed from this partial run.

Original A4 review from failed-run 34100924154 inspected every page of the seven-page OUT-09 pack, three-page OUT-10 report and six-page OUT-14 Finance evidence. Text/tables were readable and contained; the older pack completion section still incorrectly says offline/reports/Finance are incomplete. A versioned successor template is needed; original issue bytes must remain unchanged. SC-14's separately loaded current applicability now has explicit loading/retry/error treatment. A denied applicability read clears document metadata and file links, and pending/error status makes no current-applicability claim. The state matrix exercises injected dependency failure and denial independently from its real server-derived actor denial. These corrections are pending exact-head verification.
