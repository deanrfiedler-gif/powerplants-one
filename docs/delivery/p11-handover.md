# P11 — Integrated quality, access and usability handover

**Revision:** r03 · **Date:** 7 September 2026 · **State:** implementation handover; exact verification and publication governed by the linked external record. **Owner:** Dean Fiedler, personal private synthetic prototype.

[Issue #54](https://github.com/deanrfiedler-gif/powerplants-one/issues/54) · [Authoritative external publication](https://github.com/deanrfiedler-gif/powerplants-one/issues/54#issuecomment-5567364667) · [P11 starter](p11-starter-prompt.md) · [Ordered plan](prototype-implementation-plan.md).

## Current implementation and verification boundary

This is the maintained P11 implementation handover. The authoritative external issue record establishes whether delivery/publication is COMPLETE and supplies the exact final contribution, actual merged-main SHA/tree, completed run IDs/attempts, original evidence and review limits. Until that record is complete, this handover does not establish P12 readiness. Historical checkpoints below retain the facts at their named sources; earlier pending or successful component statements do not establish the latest overall result.

| Area | Current behaviour |
|---|---|
| Service flow | Existing intake/scope/readiness, controlled whole-crew booking, exact original/successor pack and personal acknowledgements remain separate. Visit forms take the current work-order version when input begins; edited proposals retain their original expected version for comparison. |
| Personal evidence | Six typed captures, immutable originals/corrections, actor-wide time overlap, private PNGs and exact completion revisions remain. Current assigned crew can read permitted evidence without inventing personal attendance; capture/correction/declaration authority remains personal. |
| Offline/submission recovery | Identity switches lock saved work and remove open business context. A refused online submission offers the existing owner-verification workspace in another tab, retains the form and requires unchanged originals to resolve before resubmission. It does not auto-unlock or delete evidence. |
| Review and response | Internal overall review notes, approved-entry reasons and render attempts remain in current Service scope. Crew receive exact review facts; only the original author receives Returned correction instructions. Original Draft entry flags are historical. Exact Approved report reviews and entry/version/hash references establish technical acceptance. HTML presentation hash, report revision and kind bind each response. Completed visits do not close an Authorised work order or ticket. |
| Finance | ADR-0018 keeps all whole-minute Travel NonBillable/no-posting. Labour F-06 is 90 MIN allocated as 60 Billable + 30 NonBillable; material is 2 EA. Source declarations, independent Finance review, original claim/outcome, unknown lookup and evidence-backed reconciliation remain required. |
| Access and recovery views | Server-derived identity/current grants govern every existing command/read/receipt/file. Same-origin tab signals only remove displayed context. SC-15 uses existing current-owner recovery projections/dispositions, which cannot approve Service/Finance or restore normal access. |
| Planner and dashboards | Same current visibility predicates, full historical restrictions, bounded date/card/lane windows and repeatable-read transaction; batched summaries/lanes preserve complete common facts. Counts use the current permitted page/filter. Linked activities have first/next navigation under the exact object/actor-bound cursor, resetting when the object changes. Loading/failure removes current counts and identifies retained older data. No utilisation/cost/revenue formula is added. |
| Outputs | Additive v2 OUT-09/10/14 definitions use complete embedded Roboto and intact logo, current source recheck and immutable original-operation storage recovery. v1 definitions, rendering sources and issued bytes remain unchanged. HTML remains within the existing offline bound or explicitly fails. |

### Exact setup and maintained checks

Use Node 24.20.0, npm 11.19.0 and PostgreSQL 16.15 with the unchanged lockfile; Next 16.3.4, React 19.2.8, TypeScript 6.0.3 and Playwright 1.63.0 remain pinned. Create ignored `.env.local` from `.env.example` and configure an owned loopback database and private byte directory outside Git before these commands. Full prerequisites and the original storage guards remain in [P08 setup](p08-handover.md#runtime-setup-and-recovery).

```sh
npm ci
npx playwright install --with-deps chromium
npm run db:migrate
npm run db:seed
npm run db:health
npm run dev
```

Open `http://127.0.0.1:3000`. Production startup remains intentionally refused. The local check compiles the production build without starting or hosting it. The development compiler filesystem-cache opt-out remains intact; neither that mitigation nor the small pure Intl formatter cache is durable business/offline storage.

Migrations 0013/0014 are additive: 0014 corrects the original 0013 SQL alias without rewriting applied bytes. Seed 13 adds exact Finance definition v2; seed 14 adds three v2 output definitions under guarded original-policy checks and append-only seed receipts. Repeat seed is non-destructive; unexpected newer policy state is refused, never overwritten. Preserve database records, private bytes/manifests and owner-bound browser originals together. P11 proves upgrades/restarts through retained gates; clean restore is assigned to P12.

Only an explicitly disposable `ppo_synthetic_test` database may use:

```sh
PPO_ALLOW_RESET=dispose-synthetic PPO_RESET_DATABASE=ppo_synthetic_test npm run db:reset
```

Run the three Python checks, `npm run check`, `npm run test:db`, `npm run test:http` and `npm run test:browser` in the exact configured environment. HTTP tests require the real launcher; browser/restart gates start their documented separate processes. The [maintained workflow](../../.github/workflows/application.yml) preserves fresh/upgrade/repeat-seed/reset and actual P08/P09/P10/CRM/E1 database/application/browser restart gates. The additional serial HTTP command is:

```sh
node --env-file=.env.local --import tsx --test tests/integration/quality-http.test.ts
```

The declared load fixture additionally requires `PPO_BENCHMARK_FIXTURE=add-synthetic-load` on the disposable test database. `scripts/quality-performance-proof.ts` records fixture counts, exact build/browser/hardware/network, 320 raw samples, cold/warm limitations and nearest-rank p95 against the candidate 3 seconds. It does not simulate a real phone or supply a screen reader. No performance error or candidate miss may be hidden by excluding a sample.

### Actual evidence and unresolved procedure limits

Verified intermediate source `2ca86b3477743114260a8c55cae1f7faaaf39c4d`, tree `5e9ff090f5ab78cf920ce5a048aef1f772299313`, executed checkout `cf58571e53c87aa117a89cf4808ad671baa612d4`, Application `34130356365` attempt 1: focused quality job `101768732629` passed 11 real-DB cases, two complete selected UI journeys (496.455 seconds) and 12 identity/state cases (376.441 seconds), zero failed/skipped/flaky. Focused HTTP job `101768732627` passed its one exact-source isolation case. This source predates the final job-metadata projection, linked-activity pagination and fixture corrections, and is not substituted for final-source regression.

Original [focused archive](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34130356365/artifacts/10022441760) is 67,840,129 bytes / SHA-256 `71df9c6b17d827c9548f3ade3f735f1204ff999941898124ec57f66e0b6e01c8`, retained `libfile_7d994e43a7288191ae4bb54ece29481d`. [Journey archive](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34130356365/artifacts/10022208732) is 33,129,988 bytes / `fa5970566c4404a1c6d7a2f5f6d90d617eecd46efb056b9f490e11e732fd69a9`, retained `libfile_68702f41dd308191ad1b138f7ff7e969`. All 154 original capture/output byte/hash/source/run records were verified, including four HTML/PDF Finance records; both original next-crew report viewport captures were inspected. [HTTP archive](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34130356365/artifacts/10021935216) is 1,088,350 bytes / `bc5e1deb16ffcc5f775cef970fb6165c547b39c9d997457cab6c798ac0b79342`, retained `libfile_4c2c64d7dfa8819189e764233e9ff4ab`.

The same source still failed load with two phone Planner timeouts among 320 samples. Original artifact `10022283129` is 6,763,237 bytes / `53ce50505c22cea9ba572b33d4558e16f0edaa9914054964be81bcde8c7e4b6e`, retained `libfile_e1001b623a08819196e1343350da6143`. Those failures show no core request, no observed DOM-ready/load event, incomplete assets and unavailable failure screenshots; memory/process observations do not establish a cause. Subsequent `c78e17d` load job `101774526534` / Application `34132145115` passed all 320 reads, zero errors, while all sixteen p95 candidate groups still missed 3 seconds (warm 5.594–14.665 seconds). The final record must retain this variability; no deterministic compiler/memory remediation or candidate pass is claimed.

The current authored full application inventory is 20 unit + 306 DB + 23 retained HTTP + one serial P11 HTTP + 110 desktop/phone browser cases (460 unique cases), with focused repeats and actual reset/upgrade/restart stages separately reported. These are inventory counts until matched against exact-source raw results. Documentation and E1 workflow conclusions are additional gates, not additional unique application cases. Local exact Node/npm lint/type/unit/build and all three Python checks have passed on recorded checkpoints; PostgreSQL 16.15, HTTP/browser/private-store and actual restart proof use disposable CI because that exact local database runtime is unavailable. Implementation self-review, independent review, owner acceptance and production readiness remain distinct.

The latest named sources and failed-run dispositions below are original evidence. Full maintained counts/conclusions still require the final contribution and actual merged main. No new full PT or AT status has been promoted. Preserve prior fully verified PT-06/PT-15/PT-16 and P10 PT-17/PT-19/PT-20/PT-21 under their original publications.

| Procedure | P11 evidence boundary |
|---|---|
| PT-01 | Current actor/workspace/company/site/account/source/target/file/receipt isolation and revoked access remain in retained real DB/HTTP/browser gates. Cross-tab and SC-15 cases add visible-context isolation. The serial HTTP output isolation case has passed from fresh seed; its full-run persistent-context result must also pass. No blanket full pass. |
| PT-18/PT-23 | Three actual v2 renderer families, v1 upgrade preservation, repeat seed, exact file hashes, denied/revoked Finance reads and original recovery gates. Long A4 review supplements source/provider identities; it cannot replace every written source-movement/missing-version step. |
| PT-27 | Native keyboard intake, controlled move, submission, response and Finance approval have executed. CI viewports/network and raw timing/p95 are recorded. Candidate latency misses and unavailable representative screen-reader/real-device evidence keep the full procedure incomplete. |
| PT-29 | Twelve focused identity/validation/recovery/state browser cases, including SC-01–15 matrix and applicable loading/error/stale/denied behaviour, have passed on recorded intermediate sources. Final source and retained shared 320px gates remain required. |
| PT-30 | The selected Service/Finance journey executes actual commands and persistence. The continuous narrative's browser/process restart, stale concurrent move and restricted stale-original recovery are separately exercised by retained P05/P08/P09/P10 gates; this journey alone does not combine them. Written completed-P12 preconditions remain blocked. |

The selected journey has two tasks on one asset and two crew acknowledgements, but only one recording technician supplies personal attendance/quantities. It does not substitute for PT-14's full two-asset fixture or infer quantities for the other crew member. Synthetic booked, captured, received and retrospective time-entry dates remain distinct. Personal completion warning snapshots remain historical source context; the exact Approved review is the technical acceptance. No operational identity verification, warranty settlement, account approval, accounting policy, distribution or production readiness follows from a response.

## Historical checkpoints and original dispositions

## Authority and verified starting point

Dean invoked the maintained P11 starter. Scope covers only P11 implementation, dependencies, additive/non-destructive fixtures when needed, verification, documentation, focused issue/branch/PR and normal expected-head merge after applicable checks/review, followed by P12 starter preparation. No hosting, live integration, production migration, communication, paid change or access/rule change is included.

Connected account `deanrfiedler-gif` (231005545), private repository, admin/maintain/push/triage/pull reported true. Starting main is `744b6e6b89e69abdd0fbd4bfdd6ef5ae51e154f3`, tree `48ddac2931df66828aef42be5c78b772d9267167`. Local checkout reconstructs the exact signed commit and matches that tree; no newer main or unrelated branch is reset. Branch: `feature/p11-integrated-quality`.

P10 [publication](https://github.com/deanrfiedler-gif/powerplants-one/issues/45#issuecomment-5562750221) is explicitly COMPLETE. Issue #45 is closed completed; PR #48 is merged. Exact P10 contribution `216102e39969aa7480bcc11898b7fe03a6d6a671` and actual merge `655f17cfc7e68036939eeecc85b75069ccde8a5c` share tree `d151081ba6bc99d0cfa387332bed74aa0f502aaa`. All three contribution runs 34077653041/34077653129/34077653140 and all three merge runs 34088289930/34088289905/34088289950 independently read completed/success, attempt 1. Current main's Application/Documentation/CRM design/E1 runs 34088912272/34088912277/34088912289/34088912253 also read completed/success, attempt 1.

Accepted CRM I1/I2, E1, unchanged issued references and design-only portal direction are preserved. During this work Projects design PR #52 merged as `ebbcac5c9e210e7742bb619da3c8518592d19f31`, tree `3deb75000c13c8eea013ff3b654745768a2015ca`. The checkpoint incorporates that exact tree and its accepted documents, including PPO-012's unchanged P01–P12 boundaries; Projects J1 remains separately prepared. Its Documentation/E1 runs 34097388155/34097388159 passed; Application 34097388077 was still running at the read. No earlier focused P11 issue/branch/PR was found. Newer decisions and main must be checked again before publication.

## Implemented checkpoint

- Shared business and diagnostic tabs unmount their displayed records and unsaved forms when another tab starts an identity switch or sign-out. A same-origin channel carries only `Lock`, never identity, role, data or credentials. Reload remounts the ordinary server-backed view. The signal can only remove context; all existing server authority remains required. It is not a new grant or offline persistence mechanism.
- Shared error summaries link matched server field errors to actual controls and move keyboard focus. Shared customer creation supplies its explicit display-name/company/owner mappings and linked validation state. Unmatched errors stay textual; no nonexistent field link is fabricated.
- Required company/owner selectors show their actual loading/error state; record capture waits for pending required selector reads. Successful recovery reviews retain a visible server-save confirmation through reload. A denied recovery command clears original details and form contents even when no browser identity signal was received.
- `Observed` distinguishes Complete, Partial, BoundedWindow and unknown completeness. A bounded report window is no longer labelled a complete filtered result.
- SC-15 `/admin` connects existing document, Finance and personal offline workflows, and presents only the existing current-owner P08 recovery list. Counts describe its permitted returned window, whose existing source examines at most 100 recent owned cases. There is no whole-backlog denominator or invented business metric.
- `/admin/recovery/:id` reads the original existing owner-scoped case, source envelope and review history; records only RetainedForReview or ClarificationRequired through the existing disposition command. Current permission governs the exact-byte image route and retry. An uncertain save retains the exact command for unchanged retry. It cannot approve evidence, change an original, restore normal access, allocate quantities or execute a financial effect. Raw original detail is confined to the authorised service-owner review disclosure.

The earlier UI checkpoint introduced no schema or grant changes. The approved Travel continuation below adds migration 0013 and seed 13; no new dependency, source/template edit, policy publication command or server aggregation endpoint is introduced. Earlier immutable SQL/seed/issued bytes and existing operation hashes remain untouched. Normal business and restricted recovery remain separate.

## Verification and limitations

Current pins remain Node 24.20.0 / npm 11.19.0 / PostgreSQL 16.15 / Next.js 16.3.4 / React 19.2.8 / TypeScript 6.0.3 / Playwright 1.63.0. Exact Node/npm and locked dependencies were verified locally. Ordinary shell cloning lacked credentials; connected GitHub reads and an existing checkout supplied a hash-verified current tree. No credentials were copied into Git. Local PostgreSQL/HTTP/browser proof is not assumed from surviving files; disposable CI is required for those gates.

The first local implementation checkpoint passed lint, typecheck, 20 unit cases and build; all three Python checks passed. These results are intermediate-source checks, not the final contribution or P11 acceptance. Eight authored desktop/phone browser cases in `tests/browser/quality.spec.ts` cover cross-tab business/diagnostic clearing, real current-authority reload, mapped validation focus/input retention, real recovery persistence/unchanged uncertain retry and loading/failure/empty/denied states with 320px recovery reflow. They require actual execution. Injected validation/query errors are UI component proof, not server-policy proof.

Pre-publication self-review replaced separate sender/listener channel objects with one shared instance per tab, preventing a tab from locking itself during its own identity switch. An initial lint rejection of synchronous effect state was resolved with the error-summary callback ref. These dispositions are implementation review, not independent review or executed browser acceptance.

Timing self-review also identified an early reload while another tab's switch request remains pending. Business and diagnostic identity adapters signal both at initiation and at completion, so a view reloaded under the still-current old session is cleared again when the switch settles. The cross-tab test deliberately delays the real session command and exercises that race. A case with no owner disposition displays ReviewRequired; it never presents an unrecorded RetainedForReview decision as saved history.

Original capture review also found that the previous successful-save confirmation remained beside a later denied command. Starting another review save now clears the previous confirmation; the server-denial browser assertion checks its absence while preserving the original earlier disposition in persistence.

First draft contribution `ae2f34c090325eed776812616a01527bcd61ca50`, tree `aed2470db431be42bcdc969ee29e96e99cbcc7c4`, is retained in [draft PR #57](https://github.com/deanrfiedler-gif/powerplants-one/pull/57). Application run 34099551465, attempt 1, passed setup, static/unit/build and fresh PostgreSQL migration/seed, then its focused P11 gate failed 3/8 cases (5 passed). Desktop validation submitted while its required owner selector was still loading; original screenshot and failure context show native focus on that selector. The form now declares/waits for required selector loading and the test explicitly selects its permitted owner. Both recovery cases reached one saved unchanged retry, then incorrectly compared the stored envelope to the wire object including its separate hash. The corrected assertion checks every original envelope field and the separate exact hash, preserving both guarantees. No existing accepted assertion is removed. Later baseline gates did not run in this failed job. Original archive artifact 10010023684 is 2,797,247 bytes, SHA-256 `86c54dba72542e2fd88fe6b20266266da45bb9ee3050d582fa016080784babd2`; original captures/proofs and failure contexts are preserved without raw traces.

The [retained original failed-run archive](https://chatgpt.com/api/library/files/libfile_ce2cec3814848191925fd855a827f3e6/download) contains 34 P11 PNG/proof pairs whose exact byte counts and SHA-256 were independently checked. They identify executed PR merge checkout `2ab1c9a18fa062723b2091aacec9504761e99546`, whose tree equals the contribution tree above, source head `ae2f34c090325eed776812616a01527bcd61ca50`, run 34099551465/attempt 1 and actual viewports. Documentation 34099551609 and E1 34099551610 passed on that contribution. Passing the separate E1 workflow does not resolve the failed application gate.

Second draft `e4038caf8c4aa74d6643c4f25a4db72134c64dd7`, tree `dd7ca458be981c6c518ce1ac38a474c3da251d55`: Application 34100372839/attempt 1 completed static/unit/build, fresh migration/seed and the focused eight-case P11 gate successfully, then continued its retained P10 gates. Documentation 34100372828 passed; the complete application workflow was not yet concluded. A follow-up expands the original-envelope disclosure during desktop/phone/320px capture and wraps long operation/hash text. Focused original evidence now uploads immediately with raw Playwright result JSON, independently of the later full-suite artifact. This is not a reduction of any baseline gate.

Preserve P10's 434 unique cases and all newer accepted tests, including full database/HTTP/browser, focused repeated gates, fresh/upgrade/repeat seed/guarded reset and real P08/P09/P10/CRM/E1 restart proofs. Original PNG/HTML/PDF/proof records must retain exact source/checkout/tree/run/attempt/hash/size/viewport/page provenance. Final-head and actual-main runs, visual inspection and failed-run dispositions belong in the external record. Raw session traces, profiles and credentials must remain outside durable review artifacts.

| Procedure | Current disposition |
|---|---|
| PT-01 | P11 cross-tab/recovery tests authored; retained direct access/receipt/file/search isolation gates still required on final source. No full promotion. |
| PT-18/PT-23 | Existing exact-byte/source/template/storage recovery controls preserved. Full integrated source-movement and all-page output review remain open. |
| PT-27 | Keyboard/validation/reflow cases authored. Declared 1,000-customer/5,000-asset/10,000-appointment/10-user benchmark, raw cold/warm timing/p95 and representative screen-reader/device evidence remain open. Viewport emulation is not real-device proof. |
| PT-29 | Added bounded SC-15 and shared validation/window cases. Complete 15-screen state matrix remains open. |
| PT-30 | Blocked by its written completed-P01–P12 precondition; no shorter journey is promoted. |
| Retained full PT-06/PT-15/PT-16/PT-17/PT-19/PT-20/PT-21 | Prior full synthetic statuses remain exactly governed by P09/P10 publications. They are not erased or relabelled by this checkpoint. |

## Approved bounded Travel treatment — 7 September 2026

[Scope narrative](../prototype/scope-and-journey.md) step 8 captures **Travel and Labour**, then step 11 sends reviewed time/material evidence to Finance. The current [Finance contract](../contracts/finance-handoff.md), ADR-0016 and `finance/context.ts` deliberately accept **Labour only**. Any Travel/Break/Waiting/Other source entry blocks readiness with UnsupportedTimeBasis; whole-source conservation prevents omitting it or calling it Labour. `tests/database/finance-boundaries.test.ts` preserves this refusal.

Dean approved the professional recommendation with “OK. Proceed on that basis.” [ADR-0018](../decisions/ADR-0018-p11-travel-and-integrated-quality.md) records the selected synthetic treatment: whole-minute Travel remains separate, its entire quantity is explicitly NonBillable with a reason and no posting, and separate Finance review binds the exact evidence. NonBillable does not establish zero cost or unpaid staff time. Labour F-06 remains unchanged; other time kinds and fractional minutes remain blocked. A future agreed policy requires a successor preserving originals.

Migration 0013 adds the direction/no-posting/source-definition guards. Seed 13 appends immutable definition v2, preserves v1 and advances only the expected original synthetic policy. Existing exact-policy checks block stale unprocessed commands; original lookup and issued-byte reads retain their current-authority recovery boundary. These changes are in progress and require actual database/upgrade/browser proof; approval itself is not acceptance.

## Remaining delivery

Verify the approved bounded treatment and complete the assigned dashboard/state/load/access/output work and evidence, then final review, normal expected-head merge and actual merged-main verification/publication. P12 starter preparation is maintained separately and grants no implementation authority. No independent review, owner acceptance, production readiness or completed P11 publication is claimed.

Newer main reconciliation: CRM handover design PR #56 merged as `4a9c27d5580edc236ff22b15a65ae441c0bc69a4`, tree `ca8b1d5760072a5b19263b93b81ffff19d5038f4`. All 16 changed documents are preserved. Decision index and document-register append conflicts retain both contributions. H-01–H-03 remain unaccepted proposals; no CRM transfer is implemented. The preceding Travel head `b241209d140dcf188bb20575f2cc2baf1710f65f` had no checks because this newer-main conflict prevented the PR test merge; it is not a passing source.

Travel checkpoint `6a91ef7c2d481dca0f041c75b3d0d82ffc5624a9`, tree `968075d65e596085c18384fa2cb43ec3412246d5`: Application 34105904946/attempt 1 passed static/unit/build and fresh migration/seed, then failed 5 of 6 new focused database cases (1 passed, 0 skipped; 28.698 s). The new SQL guard variable `d` conflicted with its entry-decision alias; forward migration 0014 repairs the name while preserving executed 0013 bytes. Three upgrade cases passed original-row/definition/migration/output preservation before their receipt assertion expected RecordUnavailable instead of the actual existing Forbidden capability refusal; the exact expectation is corrected without changing permission behaviour. Later baseline/browser gates were skipped and remain unproved on this source.

P11 screen-state continuation: failed pack/report/Finance/My Jobs lists do not render prior empty results; Draft packs explicitly remain Not issued. Retained detail reads label current status unconfirmed, and Finance actions wait for a successful read. An earlier Finance receipt confirmation clears before another command; current denied Finance/pack/report actions hide the affected context. `quality-states.spec.ts` adds a fifteen-screen loading/failure/recovery/real Systems-denial matrix, explicit empty list or unavailable detail treatment, and pack/report queue regressions. The new cases are authored pending execution, with original viewport/full-page provenance required. The passing prior eight P11 browser cases remain.

Application 34106635691/attempt 1 on `7fe7186603864454a062ac17efdc83f94d3f20ac` passed all six Travel/upgrade cases (27.530 s, zero skips) and all eight P11 browser cases (84.064 s, zero skips/flakes). Its retained P10 browser gate then failed one of eight cases: the broad alert locator matched both the correct permission error and Next.js's empty accessibility route announcer. The original phone capture confirms denial with no account balance. Three P10 generic alert assertions now target the existing business-error alert; every negative amount/account/state assertion and all test identities/counts remain. The framework announcer is preserved. This is a business-equivalent selector correction, not removal of a failing policy assertion. Later baseline gates were skipped and cannot be counted as passed. Original focused and failed-run archives are retained in the external record.

Further screen-state runs retained: 34107410636/attempt 1 (head `2ead1660e0420d62ac6077caba09b6de5d337b86`) passed 10/12 browser cases; the new matrix still counted Next.js's announcer. Run 34107820724/attempt 1 (head `b36aa7d9d7f0ad1fbce5c637f258e7f80e1e6fcb`) also passed 10/12, reaching SC-04 before its English-word matcher missed the correct “This identity cannot read service requests” refusal. The matrix now compares the visible business alert to the actual denied API message and independently asserts 403/404, private/no-store and absence of restricted source identity. No permission is changed to satisfy the test.

The selected integrated UI journey is now authored in `quality-journey.spec.ts` with bounded helpers: UI-created intake, linked scope and readiness evidence, authorisation, full-crew booking, contact failure/agreement, keyboard move, original/successor issued packs, personal acknowledgements and actual capture, distinct 30 MIN Travel/90 MIN Labour, 2 EA material, reading/checklist, owner-bound offline observation/PNG, page reload and exact uncertain-receipt retry, factual return/correction, partial attendance/owned return proposal, exact report/reserved response, and same-source Finance split/review/claim/unknown lookup/reconciliation/OUT-14. This is authored work pending actual execution, not a full PT claim. It uses bounded SyntheticApi for the unknown-outcome branch; retained SyntheticManual and actual process-restart gates remain separately required. PT-30's completed-P12 precondition remains blocked.


Retained full-suite conclusions now read: Application runs 34100372839, 34100924154, 34101367814 and 34102010920 (attempt 1) completed their database, HTTP and restart stages, then each failed one of 104 final browser cases (103 passed). All four failures reached P09 phone report issuance; the original 34100924154 phone image shows the correct Reviewed report and an output still Queued with zero attempts. Its helper clicked the asynchronous render command and immediately allowed only the ordinary five-second UI assertion. The retained P09 helper now awaits the actual worker response and additionally requires HTTP 200, private/no-store, Issued state, an issue ID, available output and a positive attempt count before the unchanged Issued-heading assertion. The report screen shows active processing and disables duplicate processing/issue clicks. This is a pending correction, not a retrospectively passing run; the 240-second overall P09 case budget remains unchanged. The original full failed-run archive is 123,066,780 bytes, SHA-256 `0e5bbf98f8309422051596d35fc82c63d52ca8e5b387ced621810a011e02de76`, GitHub artifact 10012881147.

Activities and Finance queues now show summaries derived only from the successfully loaded permitted page. Activity open/due-needed counts use the current filters; Finance ReadyForReview, OutcomeUnknown and ReconciliationRequired counts use the current state filter. Each denominator is the same returned page length, and later pages/currently inaccessible records are explicitly excluded. Loading, failed and denied reads do not present stale counts as current. No balance, cost, margin, utilisation or overdue policy is inferred. The home screen links the current delivered workflow and accurately keeps P11 verification/P12 acceptance incomplete.

The PT-27 measurement script is authored pending CI execution. `quality-load-fixture.ts` adds synthetic records only to the explicitly guarded disposable `ppo_synthetic_test` database, reaching 1,000 current-workspace organisations, 5,000 assets and 10,000 appointments. Its 1,000 new draft work orders and unassigned proposals confer no work/Finance authority; all existing records and constraints remain. `quality-performance-proof.ts` records 10 independent browser contexts/sessions (two role-appropriate existing identities, not ten distinct staff), four core views, desktop/phone viewports, a 40 ms / 10 Mbps down / 5 Mbps up emulated network, actual hardware/browser/build, one browser-cold and three warm waves, all 320 raw timing/error samples, nearest-rank p95 and the candidate 3,000 ms comparison. The guarded development build and shared server/OS caches are explicit. Candidate misses remain findings, not silently dropped samples; HTTP/UI errors fail the gate. It does not establish real-device, screen-reader, production-build or whole-product conformance.

Exact additional disposable commands, after ordinary documented migration/seed and browser installation:

```sh
PPO_ALLOW_RESET=dispose-synthetic PPO_RESET_DATABASE=ppo_synthetic_test npm run db:reset
PPO_BENCHMARK_FIXTURE=add-synthetic-load node --env-file=.env.local --import tsx scripts/quality-performance-proof.ts
```

The script waits for its application process to exit. Its separate disposable CI job preserves the declared load evidence independently of journey failures, while the retained regression job uses its own unchanged database service and resets. No maintained seed includes the load records.


Checkpoint `0c5293c4d6b7cadfe9be3a57cde8bb7423998882`, tree `eae53b63a2b09d0c04c161f10ce79df9dc7c76be`: Application 34111406973/attempt 1's independent load job 101708262403 passed exact setup/static/unit/build/migrations, then failed the fixture commit with the existing `check_location_chain` guard: “Current site needs matching location evidence.” The first fixture omitted the matching initial asset-location event; the transaction rolled back. The correction adds synthetic initial-location evidence for each newly added asset, preserving the guard and original assets. No timing samples were produced by that failed job, and its empty output directory produced no artifact. Future runs write provenance before fixture execution and record whether fixture identities remain after failure. The regression job was still running at this read. Documentation 34111407436 passed; E1 34111406887 required a completed-conclusion reread.

The journey continuation also reads the existing customer/site/asset history, including the original unsuccessful cable replacement, author and SyntheticLegacy source, and separately owned OEM follow-up. Both crew members acknowledge the first issue before a controlled move and materially changed technical instruction. A second isolated browser context retains the original offline pack with its stale-reference label while the coordinator issues the successor; original and successor HTML/PDF/manifests are saved with byte/hash/source/run/page provenance. The next assigned technician's history view is exercised independently. These new checks remain authored pending actual execution.


Application 34109279993/attempt 1 completed with 12 of 14 focused P11 browser cases passed (zero skips/flakes; 1,560.553 s). This includes both complete fifteen-screen state matrices and the retained eight P11 cases. Both newly authored journeys failed at the same scope editor: the script toggled a fresh order's already-open empty editor closed. The correction opens it by keyboard only when currently collapsed; no form control or visibility assertion is bypassed. Original artifact 10014641523 is 34,640,996 bytes, SHA-256 `d1adacb82a5b27301bd506f898e61587792c23368ab13e65b5724c0bbeb24bd7`, with 368 independently hash/size-verified PNG/proof pairs and two original failure screenshots. Executed checkout `b8123d1dbcbced34c0a6c6ddea5eaaad1faaae3e` has the contribution's exact tree `6ae170bff5ddc6f7b348038a4b42a25ef84582b5`. The [original archive](https://chatgpt.com/api/library/files/libfile_2a377a9a9a9c8191916816acb7c367b4/download) retains raw result JSON and six passing Travel/upgrade cases. Later regression gates were skipped on this failed source.

One local `npm run check` subsequently failed the unchanged startup-refusal unit test's five-second child-process limit: the launcher built offline assets and imported Next before checking its environment. The launcher now validates configuration first, before either costly operation or any socket, preserving the exact refusal conditions/assertions/budget. The subsequent complete local check passed all 20 unit cases, lint/typecheck and build; the refusal test completed in 1.664 s. This is local proof; final-head disposable CI remains required.


Checkpoint `60868c2240e1eaeb17865b8e32f0075ab86fcc0c`, tree `bfa2a24c0d2e4d6be6206d1ec5b1dcb76bc12731`: Application 34112379840/attempt 1 failed both jobs. Regression job 101711310019 passed 12/14 P11 browser cases and the six Travel/upgrade database cases, then both selected journeys received the correct VersionConflict when proposing their first visit. The untouched visit form had captured a work-order version when hidden, before scope/readiness/authorisation updates. It now captures the version on first input; an edited proposal retains its version and still requires explicit conflict comparison. No stale-command guard changes. The journey is split into an earlier focused step for diagnosis; all 14 cases and the complete final suite remain required.

The independent load job 101711309814 committed the exact declared fixture, then retained 160 desktop samples. Twenty-six of forty planner samples returned 503 under ten concurrent contexts; the other 120 desktop core reads succeeded. A response waiter's unhandled timeout interrupted phone collection. The measurement now attaches handlers to navigation and response together and saves representative original failure screenshots; neither HTTP errors nor failed timing samples are excluded. Planner latency and phone completion remain unresolved findings. Original artifact 10015137950 is 2,936,619 bytes, SHA-256 `b98527725b91d11e7606e184ca0b86a72321d4ba4c1eb0a466a22fcdd7d53acb`. No p95 or full PT-27 pass is claimed from this partial run.

Original A4 review from failed-run 34100924154 inspected every page of the seven-page OUT-09 pack, three-page OUT-10 report and six-page OUT-14 Finance evidence. Text/tables were readable and contained; the older pack completion section still incorrectly says offline/reports/Finance are incomplete. A versioned successor template is needed; original issue bytes must remain unchanged. SC-14's separately loaded current applicability now has explicit loading/retry/error treatment. A denied applicability read clears document metadata and file links, and pending/error status makes no current-applicability claim. The state matrix exercises injected dependency failure and denial independently from its real server-derived actor denial. These corrections are pending exact-head verification.


Planner load correction pending verification: weekly cards now request an explicit ScheduleSummary using the same scoped appointment core and current action/booking facts. Full review/history/contact narratives remain in the separately scoped detail endpoint; omitted detail is not represented as an empty history. Historical asset/crew/contact visibility remains checked. Two new database cases compare every common summary fact with actual detail for coordinator, site observer and assigned technician, and exercise revoked/wrong-workspace/company/filter access. The declared fixture, concurrent-user count, timeouts and candidate target remain unchanged.


Checkpoint `73e854c9a03a126a81440ddfebf771166415fcc0`, tree `a7637f1268309f42cd957b3b8528f0694bc8a2b5`: Application 34115684302/attempt 1 regression job 101721798056 passed the six Travel/upgrade cases and reached controlled booking in both focused journeys. Both correctly refused because the script had left the proposed visit's preparation state Unknown, despite separately recording tool-readiness evidence. The journey now explicitly selects Preparing before saving the owned proposal; readiness evidence and preparation planning remain distinct. This changes no booking guard. The original focused artifact 10016239475 is 2,818,483 bytes, SHA-256 `326149849d7da1ef4873c2fbf573ab8c5719836f551aeb4928f81bcad7aa97ec`. Later state/full-suite gates were skipped. The independent load job was still running at this read.


Checkpoint `342cecb58383a805eb6b2f96fdf2a00038126db6`, tree `ae6c9aa418c5b67f3705ff3d542925ef4b8c8115`: Application 34116422924/attempt 1 passed all eight focused database cases, including both exact planner-summary/current-grant comparisons. Its two journeys reached first issued packs and individual acknowledgements, then timed out on a dialog locator; the appointment detail uses the existing inline “Move or reassign” region. The selector now targets that actual accessible region; all movement controls/assertions remain. Later gates were skipped and the separate load job was still running at this read.

The preceding 73e854 performance run now completed all 320 samples: 49 planner HTTP 503 results and two planner response timeouts; all 240 other-view samples succeeded. Every one of the 16 cold/warm view/viewport groups missed the candidate 3,000 ms p95. Actual profile: Chromium 153.0.8010.12, Node v24.20.0, Ubuntu CI kernel 6.17.0-1022-azure, two Intel Xeon Platinum 8573C vCPUs and 8,322,932,736 bytes RAM, declared emulated network and guarded development build. This is a failed PT-27 component benchmark, not a real-device/production/full procedure pass. Original artifact 10016462606 is 6,784,612 bytes, SHA-256 `8ea935929da85e60ceb76415e022c7cea9c36ab696d523ec5c22e3dd6d326763`.

Successor output work is pending CI verification. Seed 14 adds OUT-09/10/14 template v2 while preserving v1 definitions and original rendering sources. All new HTML/PDF remains bound to exact source/review/issue/preparation identity. Self-contained navy/green/intact-logo outputs embed the complete lossless Roboto WOFF and retain the existing 1 MiB offline limit. The initial TTF-embedding proposal exceeded that limit and was corrected before publication; no guard was increased. WOFF2 preparation was unavailable without an extra Brotli dependency, so complete lossless WOFF uses existing fontTools/zlib. Original TTF/PNG hashes are unchanged.

The new real-database template component test saves original HTML/PDF/proofs for all three families, verifies embedded asset bytes, hashes, repeated seeds, original presentation bytes and current/revoked Finance read access. P11 upgrade tests additionally preserve all v1 template definitions. The retained CRM CA-03/10 upgrade assertion now permits exactly one v2 report definition and the explicit one-version policy advance while continuing every original definition/issue/presentation/response/receipt/byte assertion. The retained P06 late-template-change case chooses a fresh unsupported successor identity/version, avoiding collision with the newly delivered v2; its no-issue and retained-original-storage assertions are unchanged. These are documented business-equivalent extensions, not deleted cases.

### 7 September 2026 · exact version-2 originals and continuing original failures

Commit `9d2dde83c50fe76c6f5930c5ef4863024b2ec63a`, tree `1aaf7f30c4858bc985137150aa2fac8bb04cb310`, executed PR checkout `cc1668fa0c2e135ee261a3927052f3dbf35cbda9` of the same tree, completed all **9 focused database cases** in Application run `34118446774` attempt 1 / job `101730634420`. This includes actual immutable version-2 OUT-09/OUT-10/OUT-14 rendering, original HTML/PDF/manifest hashes, version-1 record upgrades and repeat seed. The two UI journeys then reached personal capture and correctly rejected a Material command lacking its original authorised task. The test now explicitly selects task and asset after each saved form resets; task 2 receives its honest NotPerformed checklist. Offline originals use their actual Original authorised task label. Attribution guards are unchanged. Original focused archive: artifact `10017424222`, 15,261,614 bytes, SHA-256 `b0bb2f963a56e04fe07ebf2cd89237655faa466a615722f8233d86ecc2ee652f`, [run and originals](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34118446774/artifacts/10017424222), retained Library identity `libfile_907ac99d0f288191841fcb208f6508a4`. No full PT or integrated journey pass follows from the nine database cases.

The earlier `342cecb58383a805eb6b2f96fdf2a00038126db6` attempt-1 Application `34116422924` failed its two UI journeys at the wrong dialog locator (the detail-screen Move form is an inline region), corrected in `9d2dde8`. Original archive artifact `10016617860`: 4,563,103 bytes, SHA-256 `d7d2e66a941a4030088c70e7d31b45f6e8c7060e3f4f3e857494b404e8dd884e`, retained `libfile_ddeee155f42c8191aee985552fd5eaeb`. Its load job `101724203157` completed all 320 samples with 54 read failures and missed all 16 candidate p95 groups. Original artifact `10017197136`: 6,792,269 bytes, SHA-256 `041494701300cec1fc116663a78adf6bba14b236891443ee578edff184eaabe8`, retained `libfile_383c4d00a3748191a754ce559b2bedf1`. The subsequent `9d2dde8` load job `101730634675` completed all 320 samples with 50 Planner failures and zero failures in the other 240 reads; all 16 groups still missed 3 seconds. The first summary projection did **not** resolve the load problem. Neither result is a performance pass.

The next bounded correction batches the permitted appointment headers, original crew and request statuses instead of running the full repeated per-card query graph. It uses the existing `scopeSql`, `orderVisibility` and shared `visibility` projections, including every historical scope asset, assignment resource and contact recipient. Resource visibility is factored once for both individual and batch reads. The same repeatable-read transaction, current grants, candidate/window/lane limits and direct detail/command gates remain; no cross-request authority/business cache, pool-limit increase or timeout relaxation is introduced. The database comparison now also asserts the exact complete card ID set against individual detail visibility, preventing omitted cards from making an equivalence check vacuously pass. Actual new load results are required.

After successful disposable database setup, retained CI gates now continue after a failed focused P11 check so their own original results can be collected. Failed steps still fail the workflow; no continue-on-error or rewritten conclusions. Each maintained gate retains its actual reset/restart/setup commands and assertions. This avoids withholding unrelated regression evidence while correcting a new journey.

### 7 September 2026 · output inspection and complete keyboard command coverage

All **13 representative A4 pages** of the original `9d2dde8` version-2 database-renderer outputs were visually inspected from the retained archive: OUT-09 4 pages / 513,030 bytes / SHA-256 `195e2809fc141636da833badfbc6b79f6cc30b91d08698baa36afddb34384f42`; OUT-10 3 pages / 535,710 bytes / `8f1aea346eec59f727bfa2d8b73a385a06bde389eedd0a72d72b728574937938`; OUT-14 6 pages / 549,754 bytes / `09e8af9ac15243b254ba57b9710b6a3ef8aa444c3a9a905ac9b1b9598e4270c0`. Source, executed checkout/tree, run/attempt and exact issue/template/manifests are in each original proof. Intact logo/clear space, embedded Roboto, readable headings/body/table wrapping and page identifiers were checked on every page; no clipping/overlap observed. OUT-14 keeps dense restricted appendices and deliberately separated evidence sections. The old originals are unchanged; this component inspection is not long-journey, PDF/UA or screen-reader conformance proof.

The next journey revision exercises native Tab, typing, select-arrow and Enter input for the complete intake, controlled two-day booking move, exact field submission, exact reserved customer response and Finance approval. Date-segment navigation uses reversible native arrow keys and checks actual input values. Other journey steps still use labelled UI controls. Focus/captures and actual saved outcomes are required; these checks do not supply an unavailable representative screen reader or real device. Desktop now exercises the written selected **SyntheticManual** Finance branch. Phone retains **SyntheticApi AcceptedThenTimeout**, exact original lookup and reconciliation. Both retain separate all-Travel NonBillable/no-posting treatment and the F-06 Labour split. The two cases and every retained P10 unknown-outcome assertion remain.

`tests/integration/quality-http.test.ts` is a new serial real-HTTP component after the retained `npm run test:http` suite. It creates and issues its own exact SyntheticManual Service/Finance source, proves available original HTML/PDF hashes/bytes and receipt replay, then tests actual file/receipt/worker/source/account access across technician, coordinator, Systems, other-workspace and second-company identities. Caller-supplied identity headers confer no authority. Unknown file fields are refused. Existing search/activity projections and absent export/notification/target routes are checked for restricted results, target identities and file names. Permitted technician report/site/My Jobs remain successful without Finance content; denied reads cannot become successful empty or leaked file headers. No global reset or grant mutation occurs alongside concurrent HTTP cases. This is an authored component awaiting execution, not customer distribution or a full PT promotion.

Planner summary counts now clear during loading/failure and identify the exact current permitted date/site/resource filter; retained cards keep their last-successful-read time. The SC-07 matrix checks all four counts are unavailable during a failed refresh. A dedicated original-journey artifact uploads immediately after its step, so a failed journey can be reviewed while all retained regression gates continue. No failed step is ignored.

### 7 September 2026 · original-owner recovery and measured planner rendering

On `dd699d64c88e93b9d8d8f536306c7fcdd136880b` / tree `099263f50d5436962fcaeced6c338d3ebd5bf40c`, executed PR checkout `9c2316117b923d74cef30dda1e3248e7a67a29d3` of the same tree, Application `34120017915` attempt 1 passed all nine focused DB cases and all 12 identity/state browser cases (365.864 seconds, zero skips/flakes). Both journeys reached seven personal entries, exact accepted offline originals/PNG, first return review and successor correction, then correctly refused resubmission: switching identities locks the saved workspace even after its originals were accepted. The original desktop screenshot shows the actionable lock error and unchanged Returned report. No second submission was sent.

The correction adds **Verify saved workspace in another tab** beside this local refusal. The original form remains open with its input; the user verifies the original identity and resolves retained originals in the existing offline UI before returning. The journey exercises the real initial refusal, native-keyboard link/owner verification, unchanged five ServerSaved originals and successful later submission. No automatic unlock, queue deletion, changed ownership, client-granted authority or weakened no-unsent-original guard. Original artifact `10018298007` is 53,296,455 bytes / SHA-256 `c9300773f686c63eeadd8b8da7e3a4882de3cecee87cb0e578828e6aef213477`, retained [focused originals](https://chatgpt.com/api/library/files/libfile_cfb6a3072ee0819197015254c54eeee3/download). Full retained regression gates continue separately.

The same source's load job `101735685833` completed all 320 samples and removed the earlier Planner HTTP refusals. Two phone-cold response waits still timed out at 120 seconds (virtual users 4/5, no response received), so the unchanged failure gate failed; all 16 candidate p95 groups still missed 3 seconds. Original artifact `10018232683`: 6,693,177 bytes / `52a280731e66e8cd1b3bbc99269ef58648cb374389fca2d321820a4449df37d3`, retained [raw timings and images](https://chatgpt.com/api/library/files/libfile_68b7e2859ec08191b8ed563e861b922c/download). This is improvement in one failure mode, not a completed performance pass.

The next rendering correction reuses at most 16 immutable locale/zone Intl formatters and computes each displayed appointment day / day boundary once per planner render. This removes repeated work across every appointment × resource × day without dropping cards or changing date, timezone/DST, access, query, capacity or booking semantics. Only locale/zone formatters persist in that small map; no date result, permission or business record persists there. Existing DST gap/repeated-time assertions remain. This runtime formatting optimization is separate from the retained development-compiler filesystem-cache opt-out and gives no durable business/offline-storage guarantee. Actual new benchmark evidence is still required.


### 7 September 2026 · completed commands before final crew-history failure

Source `ef3e897a5b4423d587922d3c447242d095569a38`, tree `a8b25f3a6e44bb86dd0f4f7371fb47bd674a750d`, executed checkout `aff1f507144b5c498cd9d445a1d4bc72849649da` of the same tree: Application `34122323926` attempt 1 passed all nine focused database cases. Both selected browser cases executed the actual locked-owner refusal and native-keyboard verification/retry, unchanged originals, second factual review, remaining-work proposal, exact reserved report response, Finance return/revision/approval and reconciliation with original OUT-14. Desktop used SyntheticManual; phone used SyntheticApi AcceptedThenTimeout followed by original lookup. All five selected native-keyboard command families executed. Both cases then failed at the final second-crew History button: the existing API permits this current assigned crew member to read original entries, but the UI hid all history behind that person's actual attendance. The two cases remain failed (521.531 seconds, zero skips/flakes), not retrospectively passed.

The correction displays the existing server-permitted history without capture/correction/declaration controls when personal attendance is absent. It neither creates attendance nor changes any read/source/file grant. The journey now requires the actual read-only history heading, null personal attendance, original and successor narratives, absent correction/capture controls and absent Finance canary. Original artifact [10019074100](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34122323926/artifacts/10019074100) is 31,866,793 bytes / SHA-256 `adfd741ef6f0d75d4711e795f604b3511d91943c2ee59b9e86730c1acf2892e3`, retained `libfile_4a32c80af28c819196d9c4626d907b17`. Original long outputs are now present: each journey has a 6-page first pack, 6-page successor pack, 5-page reserved report and 7-page Finance output. Their complete inspection remains separately required.

The same source's load job `101742974764` completed 320 samples with one desktop Planner HTTP 503 and one phone-cold core-response timeout. All 16 candidate groups still miss 3 seconds. Planner warm p95 is 11.830 seconds desktop / 11.696 seconds phone, compared with 34.695 / 32.577 seconds on the preceding `75b01a2` run. This is a profile-specific improvement, not a candidate or full PT-27 pass. Artifact [10019054930](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34122323926/artifacts/10019054930) is 6,753,179 bytes / SHA-256 `76e4dc41363360b7688116ac64fd81e5e6d63fdb666de592096a0ae622ed8a4f`, retained `libfile_d1f6c7900eec8191aae3de498f7fb20d`.

The next bounded optimization reads permitted resource lanes, calendars, skills, date-overlapping blocks/exceptions and anonymous busy intervals in sets within the existing repeatable-read transaction. It preserves the exact prior lane predicate, candidate limits, complete fields, ordering and individual-refusal behaviour. The existing two planner comparison cases now also compare every lane against the unchanged individual resource endpoint and persisted availability/reservation records. Pool size, connection/statement limits, benchmark load and failure assertions are unchanged. Every failed virtual user now retains an original screenshot and at most 20 core-request event timings/statuses, without headers, bodies, query strings or session traces; evidence capture time is excluded from the already-finished sample.

The `75b01a29f9dd681669a1d4932c8a600254526d29` predecessor / tree `d2a17c9f328e6737c5cff46baf6698a0899076e4` / executed `3a9fe5c495d7bfc41f15ab2c6f6a9fb1f0eb5e50` retained the same two owner-lock failures and passed the 12 focused identity/state cases. Journey artifact `10018641937`: 19,548,655 bytes / `ceb7660ca8a5a6f0ae5e5fe1b48cc2664ccff0c3ef25be1070d5089ebfc8a7f4`, retained `libfile_4959de561c8081919d4f331666047755`. Performance artifact `10018798872`: 6,767,199 bytes / `19bec024bbf00531dd88b55297d09b5392548691feecb3b9a8d2424925352b91`, retained `libfile_1662999bb2ec8191b5a1027f4cf0fcca`; all 320 samples completed with two Planner 503s and one phone Work-order timeout, and all 16 p95 groups missed the candidate. These failures remain recorded.

Original `dd699d6` SC-01–15 desktop loaded captures were visually inspected at 1440×1000. The SC-06 current-screen text incorrectly said field capture was unimplemented. It now describes individual My Jobs capture after current-pack acknowledgement; the readiness label is Pack dispatch checks complete. The retained P06 browser assertion checks the same readiness predicate using that business-equivalent label. No issued template or original bytes changed. Main remains `4a9c27d5580edc236ff22b15a65ae441c0bc69a4`; PR #58 remains a separate unaccepted draft. Full retained workflow completion, current-source visual evidence and normal checked merge remain required.


Original visual inspection checkpoint: every loaded SC-01–15 desktop 1440×1000 and phone 390×844 capture from `dd699d6` run 34120017915 was inspected. Current accepted navigation/identity disclosure and intact branding remain; the legacy shared identity selector still takes substantial phone height. PR #58's proposed global refinements are not accepted authority here. The `ef3e897` long-output run's desktop first pack (6 pages), successor pack (6), reserved report (5), SyntheticManual Finance evidence (7), and phone SyntheticApi Finance evidence (7) were inspected in full: **31 A4 pages**. No clipping/overlap observed; long narratives and quantities remain readable. Finance appendices are small/dense, and deliberately separated sections leave sparse pages, including a short note on page 3. No PDF/UA or screen-reader claim. All **140 original journey PNG/HTML/PDF/manifest proof pairs** passed independent byte-count, SHA-256, source/tree/run/attempt checks. The retained ZIP remains the original archive; inspection PNGs were rendered only for review, not substituted for its original PDFs.


## Selected journey pass and remaining diagnostics — 7 September, 6b04f30

Source `6b04f3071a0bfa1a57b8c84f2ae16a12250ede49`, tree `1b3b812ba1be3ed104f175e3799552d2080a668c`, executed checkout `bb6381c6ddbba148e163719b60026fe411a53a41` of the same tree: Application `34124371091`, attempt 1, passes the nine focused database cases and **both selected desktop/phone journeys** (486.488 seconds, zero skips/flakes). The last step proves the other currently assigned crew member, with no personal actual attendance, sees permitted original and correction history without capture/correction controls. A subsequent assertion traversing that actor's existing Service review navigation to the issued report is authored after this source and awaits execution; it is not included in this pass.

Original journey artifact `10019858195` is 32,422,995 bytes, SHA-256 `228e194cf7f8585f76e47981179f865fff27444b2bd6d7c2e987548b0527aadf`, retained `libfile_0cde82c16ed481918ecf97caf81b7c30`. The original performance artifact `10019933209` is 6,769,782 bytes, SHA-256 `309e26c1a3883ee3487174fa476291a842fa441d622ddfeb5a2b8a7e1ec40b2d`, retained `libfile_cd7e815d5670819180dafdb31014f797`. Its 320 raw samples contain five phone Planner response-wait timeouts, no observed core GET events for those samples and no HTTP 503. All sixteen candidate p95 groups still miss three seconds. The failure screenshots could not complete within ten seconds; no missing image is represented as a successful capture. The next diagnostic revision records bounded main-document/crash events, aggregate asset counts, screenshot refusal and read-only process RSS/cgroup memory counters. It does not change concurrency, load, network, timeouts, assertions or business authority. Browser/resource failure is an investigation hypothesis, not an established cause.

The twelve state/access cases on this source produced eleven passes and one failed phone direct Systems account-observation request (`ECONNRESET`, 363.445 seconds, zero skips/flakes). The failure returned no access decision; a transport failure is not a denial pass. Its Playwright text diagnostic unexpectedly included the disposable synthetic session cookie. The unredacted durable duplicate was removed from active handover files; the private original CI artifact has its fourteen-day expiry and the available connector does not expose artifact deletion. Review derivative `P11-focused-reviewed-run-34124371091-attempt-1.zip` (retained `libfile_561e49bde03881918f33dcaa4821b02a`) is 65,803,752 bytes, SHA-256 `d7810b046d9fbc0ed3f8cfdd8b6d34c9532dac22791f86fbc6741cdc0b1b7751`. Its manifest identifies original artifact `10020079584` (65,819,234 bytes; SHA-256 `2a5e466f8f0ad4d8fac908316e3a874ebcde32ff263a8937935e5fbf944d4ac0`) and the two redacted text files, before/after lengths and hashes. Every original PNG/HTML/PDF/proof and failure status remains unchanged. A scan of all previously retained ZIPs found no other `ppo_local_session` token values.

`prepare-review-evidence.py` now removes disposable session values only from textual result/error/log diagnostics before each upload. Unexpected session material in non-diagnostic source evidence refuses upload instead of rewriting source bytes. It records each transformation's source/review hashes and provenance; original output/image bytes are untouched. Raw trace ZIPs and the generated Playwright HTML diagnostic report (which can embed raw diagnostics) are excluded from durable review uploads; original screenshots, test statuses, outputs and proof records remain available. A direct synthetic check verified failed-status preservation, session removal, unchanged issued HTML, repeat no-op and refusal to alter an unexpected source file. The HTTP component also retains its TAP result immediately after the unchanged retained HTTP suite, so its actual failure can be examined before the remaining restart/browser gates finish.


## Report-review isolation and verification scheduling correction — 7 September, b5270c8

Source `b5270c85ede2d2a8838270ad7597a22bb4725492`, tree `c305ed5e5003993bc7829c6e7a8f5d3f5ffeee72`, executed checkout `db714c4cd16c24f55f596a6800acc858192d1f8b` of the same tree, Application `34127442593` attempt 1: the declared load job `101759335498` **passed all 320 core reads with zero errors**. All sixteen cold/warm groups still miss the candidate 3,000 ms p95; warm p95 ranges from 5.206 seconds (phone My Jobs) to 13.051 seconds (desktop Work order), with Planner 10.068 seconds desktop / 9.583 seconds phone. Original artifact `10020928233`: 6,745,447 bytes, SHA-256 `251c268ec30c8fd2551a6376de6444100d83317b88ad8285ff30d119e9037fa4`, retained `libfile_9cf3b5159e448191a24d3d17c5a1b302`. Eight original PNG/proof byte/hash pairs verified. Sixty-four resource observations were recorded; cgroup counters were unavailable and named-process RSS is not a complete process census or unique memory. No crash or memory cause is established for the earlier timeouts. This passes the read-success gate, not the candidate latency target, real-device or full PT-27 procedure.

Both selected journeys again executed all previous commands, but the new next-technician report navigation assertion found an actual internal-review-note disclosure on the report detail view. The two cases remain failed (547.699 seconds, zero skips/flakes). Original artifact `10021101041`: 32,801,215 bytes, SHA-256 `128016bc32bf5c490441d25043073114a40a285c17c7f75e348c4d6870e7cb77`, retained `libfile_f843db8fdd3081918496772e4cba1d4b`. The earlier 6b04f30 pass ends at permitted capture history and does not cover this additional path. The reserved customer HTML was already free of the canary; the leak is the staff review DTO shown to an assigned crew viewer. This critical access finding prevents merge until the corrected path passes.

The correction reuses `reportContext`'s existing current scoped `service.work_order.edit` distinction for internal Service review content. Technician DTOs allowlist exact review/entry IDs, versions, decisions, source hash, authority disposition and review time, omitting internal overall remarks and approved entry narratives. Only the original report author receives the exact Returned per-entry correction instruction needed for a successor; other crew retain factual decisions and original/corrected evidence. The UI renders only supplied comments. No review, issued source/presentation byte or approval/assignment/Finance grant changes. Two added real-DB cases exercise Approved/Returned, original author/other crew, preserved decisions, current Service access, Systems/other workspace/company refusal, revoked report read and original receipt refusal, unchanged persisted review rows and allowlisted customer HTML. The real-HTTP component also checks the actual technician report DTO and bytes for the review canary. These corrections await actual execution.

The earlier dd699d6 regression job `101735686094` reached the 90-minute job limit during its 110-case final browser suite; cancellation also prevented its final artifact upload. Its original completed log records 20 unit, 304 unique DB and 23 HTTP cases passed, plus focused and actual restart stages. The final browser gate was cancelled and cannot be counted as a full pass; focused original archives from that run remain available. Focused P11 DB/journey/state steps now run in their own independent disposable job, while the original full `npm run test:db`, `npm run test:http`, `npm run test:browser` and every retained P10/P09/P08/CRM restart/reset/upgrade gate remain in their existing order. No test assertion, timeout or failure conclusion is weakened. The focused DB family is now eleven authored cases (nine prior, two review isolation). A second short disposable job runs the new serial P11 HTTP case from fresh seed for prompt failure evidence; the full regression still executes it after the retained HTTP suite on that same server. These jobs share no database/application/private storage. The full regression keeps its existing 90-minute limit; focused jobs each have 30 minutes, and all are required to pass.


The completed original log for `75b01a2` Application `34121420775` / job `101740093277` now identifies the serial HTTP failure: its final permitted technician report HTML request omitted the required `presentation_id`, receiving the correct 422 instead of 200 (66.769-second case; no skip). The preceding Finance issue/hash/receipt and restricted-role assertions had executed; later positive-read/final-original assertions had not. The test now derives the exact `IssuedReport` presentation from the current permitted report/issue, requests that identity and verifies the returned HTML presentation hash. It additionally retains the omitted-ID request as an explicit 422 assertion, so no implicit latest-version fallback is introduced. The server contract and bytes are unchanged. The same old full job was later cancelled during its final browser gate; its archive `10021587901` is 200,061,938 bytes, SHA-256 `cfe4dbe0f04ff80fe8b9b050e26be9c3d7a61f9f8b68e476b07f271d23fd0f7b`, hash/byte verified and inspected locally; its failed HTTP/browser and cancelled conclusion remain evidence.


CI-result integrity correction: focused HTTP job `101767230486` on `1d47e3d98c7fa4bc3e0be56b80210af7eb595d42` / Application `34129895375` was labelled Success although its original TAP records one failed case (74.278 seconds, the same omitted presentation identity). The newly introduced `tee` pipeline inherited GitHub's implicit `bash -e` shell without `pipefail`, allowing tee's success to mask the producer failure. The focused DB TAP pipeline had the same shell risk; its previously published nine-case passes were independently verified from raw TAP, not inferred from that label. The workflow now explicitly selects Bash, whose Actions invocation includes `-e -o pipefail`, for every run step. A direct local `false | tee /dev/null` check under those exact options returned 1. No test failure can be deliberately accepted or rewritten as a pass. Original artifact `10021750825` (1,084,101 bytes, SHA-256 `f483fe5f172553a1fc99c146880eb378255d6fa09363f065e34e06a981147ecf`) retains the failed TAP and actual Finance output. The new review-isolation fixture explicitly orders its proposed entry IDs canonically before exact persisted-set comparisons, preserving every ID/version/decision/reason assertion. Final verification requires actual raw results as well as correct completed workflow conclusions.

The same existing Service projection now scopes internal render-attempt IDs/states/error codes/counts. Crew retain the exact report status, permitted presentations and immutable issue metadata; internal job cards are absent. The Approved DB fixture queues an actual output and proves it is present for Service and absent for both crew viewers, without rendering or changing any issued bytes.


Further original evidence: `2ca86b3` focused HTTP job `101768732627` / Application `34130356365` records one passed case, zero failures in raw TAP, including the required exact presentation and hash. `1d47e3` focused quality job `101767230756` records eleven passed database cases, zero failures in raw TAP; its desktop journey passed, phone journey failed before Finance selection, and screen-state matrix returned eleven passes/one ECONNRESET failure. The phone original shows the coordinator still current although Finance was selected in the control. The helper now awaits the actual identity POST, private/no-store response and server-returned display name after native button Enter, rather than interpreting a still-enabled control as completed change; Finance identity is selected on the loaded permitted source before navigation. No identity/grant is fabricated and no request retry hides the failure.

The cancelled `75b01a2` full archive also records a real fixture collision: the new serial HTTP case reserved 17 November, already used by the retained P06 browser scenario. Only the new case moves to independently reserved 11 January 2027; the P06 fixture and every conflict assertion remain. Both full-suite P11 journeys found the site OEM follow-up absent from the first bounded activity page after earlier real fixtures accumulated. Related activities now offer scoped next/first-page navigation with explicit current-page counts, using the unchanged actor/filter-bound server cursor and visibility. The journey traverses those actual pages and still requires the exact original OEM item. It does not reset earlier suite evidence or increase the server page limit. These new corrections require full execution.


The new explicit shell correctly failed `c78e17d` focused HTTP job `101774526707` (Application `34132145115` attempt 1): raw TAP zero passed/one failed, 6.719 seconds. Moving the new fixture to January exceeded the unchanged published scheduling/calendar/resource period ending 1 January 2027. The maintained policy correctly refused confirmation with BookingBlocked. The new fixture now uses Monday 30 November 2026, verified within that published period and unused by the retained HTTP/browser/restart fixtures; no policy or resource validity is extended. This failed original remains evidence of the real guard and effective pipefail.

Additional retained originals: cancelled full `34121420775` archive is `libfile_208f28da33948191b2608f046222c579`; failed original HTTP `34129895375` archive is `libfile_97464f300eb481919cc316307bc4577e`; reviewed failed quality `34129895375` archive (57,908,891 bytes / `8b5f18c104ed642a28dcf7b41e3946ad1a75691917c7ad046350d11b885c3c01`) is `libfile_5cbd379d84708191bf82c5cdb8f6cfa4`. Hash/size and absence of raw session values verified before retention. Diagnostic-only redaction preserves status and all original issued/image bytes.


Full-suite orchestration budget: three older 90-minute full jobs (dd699d6, 75b01a2 and 6b04f30) were cancelled during their final 110-case browser gate, with original failures retained. Focused P11 work was moved into separate disposable jobs, but the retained full suite still executes the two complete P11 journeys and actual P08/P09/P10/CRM restart gates in its original persistent context. The full job now has a 120-minute outer budget so those bounded stages and the original evidence upload can complete. Every per-case, request, connection, SQL and browser timeout, benchmark target, concurrency and assertion remains unchanged. Focused jobs keep 30 minutes. This budget does not turn any failed/cancelled source into a pass. Related-activity cursor state is keyed to its exact object type/ID, so navigating to another object starts at that object’s first currently permitted page.
