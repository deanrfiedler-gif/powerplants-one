# P08 starter brief — Offline queue and exception recovery

**Prepared:** 5 September 2026 · **Owner:** Dean Fiedler · **Scope:** a future separately instructed P08 task. This file prepares P08; its existence does not start offline implementation or authorise a deployment.

Copy the task below into a new session only when ready to begin P08. First verify the final [P07 handover](p07-handover.md) and the externally maintained publication record on [P07 issue #32](https://github.com/deanrfiedler-gif/powerplants-one/issues/32). Do not treat a draft PR, a branch check, a screenshot or this starter's preparation date as proof of merged-main publication.

---

Act as senior full-stack developer, architect, product designer, business analyst and quality engineer for Powerplants One (PPO), Dean Fiedler's personal private synthetic prototype. Implement **only P08 — Offline queue and exception recovery** from the maintained ordered plan, verify it, provide a reviewable repository handover, use normal merge after applicable checks/review and verify merged main. Prepare P09's maintained starter, then stop. Use synthetic data only.

## Authority and live starting verification

I authorise necessary local implementation/dependencies, one focused P08 issue, a dedicated branch, additive forward PostgreSQL migration and non-destructive synthetic fixtures when needed, domain/API/UI changes, automated/manual checks, documentation/register updates, commits, a PR and normal expected-head merge after applicable checks and required review are satisfied. Make reasonable reversible choices. Ask only for a consequential unresolved choice or genuine access restriction, after useful preparation. No hosting, production migration, live ERP/SharePoint, customer communications, paid-service changes, access/visibility/membership/branch-rule changes or P09–P12 implementation is authorised.

Repository: https://github.com/deanrfiedler-gif/powerplants-one. Read AGENTS, README, STATUS, the P07 starter/handover/publication, this brief, ordered plan, PPO-STD-001, BP-02/BP-07, ADR-0009–0012 and any newer decisions, dictionary, API/document contracts, acceptance catalogue, decision/document/traceability registers. New maintained user decisions override older assumptions. Preserve all issued source bytes and all 78 parent identities.

Verify connected GitHub user, private visibility and actual permissions; main SHA/tree, P07 issue #32 / PR #33 states, final expected head and checks, exact merged-main workflow results and any later commits. Inspect existing P08 issues/branches/PRs before creation. Preserve unrelated local work and newer main. Do not assume old scratch/database/artifact processes remain. Resolve an incomplete P07 publication dependency before claiming P08 ready; never reset newer work to a copied SHA. The external P07 publication record is authoritative for final identities and run IDs, avoiding a self-SHA claim in the handover.

## Preserve the real P07 online foundation

Retain pinned TypeScript/Next.js/PostgreSQL architecture and dependencies unless a concrete P08 need justifies a primary-source-supported, licence/runtime-checked exact new pin. Inspect SQL constraints/triggers, identity/permissions, workspace transaction locks, operation receipts, canonical hashes, outbox, P04/P05 authority/scheduling, P06 pack guard/byte adapter and P07 start/capture/attachment/correction/completion before designing additions. Record material choices in the next appropriate ADR and amend exact physical contracts.

P07 provides current assigned jobs and Field Job, individual actual start after the complete current crew acknowledgement set, immutable attendance authority snapshots, strict six-type capture, actor-wide time overlap exclusion, successor corrections, private durable bounded PNG attachments and completion drafts. It retains only in-memory unchanged-operation retry in an open page. It has no IndexedDB/service worker/replay queue, report submission/review, approved entry set or Finance processing.

Current P06 readiness is derived; stored dispatch clearance, cached pack or an old receipt is not transferable authority. Offline start must be a provisional intent until the server adjudicates current actor, assignment/schedule/scope/issue/hash, complete crew responses and non-waivable controls in the same actual-start transaction. No one starts or acknowledges for another. Customer date agreement is separate from pack acknowledgement. Successor Draft does not authorise extra work. Tool preparation cannot waive competency, site access, isolation or shutdown. Started visits keep P05 ordinary future-move/cancel refusal. Preserve all original captures and issued bytes when authority changes.

## Bounded offline design and implementation

Implement the P08 portions of SC-09/SC-10, the applicable exception/recovery surface and DAT-08, API-C15 `/sync/operations`, online/offline TR-10 and relevant exact contracts. Use PT-11/PT-12/PT-24/PT-28 as component obligations; read every precondition and step before claiming a full pass. P09 submission/report/customer-response and P10 Finance remain absent. Completion remains a technician draft unless a separately maintained decision explicitly assigns a narrower transition to P08; do not implement reports merely to fill a command catalogue.

Define a versioned IndexedDB schema and explicit service-worker/cache scope. Separate application shell, currently permitted assigned-job context, unsubmitted local evidence, original command envelopes and local attachment bytes. Cache only necessary synthetic service-audience DTOs; exclude Finance/internal metadata and unrelated files. Explain what is available offline and when it was last verified. Offline data is stale context, not current server authority.

Prove a durable local commit before displaying locally saved. Retain Pending/LocalSaved/Sending/ServerSaved/Conflict/ReviewRequired/Failed or contract-equivalent states, with exact meanings; never show queued or local data as server accepted. Treat local quota, transaction abort, blocked database upgrade, unavailable IndexedDB, device/browser restart, storage eviction and service-worker update as explicit failures or recovery states. Do not silently fall back to memory while claiming persistence. Distinguish unsaved form changes and local save from byte storage and server receipt. Keep accessible keyboard/phone controls and readable errors with navy/green synthetic styling.

Persist one immutable original operation ID, schema version, payload and canonical hash before sending. Changed reuse must conflict; correction/new meaning gets a new operation with explicit lineage. Keep capture times, original assignment/version, exact issued hash/scope and actor context; server actor and receipt time stay authoritative. Causal dependencies must be explicit: provisional start, capture requiring accepted attendance, attachment initiation, exact byte transfer/finalisation, photo/check references and completion draft. Do not rewrite an old queued payload to make stale authority appear current.

API-C15 must use bounded batches, per-operation receipts/outcomes, deterministic dependency ordering and partial-failure isolation without dropping siblings. Current permissions apply to both new acceptance and old receipt recovery. Use durable transactional facts and existing audit/outbox semantics. A lost accepted response must recover the same receipt without another capture, attendance, correction, attachment, follow-up or draft. A failed child must not imply its parent or siblings failed, nor allow a draft to claim a required unavailable photo.

Design multi-tab/concurrent sender coordination with a durable claim/lease or equivalent recoverable ownership, no permanent stuck locks and no duplicate send effects. Retain pending operations through application and database-process restart. Explicit retry must reuse accepted/recoverable original identities. Backoff/network hints are scheduling aids, not authority or receipt evidence. Do not make an unbounded background sender or falsely claim a network callback proves connectivity.

## Permission and exception recovery boundaries

Exercise revoked identity/grants, session expiry, reassignment, cancellation before start, scope successor, pack amendment/withdrawal, stale versions and tenant/site/appointment traversal while operations are queued. Preserve factual captured evidence in the maintained narrow recovery boundary with an owned disposition where permitted; never use recovery capability as general record/file access or silently discard evidence because normal access is revoked. Define exactly which minimal metadata is recoverable by the original actor and which authorised service owner may review it, grounded in maintained contracts. Same-404 missing/out-of-scope behaviour and normal capability errors remain consistent.

Offline session/cache ownership must prevent switching synthetic identity from exposing another actor's cached jobs, filenames, thumbnails, evidence or receipts. Define sign-out, identity change, permissions refresh and stale-cache handling without deleting unsent evidence invisibly. Any retained local private material must have clear recoverable ownership and current limitations. No operational device encryption/security claim is implied by a browser prototype.

Keep attachment bytes and command metadata consistent across local/database boundaries. P07's adapter preserves immutable private provider/item/version/hash/size and verified retrieval. Validate actual supported media, bounded size/dimensions, interruption, wrong hashes, unsupported formats and missing original bytes. Storage-success/database-failure recovery uses the original operation and hash; no silent byte replacement, regenerating old issues, destructive orphan cleanup or deletion of referenced evidence. A metadata row alone is not usable attachment storage.

Preserve typed validation and task/asset authority, actor-wide time overlap checks and all correction originals. Captured quantities remain distinct from approvals/billability; no payroll rounding, automatic breaks, stock posting, tax/rates/warranty/financial inference. Failed fixes and uncertain asset identity remain explicit. Partial work retains owned remaining tasks and any proposed return visit still uses existing P04/P05 commands; do not fabricate a confirmed booking.

## Required verification and evidence

Run all maintained Python checks, `npm run check`, `npm run test:db`, `npm run test:http`, `npm run test:browser`, and preserve all P01–P07 cases or document a business-equivalent replacement for an obsolete assertion. Use exact documented startup/environment. Execute real fresh migration, P07 upgrade, repeat seed, guarded reset and PostgreSQL/application/browser restart. If local PostgreSQL/browser execution is restricted, use the already authorised disposable CI workflow, clearly identifying CI versus local evidence. Do not substitute mocks for real storage/database/browser proof or weaken a check to fit the environment.

Add meaningful real-browser tests for offline shell/context, IndexedDB durable transaction success/failure, page/browser-context restart, actual offline navigation, reload, quota/error injection at the real storage boundary, original operation recovery, multi-tab claims, schema upgrades with pending data, partial batches/dependency refusal, attachment interruption and recovery. Prove server persistence and exact original receipts through real HTTP and PostgreSQL after reconnect/restart. Include malformed/unknown fields, wrong actor/workspace/company/site/appointment/file access, stale authority and revoked receipt recovery, duplicate/changed reuse, time conflicts, late audit/receipt/outbox rollback and no premature report/order/ticket/Finance closure.

Exercise a complete synthetic offline interruption through the UI with typed evidence, at least one exact durable PNG, an original-operation retry, a controlled exception and a completion draft. Distinguish a real browser offline condition from a mocked 503. Keep required synthetic/online-incomplete labels accurate as offline components become available; PP-01 remains incomplete.

Inspect original desktop 1440×1000 and phone 390×844 screenshots after intended route/data loads. Cover current context, unsaved/local saved/sending/server saved/uncertain/conflict states, long content, errors, keyboard focus and clipping. Preserve originals with source head/tree, executed checkout, run ID/attempt, scenario, viewport, byte count and hash. Record failed-run dispositions and the exact fix; screenshots supplement persisted/API proof.

Promote a full PT/AT only when every precondition and every step actually exists and executes. Preserve other full statuses. Component-verified, implementation self-review, independent review, owner acceptance and production readiness are separate claims. Do not create fake reviewer approvals or downstream reports for test convenience.

## Repository handover, merge and stop

Maintain one focused issue and branch. Update README, STATUS, ordered plan, ADR, dictionary/API/document contracts, affected registers and a detailed P08 handover. Explain delivered behaviour/exclusions, exact schema/operation/dependency/recovery choices, current capability boundaries, migration/seed compatibility, preserved original evidence, exact runtime and reproducible setup/check/recovery commands, test counts/results/limits, failed-run dispositions, original screenshot/media provenance and remaining operational/integrated dependencies.

Before normal merge, re-read actual PR head, required checks/reviews, unresolved threads, current main, permissions and applicable rules. Record inaccessible rule reads accurately; do not bypass or modify controls. Merge using expected head SHA. After merge verify exact main SHA/tree, PR/issue states and actual merged-main workflows. Place final publication evidence in an external issue record linked by the handover; do not make circular self-SHA claims.

Prepare a detailed maintained P09 starter requiring verified P08 publication first and preserving review/report/customer-response versus Finance boundaries. Stop after verified P08 publication and P09 preparation. Do not begin P09, Finance, hosting, live integration, production migration or customer communication.
