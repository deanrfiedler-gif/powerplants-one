# Mobile CRM implementation handover

**Revision:** r01 · **Date:** 8 September 2026 · **Owner:** Dean Fiedler · **Parent:** PPO-009 / issue #9.

Implementation is based on main `923bd9b90412c80782331b7085a6880750f12640` in the existing Next.js/PostgreSQL application. The [mapping decision](../decisions/mobile-crm-implementation.md) identifies components, data and permissions. The approved standalone walkthrough remains reference evidence; this increment changes the working application.

## Delivered behaviour

- Shared mobile header, five-destination bottom navigation, keyboard-contained More menu, tab panels, 44px controls, 16px inputs and text-labelled status styles. Desktop navigation and CRM Board/List remain available.
- Opportunity cards emphasise the primary customer contact. Internal owner remains in Details, List and filtering; Activity owner remains with the action.
- Organisation lookup uses bounded server search and explicit result selection. Changing its text clears selection and dependent site/contact context immediately. Loading, empty, error, revoked access and stale-version states remain distinct.
- Timeline opens the saved Activity. Completing it preserves its outcome and provides a return path for creating a separate follow-up. Date/time entry uses Brisbane time with an exact persisted instant. Existing command receipts, version checks and reconciliation remain in use.
- Details contains customer context and Requirements and scope; Commercial resolves the existing estimate and exact draft quotation versions through current permissions.
- Organisation Details shows a count summary and View sites. Sites contains the permitted sites and their facility/growing-area hierarchy. Estimating and Service use the common shell, form and status presentation.

## Verification record

The first implementation passed lint, TypeScript, 20 unit cases and production build. The repeated local static/unit/build check also passed after the first layout refinement; the final focus and shared-heading capture corrections are included in the next CI run. Local Chromium 143 rendered ten views at 320px, 390px and 1440px with fixture API responses; the captures were visually reviewed. That exercise validates presentation and lookup invalidation only, not backend persistence or permissions.

Three new PostgreSQL cases cover commercial/primary-contact projections, bounded scoped directory search and permitted site/facility counts. Three new browser procedures run in both phone and desktop projects: complete/reopen/follow-up persistence, Organisation Sites/navigation/keyboard checks, and lookup invalidation/unsaved-draft protection. Existing CRM conflict, receipt, permission and I2 responsive suites were adapted to the accessible controls and tabs. The application workflow runs the new focused database and browser cases before the existing full regression, including the repository's real database-restart proof.

Local PostgreSQL installation was unavailable under the execution environment's user/group restrictions. Database and HTTP/browser integration results must therefore come from disposable PostgreSQL in repository CI; no local fixture render is counted as this evidence. Foundation links to existing screenshot evidence cannot all be checked in the partial local checkout; full-checkout CI remains authoritative for that gate.

[PR #61](https://github.com/deanrfiedler-gif/powerplants-one/pull/61) contains the implementation. At commit `1a379ac2`, [Application run 34173507638](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34173507638) passed all three new PostgreSQL cases and 13/14 focused browser cases, including completion and persisted follow-up on phone and desktop. The remaining case identified mobile menu focus leaving the dialog; explicit keyboard wrapping was added. [E1 run 34173507755](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34173507755) reached the restart proof but its screenshot locator referenced the replaced estimate heading class; capture helpers now use the shared heading. Documentation assurance passed. These failed runs are retained; corrected-head regression remains in progress. No merge, deployment, live-data test, physical-phone test or business acceptance is claimed by this working record.

## Remaining implementation sequence

1. Finish the current CI and phone/desktop journey checks, review permission/error states, and correct any regressions before integration.
2. Implement the shared Facility type/common/conditional-field contract and its approved greenhouse, berry and mixed-nursery scenarios. Carry exact Organisation → Site → Facility/Equipment IDs into estimate and service scope; prevent cross-site assignments. Current counts show permitted linked records, not an invented active-only total.
3. Apply the components to the bounded Projects J1 runtime when its dependencies are implemented. Projects currently remains Planned; there is no project persistence or permissions surface to verify in this increment.
4. Extend module-specific estimating/service flows through their current contracts. Opportunity Files and richer scope attributes require their actual document/field contracts; commercial draft outputs are already linked through E1.
5. Review the verified journey on a physical phone before an operational pilot. Production identity, hosting, integrations and customer data retain their separate existing readiness requirements.

## Corrected-head results and publication continuation

At published commit `84f1df3c040a174c37ca8b9ab4e517697941fc93`, [Application run 34173922048](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34173922048) passed 3/3 mobile projection/database cases and 14/14 CRM journey/browser cases. The wider I2 suite passed 10/12; the two failures were the same desktop/phone test waiting for Organisation related Activities before opening the new Timeline tab. The local test now opens Timeline before asserting the actual response and canonical Activity link. The subsequent full application regression/restart stages were skipped and remain pending.

[Estimating E1 run 34173922045](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34173922045) passed 11 PostgreSQL cases, 2 HTTP cases, all 10 desktop/phone cases, and the three-application/two-database-restart proof at that published commit. Documentation run 34173922080 passed. Local final lint, typecheck, 20 unit cases and build passed after the header/style refinements. The final twelve-view fixture review also passed, including explicit menu keyboard wrapping and no horizontal page overflow at 320/390/1440px; it remains UI-only evidence in Chromium 143.

Automatic approval review rejected publishing the final patch to the same private repository, stating that the current user-authored transcript must explicitly authorise the destination and code publication. Repository identity/permissions and prior user-authored mobile implementation authority were checked; the latter was retrieved through Personal Context. Review still rejected the retry. No alternate publishing route, merge or deployment was attempted. Dean subsequently confirmed publication of the named final-refinements patch to `deanrfiedler-gif/powerplants-one` and the update and verification of PR #61. This confirms the intended repository work, but automatic approval review subsequently rejected both the combined 31-file upload and the exact original nine-file patch. The stated reason remained missing explicit current authorisation for the payload and destination. No repository write succeeded in this continuation; PR #61 remains at `84f1df3c`. No alternate publishing route or merge was attempted.

Before publication, P11 merged into main at `b3597f79413f87b9b2f1ce75a66a2addefbb0e34`. The branch integrates that exact main, retaining its session clearing, recovery, scoped reads, original-output and workflow checks. Two overlapping UI hunks were resolved to keep both the mobile Organisation Timeline and keyed related Activities, and both the mobile header and active Service planner navigation on appointment details. Lookup inputs also expose the existing P11 validation target attribute. Repository CI must run again on the combined final patch before integration.


The reconciled local source passed lint, TypeScript, all 20 unit tests and production build. Naming assurance passed with 86 document records and PP-01 assurance retained all 78 parent dispositions. A repeated twelve-view fixture browser review passed at 320/390/1440px with no document overflow or browser errors; refreshed board, Organisation and Estimating captures were inspected. This is UI-only evidence and does not replace disposable-database CI. The reviewable `ppo-mobile-crm-main-ready.patch` applies to main `b3597f79`; it includes the mobile changes while preserving the merged P11 baseline. Publication, combined backend/browser regression, normal merge and actual-main verification remain blocked/pending.

## Explicit publication and final verification

Dean explicitly authorised uploading `ppo-mobile-crm-main-ready.patch` to `deanrfiedler-gif/powerplants-one`, updating PR #61, completing verification and merging when checks pass. Publication succeeded at `ffa1728825e91beefcab9177487a21110b5b6f5d`, tree `f74b806be292f9987815ae45c869da56fe2b1567`, preserving both prior mobile history and P11 main. The earlier publication blocks above are historical. Integration review also aligned the retained P11 recovery keyboard test with the phone All modules menu; its recovery/original/permission assertions remain unchanged.

The [PR #61 publication record](https://github.com/deanrfiedler-gif/powerplants-one/pull/61) is authoritative for subsequent exact-head CI outcomes, normal merge and actual merged-main checks. These run results are recorded externally to avoid changing the tested source merely to append its own final commit ID. No fixture capture counts as PostgreSQL persistence or permission evidence. Physical-phone review and operational acceptance remain separate.

Integration layout check: the longer Exceptions and recovery module label overlapped the identity button at 320px (not at 390px). The shared mobile heading now shrinks with an ellipsis; the full title remains in the page heading. The retained P11 recovery journey asserts separation of those actual controls at 320px and preserves a capture.

Full combined run 34176816678 attempt 1 passed 117/118 browser cases. The sole failure was the inherited mobile PT-29 matrix waiting 15 seconds for an injected unavailable-record response. The response timer incorrectly included page reload and independent identity setup. Baseline main run 34174884619 separately passed 111/112 browser cases and timed out on document load in the same matrix. The matrix now waits for DOM readiness and an actual successful selected GET before assessing the loaded screen; the unavailable-response budget starts after identity readiness and the selected request is observed. It still requires the actual 404 response, visible unavailable state, recovery, and real server-derived Systems denial. The 15-second response, 60-second navigation and six-minute procedure limits remain, with explicit prerequisite sequencing and named screen steps for diagnostics. Original failures and stopped/superseded attempts remain recorded in PR #61; no cancelled stage is counted as a pass. This corrects test sequencing and does not establish a root cause for every earlier intermittent transport failure.

The stopped 8965ba5 run 34176567554 additionally exposed a 60-second document-load timeout at the crew acknowledgement journey's personal-job navigation. That navigation now waits for the actual actor-scoped job GET and rendered identity/error readiness before using the original acknowledgement and start controls. Its 60-second navigation and ten-minute overall limits, original evidence, exact acknowledgement and server-save assertions remain. All source/grant/receipt application behaviour is unchanged by these test corrections.

## Browser and output lifecycle investigation — 8 September continuation

Dean explicitly authorised isolation, bounded instrumentation/correction and normal verified merge of PR #61. Refreshed head remains `1c7a62b66eeb8d59aa20201e770e7f7f6378849c`; main advanced to `b489a8c0a68271dc10dd92c85f06bbee7606559f` through design PR #62. The continuation preserves that contribution and both workstreams' status/register entries. Approved mobile presentation is unchanged.

Confirmation job `101935503159` is now complete: 116/118 browser procedures passed, zero skipped. The pack retry response again exceeded its original 15-second wait; the Sites recovery phase independently failed the five-second loading-count assertion. The latter snapshot contains loaded Site content and a still-loading related-record section. The pack's later error snapshot shows the original output Issued, which establishes eventual visible issuance only, not when the response arrived relative to its deadline. All later original Finance/report/database/HTTP/persistence/offline restart stages passed. Load confirmation job `101935503378` failed 3/320 samples; all sixteen candidate 3,000 ms p95 groups were unmet. Candidate timing failure and failed-read CI failure remain separate.

The original load ZIPs `10039369615` / `10040980411` and browser ZIPs `10039833761` / `10041601498` were materialised from their exact GitHub-returned file IDs and inspected. SHA-256 values respectively: `dff8a55ec7ea3eb4f4f8486d50486a45a45fc62f986ccf0b942827b6d68353ef`, `5ea86bbe81344696380b01ac6f9fa4017b4dc294a4e418e3b6272e1495533d85`, `5cb82613ea917c0fefa3a244a2434229cd0f6ded462b3ff84af7d264c0c1db31`, `016b6f32f0b908db91031728868a75e56e03668e8834b703d0c87255d75bab16`. Raw trace ZIPs are excluded and were not inspected.

Next experiment retains all 320 samples, ten concurrent contexts, four waves, global network rules, assertions and deadlines. Opt-in local-only diagnostics record bounded server request arrival/finish/incomplete-close timings, protocol asset bytes/completion and declared-rule observations, and renderer launch/page/content/font/PDF/close stages. Failed samples are frozen before at most three five-second independent server-asset probes and one bounded renderer liveness probe; these are auxiliary observations and never substitute for browser reads. Render tests additionally retain wait/request/response/click lifecycle timestamps while preserving rejection outcomes and existing timeouts. No headers, queries, bodies, identities, credentials or generated document contents enter the added logs; caps and redaction checks remain explicit.

Hypotheses remain open: requests absent from server logs indicate browser/request scheduling; server-arrived but unfinished assets indicate delivery/handler work; server-finished assets with stalled protocol completion or renderer liveness indicate browser-side consumption. Render lifecycle stages distinguish action readiness, request/route delay, Chromium/PDF work and post-render completion. This is diagnostic instrumentation, not a proven application or harness fix. An unchanged green run alone cannot close the causal investigation.

All local runtime/source files were verified against the current GitHub tree. Historical screenshot assets are absent from the partial local checkout, so full foundation evidence-link assurance remains a CI check. Local pinned Chromium download is unavailable; exact-browser and disposable PostgreSQL integration proof must run in CI. Current-head results, causal conclusions and any subsequent smallest correction will be recorded in PR #61 before merging. No production, real-device or operational acceptance is claimed.

Instrumentation follow-up: `c50301e0` passed documentation, E1, focused quality and HTTP/output isolation. Its focused pack response observations were approximately 2.9–3.8 seconds; these do not explain the full-suite failures. The initial renderer-stage instrumentation changed a file included in v2's source fingerprint. Before final integration, it is removed from that file and placed around worker claim/render calls instead. All nine original fingerprinted sources now match `1c7a62b6` byte-for-byte; no template successor or existing-template rewrite is intended. Results from `c50301e0` remain diagnostic evidence from its declared source, not final compatibility proof.

Source inspection additionally identified an unbounded post-view screenshot in the load harness. It now has the same ten-second bound as failed-sample captures; failures are retained and independently fail the gate after subsequent samples are attempted. Each completed wave is also logged. A 25-minute step limit inside the existing 30-minute job leaves time to preserve incomplete diagnostic evidence; incomplete or failed sampling never becomes a pass. No response, action, navigation or overall job deadline is increased.

A separate controlled comparison runs the entire unchanged 320-sample protocol using the pinned bundled `chromium` channel instead of Playwright's default headless shell. Both arms retain global asset/API throttling and ten concurrent contexts. Browser selection is recorded in the proof; neither result replaces the other. [Playwright's browser documentation](https://playwright.dev/docs/browsers#chromium-new-headless-mode) documents these distinct implementations. This tests a browser-implementation hypothesis; it is not an assumed cure or a permission to omit the original gate.

A separate local Node-only probe completed 80 document/asset loads across Customers and Planner, ten clients and four waves each, with no delivery failures (maximum approximately 1.1 seconds). Earlier local attempts failed before sampling because cross-command loopback access was refused and the first server retained a development-directory lock; those observations are preserved. The successful probe provides no browser, emulation, database or CRM API acceptance and cannot substitute for CI.

The instrumented shell load at `c50301e0` reached the original job limit with 310/320 persisted samples: 287 passed, 23 failed, final wave incomplete. All failed observations had no core GET. Eight cold renderers answered the bounded JavaScript probe while their asset streams stalled; fifteen subsequent-navigation probes did not answer. All 24 independent post-failure static downloads completed with HTTP 200 in 35–357 ms. The server completed 5,336 of 5,343 static requests (maximum 2,872 ms); the other seven closed incomplete within 14–54 ms. In the cold failure cohorts, all ten representative devtools-script responses finished on the server. This supports browser-side transport/consumption after delivery, not a demonstrated CRM API defect or OOM. The logs intentionally contain no per-user identifier, so correlation is at cohort level. Artifact [10043894944](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34193761221/artifacts/10043894944), SHA-256 `3b5f5058d2279ec466d965b0bdb041b48056a1e3c8b3451116a596c93bfd058b`, retains the incomplete proof. The unbounded group screenshot found by inspection is a separate harness defect; this cancellation happened during sampling and does not prove that screenshot caused it.

On exact head `c29e8f42` / tree `3a8b34ce1846a551be19c4e34cb2d810a463883b`, pinned full Chromium 153.0.8010.12 completed **320/320** samples and all captures. All 640 observed core GETs carried the global rule. All sixteen candidate p95 groups remained unmet (approximately 8.2–21.8 seconds), with one individual sample about 39.2 seconds. Artifact [10044213717](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34196005783/artifacts/10044213717), SHA-256 `8b316b5bca9990ffdfade036c5693a920843a46783018bf710973e04d4060946`, is comparison evidence, not current final-head acceptance. The same-head shell arm retained **312/320** passing samples: phone Customers failed once, Work order four times, and My Jobs three times; its step also hit the 25-minute diagnostic deadline. All eight failures again had no core GET; three cold renderers replied while asset downloads stalled, and all nine independent post-failure asset downloads succeeded (16–61 ms). Artifact [10044690147](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34196005783/artifacts/10044690147), SHA-256 `6e7eca12a4df3fd00da71c7de7f6b5e91b701b74f9db387d63436cb4a78a9a1b`, preserves every recorded sample and failure. Selecting the pinned full Chromium channel was a proposed browser-harness correction, subsequently rejected as a sufficient cure by the fresh run below; the same complete load contract and every browser procedure remain. The temporary duplicate comparison job is removed after its evidence is retained. The precise upstream Chromium subsystem and long-term recurrence rate are not established by one comparison.

Document generation was investigated separately. The instrumented full browser run again passed 116/118 with zero skipped. The failed phone pack request started 279 ms after observation began, and its click resolved at 307 ms. Server timing shows Chromium launch alone took **12,877 ms**; page/content/font/PDF/close work then took **1,594 ms**. The server completed HTTP 200 after **15,972 ms**, beyond the unchanged 15-second response wait. This was a slow renderer launch, not missing click readiness or a pre-API asset stall. All 41 observed renders launched and completed; the next-slowest launch was 3,207 ms. The four report response waits passed in 3.3–5.3 seconds, which does not retrospectively explain the original report failure. A separate phone screen-matrix failure waited for its My Jobs detail GET. Artifact [10044479706](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34193761221/artifacts/10044479706), SHA-256 `81c89a79f88fc8e542e346e735618726a70b4d697a6e39ada2b8800cd0a55b78`, retains JSON, contexts, captures and bounded server metadata; raw traces were excluded and not inspected. No render timeout, renderer implementation, output source fingerprint or issuance assertion is changed to make that result pass. Final combined-head regression must establish the current outcome; launch variability remains a limitation to report.

Main subsequently advanced to `4a632088afb55224761fa722c98fcbea2546da81`, disabling Playwright server management in CI. The cited prior job [101941207079](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34188377381/job/101941207079) actually failed the load core-read assertion; Python review preparation succeeded and artifact upload completed. New-main focused job [101968872873](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34197655415/job/101968872873) then failed all 20 browser procedures with loopback connection refusals. Ordinary browser workflow steps invoke Playwright without a separately started server; explicit HTTP/restart scripts have their own lifecycles. The PR integrates that main history and restores Playwright ownership for ordinary CI browser steps. This corrects the demonstrated server-ownership regression without treating it as the cause of the earlier load or render failures.


Fresh verification on `57030e4793367289f0e3b84ad8bfe37e10be6ee0` / tree `b8ece5844bfb84b14d96ce4d29e39dac6fbe983f` rejected browser selection as a sufficient cure. Load job `101971208205` persisted 300/320 samples: 290 passed, ten failed, twenty incomplete at the 25-minute diagnostic deadline. All ten failures recorded no core GET. Four cold failures completed only 0–2 assets but answered the renderer probe; six subsequent-navigation failures did not answer it. All twelve independent static downloads completed HTTP 200 in 14–103 ms. Artifact [10045658044](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34198376281/artifacts/10045658044), SHA-256 `ffb203e0c8912df2068271248e88b3e82b37905e04fc12493f665948cf4796db`, retains the failures. No complete current-head p95 result is claimed; all sixteen groups missed the candidate-only target in the unchanged confirmation, separately from failed-read gating. Documentation, E1, focused quality/access and HTTP/output isolation passed; full regression was still running when this diagnostic follow-up was prepared. The c29 full regression ended with runner shutdown signal/exit 143 at 07:30:06 UTC, without a completed full-browser result. Neither an interrupted run nor the earlier successful comparison replaces current-head verification.

The next bounded diagnostic keeps the current browser choice fixed and adds Chromium NetLog in its default private-information-stripping mode, with a 64 MiB bound plus constants and automatic flush after 1,380 seconds. [Chromium's primary capture documentation](https://www.chromium.org/for-testers/providing-network-details/) describes these supported startup flags. Raw capture and temporary fragments stay in a dedicated runner temporary directory outside all artifact roots and are removed during preparation. Only allowlisted event/source/phase symbols, numeric timings/dependencies/byte counts/status/error codes and same-origin static build paths survive; headers, bodies, arbitrary parameter strings, credentials, query strings, raw bytes and external URLs never enter the retained metadata. Canary tests cover exclusion and the explicit 100,000-event retention cap. Invalid/incomplete capture preserves its status and fails preparation rather than exposing raw fragments. Capture overhead, capped coverage and the lack of per-user NetLog source mapping remain limitations.

This experiment distinguishes HTTP transaction/socket progress inside Chromium from the already observed server completion and stalled CDP response delivery. It changes no network rule, asset coverage, measured samples, concurrency, UI, controlled renderer implementation, template fingerprint or response/action/navigation deadline. It is diagnostic instrumentation, not an application correction or a new green acceptance claim. Merge remains blocked until a supported correction and all applicable exact-head checks pass.

## Stall isolation by single-factor reproduction and correction — 8 September

A parallel continuation reported reproducing the load stall outside CI on the exact pins (Node 24.20.0, Playwright 1.63.0, Chromium build 1243, PostgreSQL 16.15) in a disposable two-CPU Linux container running the unchanged `scripts/quality-performance-proof.ts` from `c50301e0` with `PPO_PROOF_DIAGNOSTICS=1`. The retained CI evidence was first re-read from GitHub through the authenticated browser session and verified: load artifacts `10039369615` (`dff8a55e…`) and `10040980411` (`5ea86bbe…`), browser artifacts `10039833761` (`5cb82613…`) and `10041601498` (`016b6f32…`), run 34193761221's partial load artifact `10043894944` (`3b5f5058…`, 310/320 persisted, 23 failed) and its browser artifact `10044479706` (`81c89a79…`, 116/118).

Every stalled sample, in CI and locally, has the same shape. The server received and finished each request that reached it (static assets p50 ≈ 50–130 ms, p95 ≤ 1.7 s; no incomplete closes for those assets); independent post-failure downloads of the same assets completed in 14–357 ms. The renderer answered its probe with `readyState: loading`, `visibilityState: visible`, `onLine: true`. Chromium's network service reported response headers (`Network.responseReceivedExtraInfo`) for a script the renderer never received (`Network.responseReceived` absent), the render-blocking stylesheet body stopped mid-stream, the six same-host sockets stayed occupied, and the remaining chunk requests were never sent (the server received exactly nine of ten). Later navigations for the same context never reached the server. No application code executed in a stalled sample. Some stalled contexts recovered after four to six minutes; the mobile PT-29 SC-10 failure in run 34193761221 shows the same shape in the unthrottled test browser (document and chunks served by 06:55:20 UTC, first page reads reaching the server at 06:59:26 UTC).

Labelled single-factor variants of the same 320-sample procedure (never the accepted configuration) then discriminated the trigger: accepted procedure — stalls (one context by sample 90 before the run was discarded because a native Windows journey run reset the shared disposable database; the 401 samples after that reset are recorded as local interference, not evidence); without `Network.overrideNetworkState` — stalls (4 failures by sample 210); legacy `Network.emulateNetworkConditions` — 6/320 failed (users 8 and 9); **no network emulation — 320/320; by-rule throttling without `Network.enable` on the observation session — 320/320; one Chromium browser process per virtual user with throttling, observation session and every assertion unchanged — 320/320.** Those reported variants support a process-sharing/DevTools interaction hypothesis. Their raw reproduction scripts and results are not linked as retained artifacts in this commit and have not been independently re-inspected in this continuation. They do not establish that every retained failure requires throttling, particularly the separately retained unthrottled full-suite stalls. The ten-process correction remains subject to exact-head verification.

Correction: the load proof now launches one Chromium process per virtual user (each with its own network service and DevTools sessions) and opens the same fresh context and server-issued session per view wave. Samples (320), concurrency (10), waves (4), global by-rule network conditions, per-request rule verification, response/navigation deadlines, capture assertions, candidate p95 reporting and the retained failure-freezing diagnostics are unchanged; the profile records the process topology. NetLog capture, if enabled, is written per browser process and the metadata preparation reads every capture with the same bounds. Added failure-time observations: network-service header timing per asset, renderer document/visibility/online state, and two bounded renderer-issued fetches (same host and alternate loopback host name). The corrected script completed 320/320 locally on the first confirmation; exact-head CI on the pushed candidate is the gate and its results are recorded in PR #61.

Document generation: the mobile pack retry in run 34193761221 was sent 279 ms after observation began, completed successfully on the server in 15,972 ms because the controlled Chromium launch took 12,877 ms, and the test's 15-second UI action budget rejected the wait one second before the answer arrived; the other forty renders in that suite took 2.4–5.6 s. The render retry is a synchronous controlled command bounded by the application's two-minute render lease, so `observedResponse` now waits for that response up to the lease (`RENDER_LEASE_MS`), applied to the pack, report and journey report helpers. The click keeps its 15-second action timeout, every assertion on the answer (status, cache control, Issued, issue id, output availability, attempts) is unchanged, no renderer or template changed, and the lifecycle attachment records the bound. The original report failure in attempt 1 (click waiting for scheduled navigations) remains unexplained and retained.

Residuals: the PT-29 mobile SC-10 stall above is retained as a failed observation in the ordinary suite and is not corrected here; all sixteen candidate 3,000 ms p95 groups remain unmet in every run (development-server load, candidate-only, separate from the failed-read gate); no production, real-device or operational acceptance is claimed.


### Integration review of concurrent 48539c54

The branch advanced from `178b5390` to `48539c54d2e1e88e442eb146abfa8e5e5c1849dc` while the network-capture coverage correction was being tested. Its history, ten-process load topology, global rules, complete sample contract and new network-service/renderer observations are preserved. Reported local single-factor evidence above remains attributed to that continuation; the raw experiment code/results must be retained for independent causal review. Same-host/alternate-host renderer probes observe fetch-header completion only, not successful asset-body delivery; the alternate hostname remains subject to the gateway's existing host rejection.

The concurrent 15,000-to-120,000 ms render-response timeout increase is withdrawn. The two-minute render lease controls when another worker may claim recovery; the source does not enforce an HTTP completion deadline at lease expiry. Therefore it is not an evidence-backed replacement for the existing response budget. The observed 12,877 ms browser launch explains the specific late pack response but is not corrected by making the waiter longer. Original response and click bounds are restored, all answer/issuance/output assertions remain, and the added journey-report lifecycle attachment is retained. The original report scheduled-navigation failure remains unexplained. This follows Dean's explicit instruction against increasing timeouts merely for green checks.

The first NetLog head `178b5390` passed 320/320 reads, all captures and all 640 globally ruled core GETs; all sixteen candidate p95 groups remained unmet at 6.8–15.3 seconds. Artifact [10046388979](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34201761718/artifacts/10046388979), SHA-256 `fa43c30aeb1683db79b3c82106530a846e7b55fa521ef53133103838cbde0381`, contains no raw NetLog or trace ZIP. No stall reproduced. Packet events filled the raw ring and metadata tail: 545,637 raw events, 100,000 retained events covering only 08:04:19.455–08:04:30.288 UTC. This cannot explain earlier outliers or failed attempts.

The integrated capture correction retains the concurrent per-process 64 MiB raw bounds (ten files, at most 640 MiB plus constants) and parses each file as a stream. Packet events become exact per-source counts/bytes/first/last times, preserving early request/header/body/socket lifecycles for up to 60,000 sources per capture, alongside 100,000 structural events. Stage/dependency/error caps and conservative raw-rotation indicators remain explicit. Preparation requires either the single legacy capture or all ten named user captures. Invalid input records fixed error categories only: JSON parser exception text can contain raw input fragments and must never enter artifacts. Raw files/fragments are removed on success or failure. Canary, streaming/incomplete-input, packet-flood and cap checks passed; no measurement assertion or deadline changes accompany this encoding.

The preceding `57030e47` full browser artifact [10046334456](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34198376281/artifacts/10046334456), SHA-256 `1a1a9a12b4116a3f4d03cc3b200c7994a0f2d95d0e484de45cabd3e72f3dd8c2`, passed 116/118, zero skipped. Phone Equipment history remained loading at quality-prepare.ts:184; phone Sites recovery retained loading related records at quality-states.spec.ts:281. The phone journey stopped before pack generation. Four report waits passed in 3.29/3.77/8.24/9.81 seconds. These failures are not established as the globally throttled pre-API stall or the separately measured PDF launch delay. Diagnostic-only c503 subsequently passed all remaining Finance/report, 309 PostgreSQL, HTTP, persistence and offline restart stages; its browser failures still fail the job. The current-head full regression and causal review remain mandatory; merge is held.


## Original current-head failure evidence recovered — 8 September 2026

Artifact [10049379888](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34204653360/artifacts/10049379888) has now been materialised successfully. Its independently recomputed SHA-256 is `23b1b4fdd945f7805fd31f71ea946ee7f5458b82d2d55bd1bd06a8ad501355f1`. Provenance matches source `5b6b8bf7`, executed checkout `5309fd03`, tree `5d21fa7b` and run `34204653360` attempt 1. Results JSON confirms 115/118 passed, three phone failures, zero skipped/flaky. Failure screenshots, redacted error contexts, response-lifecycle attachments and server process 8851 metadata were inspected. No raw trace ZIPs are present. The selected original events and limits are retained in [browser-failure-timelines.json](../testing/evidence/p11/browser-failure-timelines.json).

- The selected journey report retry was observed at +193ms and its click completed at +223ms. Its original 15-second response wait rejected at +15,436ms. Gateway request 11528 completed HTTP 200 after 22,605.73ms, including 21,329ms between report-render-start and report-render-complete. This confirms late rendering; the inner launch/content/font/PDF/close stage is not established for this occurrence.
- SC-10 setup detail GET 12516 completed in 393.39ms; document 12517 completed in 655.78ms. Initial static responses completed by 09:16:25.145 UTC. The next HMR-client script request only reached the server at 09:20:42.259 UTC; page identity/detail requests followed around 09:20:44 UTC and completed successfully. The 60-second selected-response test failed. Gateway completion is not proof of browser delivery/consumption, and this unthrottled failure is not disposed of by the separately passing load test.
- The final P09 report re-read followed the expected reassociation refusal HTTP 409. GET 13027 reached the gateway at 09:23:39.314 UTC and closed without a completed response 53,188.85ms later. That interval is not a measured database query. Existing evidence cannot distinguish route dispatch, identity, database acquisition/work, response construction or process scheduling.

The bounded follow-up adds opt-in numeric server/test-runner heartbeats, request-scoped read phases and browser lifecycle attachments to the three affected test files. It uses existing Playwright page events; no extra CDP session, routing, browser/network change or liveness probe is introduced. The diagnostic replay runs the same three original phone procedures together against fresh disposable PostgreSQL, alongside all original workflow gates. All original response/action/navigation/test/job limits and assertions remain. All nine controlled-output fingerprint sources remain byte-identical. Diagnostic overhead and fresh-fixture replay versus full-suite history are explicit limitations; a passing replay alone is not causal closure or permission to merge.

Local TypeScript, focused ESLint, all 28 unit cases, production build, prototype/naming and whitespace checks passed. Test discovery confirms exactly three selected phone cases. The local foundation check retains 176 missing historical-image links in this partial checkout; it is not a foundation pass. The existing report-template dynamic filesystem tracing warning remains visible in the build and is not established as the browser failure cause. Exact-head CI results are recorded in PR #61. This environment has the pinned package tree but Node 24.19.0 rather than CI's 24.20.0, and no PostgreSQL service. No local database/browser integration pass is claimed. The existing running full regression is allowed to preserve its remaining outcomes.


## Exact-head load pool-capacity correction — 8 September 2026

Run [34213541685](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34213541685) on diagnostic source `60ca8038` produced one retained failed read among all 320 declared samples. The exact load artifact [10051227070](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34213541685/artifacts/10051227070) was recovered using the supported GitHub-file materialisation path and independently matched GitHub's SHA-256 `4959056cce9ec8fb1420fafdbd7b95f9ac2c0b136a326765bb4b88233f517543`.

The failed phone Planner cold sample was not a pre-API asset stall. Its document returned HTTP 200, DOM and load events fired, all 19 assets completed, two core requests carried the declared rule and the page showed the guarded dependency-unavailable state. Same-process gateway metadata records ten overlapping schedule/site responses with HTTP 503 after 3.23–4.50 seconds. PostgreSQL's retained service log remained ready and contains no fatal/connection-slot error. The application pool was capped at eight connections with a 3,000ms acquisition deadline while ten users concurrently opened Planner; each startup uses three database-backed request paths (local session, schedule and sites). The observed clustered failure time and source limit support pool acquisition exhaustion as the bounded cause; they do not convert the other unthrottled browser/render failures into pool failures.

The runtime correction increases the application pool ceiling to 32: enough for the declared 30 simultaneous startup request paths plus bounded headroom. It does not alter PostgreSQL's server limit, query or transaction semantics, the 3,000ms connection acquisition deadline, the 10,000ms statement deadline, permissions, response contracts, UI, renderer/template sources or immutable outputs. A unit check fixes the declared-user/read relationship and unchanged deadlines. No load users, concurrency, waves, samples, global rules, applied-rule assertions or failed sample are removed.

The focused diagnostic replay [102019873176](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34213541685/job/102019873176) passed exactly the three previously failing phone procedures, with three render-response waits completing in 2.967–2.998 seconds. Its exact artifact [10051183708](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34213541685/artifacts/10051183708) independently matches SHA-256 `6e2b030dbc1753ad20e3b3ec00e446fab66c6899fd7a010f55af6933fefe3266`. Prompt route/database acquisition in this fresh replay narrows those occurrences but does not causally dispose of the original 22.6-second render, 4.3-minute browser gap or 53.2-second incomplete report read. The original failures remain in the ledger.

Merge remains held for all applicable checks on the corrected exact head. In particular, the load gate must complete 320/320 reads with all ten captures and the full browser/database/HTTP/restart job must finish without using the focused replay as a substitute.

### 2026-09-08 — retained full-run memory evidence and correction

The full `60ca8038` browser step completed 114/118 cases. The [memory-pressure record](../testing/evidence/p11/browser-memory-pressure.json) retains all four failed assertions, source/tree identity and independently verified artifact SHA-256. Server RSS reached 6.44 GB; free system memory fell to 14.45 MB. A two-second browser test-process sampler recorded a 438.9-second gap during the phone journey, while the server also stalled. This supports addressing memory pressure without extending the original test deadlines. It does not classify the earlier load 503 exception or prove every failure has one cause.

The project had disabled the filesystem cache that [Next.js requires for compiler memory eviction](https://nextjs.org/docs/app/api-reference/config/next-config-js/turbopackMemoryEviction). Enable filesystem snapshots and automatic eviction during each guarded development-server process. Clear the owned `.next/dev/cache/turbopack` directory before startup so every restart still recompiles from a cold compiler cache. Business records, output bytes and retained diagnostics are outside that directory. The performance evidence profile explicitly records the new within-process reclamation behavior; ten independent browsers/users, 320 samples, network rules, assertions and timeouts are unchanged. All nine output fingerprint inputs are unchanged. A local no-session probe copying all 1,927 earlier evidence files did not reproduce recompilation stalls; no renderer/template edit accompanies the memory correction. Final full-suite verification and publication are recorded on PR #61.


## 2026-09-08 — desktop response boundaries after memory correction

The full browser artifact [10072465352](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34264208660/artifacts/10072465352), independently SHA-256 verified as `901d1a2490359edad2adb302b850327b997f6bd60dc00d32443b758812524350`, records source `e2b7b41b7996366525bb50db33409b571ea85d8a`, executed checkout `790324344c07e6b771a950fadf52091481216699` and matching tree `968be21a891c60c801c84183d79cd388bcb5d20a`. The step passed 116/118 cases in 28.8 minutes, with zero skipped/flaky/retried. All three originally failing phone procedures also passed inside this full suite. Six independent gates passed, including all 320 load samples and ten captures; all sixteen candidate three-second p95 groups still missed their target. Full-regression failure is not overridden by those components.

The [structured response-boundary record](../testing/evidence/p11/browser-response-boundaries.json) retains both failed desktop assertions. The CRM Commercial link assertion expired after five seconds while its two authorised GETs remained pending; browser teardown closed them after approximately 5.63 seconds. Its screenshot still displays loading. The recovery popup's local-session, cached-job and assigned-job GETs all returned HTTP 200 in sequence, taking approximately 3.334, 1.684 and 0.802 seconds. The screenshot shows a verified identity alongside the previous locked notice while reconnect finishes. Raw trace ZIPs are absent; sanitized gateway paths cannot classify the CRM internal compilation/database split. These observations justify response-aware postconditions, not an assertion that either stored business result was missing.

The correction awaits the exact Commercial and final assigned-job responses before the original UI checks, adding HTTP 200 and private/no-store assertions and checking the Commercial response's exact estimate ID. All existing link, saved-original, keyboard, identity and recovery assertions remain. Their five-second assertion values and existing request/action/navigation/test limits are unchanged; the response observation explicitly precedes each finished-state postcondition. There is no fixed sleep, retry loop, skipped procedure or changed load/permission/output contract.

The offline shell disables identity verification until startup has settled, and during a verification already in flight. It displays a pending notice immediately, a completed notice only after the existing checks, and a retained-originals error notice on failure. A new isolated browser case per viewport holds real shell startup and an explicitly synthetic assigned-job response to check startup, pending, duplicate-activation and denied-recovery behavior. Those mocked responses only establish UI-state behavior; the complete selected journey continues to verify real ownership and the five exact retained originals.

Full-suite server RSS peaked at 3,926,302,720 bytes, free system memory remained at least 1,693,999,104 bytes, and the largest two-second heartbeat interval was 2,338ms. The earlier multi-minute scheduling gap did not recur in this run. Local lint, TypeScript, all 29 unit tests and build passed on Node 24.19.0; local Chromium/PostgreSQL are unavailable, so no local browser/database proof is claimed. The existing dynamic filesystem tracing build warning remains. CI on the resulting commit, all later regression stages and publication remain mandatory before merge. Nine controlled-output fingerprint inputs, issued references and all 78 parent IDs remain unchanged.

## 2026-09-08 — bind Systems denial to the actual browser response

The full `1b9660ad` browser step subsequently passed all 120 cases in 21.9 minutes, with zero failed/skipped/flaky/retried. [Artifact 10074233937](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34269603077/artifacts/10074233937) independently matches SHA-256 `3b3b4dd0831ee22627668d248ad9a743ec9a0e3c4a0443dd76885faba0aca819`. This includes the original three phone procedures, both corrected desktop procedures and the new identity-control case in both viewports. The independently failed focused matrix below still prevents merging this head; later regression stages and corrected-head verification are separate gates.

Source `1b9660ad6a1b91b9aae8adae5926cb190f6fc985` passed both formerly failing desktop procedures inside the full suite and the new identity-control case in both viewports. Five independent gates passed. The focused state matrix failed one desktop SC-04 check because an additional `page.request.get` raised `read ECONNRESET`; the eleven other focused cases, both selected journeys and all six retained Finance cases passed. [Artifact 10073726524](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34269603077/artifacts/10073726524) independently matches SHA-256 `262e807693ff43dc1b9d414bf06e4f4787931fea623635e67933fb6701035add` and the exact source/tree. The [structured denial record](../testing/evidence/p11/browser-denial-response.json) retains the failure, browser and gateway events and limitations.

After the accepted Systems identity POST, the browser's two service-ticket GETs returned HTTP 403 and finished at +50,287ms. Matching gateway requests 536/537 returned in 40.19/40.77ms. The screenshot shows the real Systems identity, the expected access-denied message and no service record. All 2,573 requests received by server process 14009 finished; there is no additional matching gateway request or handler rejection for the failed API-client probe. Its underlying socket-reset mechanism is not classified by this evidence.

The correction observes the screen's actual GET initiated after the real identity POST returns HTTP 200, then applies the original 403/404, private/no-store, visible-error, no-loading and sensitive-record absence assertions. Request identity is tracked from page events so an older in-flight response cannot satisfy the check. Selected query values are also checked, including equivalent Planner instants and the exact account ID. Per-screen denied status and response source are retained in the matrix evidence. The separate API-client probe is removed from this UI-state assertion; independent database and HTTP permission suites remain. No application or permission behavior, response/action/navigation/test deadline, retry, case count, load rule or output fingerprint source changes. Focused ESLint and TypeScript pass locally; fresh full CI and actual-main verification remain mandatory.

The current load artifact records 320 unique successful samples, two rule-verified core GETs per sample, all ten captures and no capture failures. It also records four CPUs and 16,766,410,752 bytes RAM, compared with the earlier load runner's two CPUs and 8,323,969,024 bytes. Timing differences across those runs are not a controlled measure of the source correction. All sixteen candidate three-second p95 groups remain unmet. The existing full runs continue to retain their later regression outcomes; no unchanged retry or earlier-head component result is counted as the current merge gate.
