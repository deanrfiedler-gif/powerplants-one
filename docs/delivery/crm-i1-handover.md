# BP-03 I1 — Owned opportunity and qualification follow-up

**Revision:** r02 · **Date:** 6 September 2026 · **Owner:** Dean Fiedler · **Status:** I1 implemented and component-tested; final combined checks and merge publication remain governed by the external record.

Implementation issue [#39](https://github.com/deanrfiedler-gif/powerplants-one/issues/39) is linked to discovery parent [#9](https://github.com/deanrfiedler-gif/powerplants-one/issues/9), which remains open. [ADR-0015](../decisions/ADR-0015-crm-i1-owned-opportunities.md) records the bounded implementation choices. The [external publication record](https://github.com/deanrfiedler-gif/powerplants-one/issues/39#issuecomment-5557831957) owns final identities and actual merged-main results; this file does not claim its own future SHA. [PR #40](https://github.com/deanrfiedler-gif/powerplants-one/pull/40) is the review surface. [I2 starter](crm-i2-starter.md) is preparation only.

Starting main was `ddc1a3cce769e011939e621d8d5f176542f48f8c`, tree `d6d374ec30c620276e402d6a15f5adbc4d25bf02`. [Discovery publication](https://github.com/deanrfiedler-gif/powerplants-one/issues/9#issuecomment-5557062585) was verified. P09 #36 / draft #37 remained active at `9931e3c4501e7aa4d44140066b882b8985ba2a61`; it is not counted as delivered. An independent local clone at that exact main/tree and `feature/crm-i1-owned-opportunities` preserve all unrelated worktrees. Migration 0010/ADR-0015 are reserved after P09 0009/ADR-0014; accepted ordering must be reconciled before merge.

The implementation adds typed opportunity/config/event persistence, Internal Activity links, three scoped CRM capabilities, guarded create/qualify/next-action commands, current-permission receipts and online responsive screens. The accepted synthetic journey survives actual application/PostgreSQL restarts. Combined run 34024503776 passed 12 unit, 234 database, 17 HTTP and 64/66 browser cases, including all eight CRM browser cases; both failures were P09 test selectors, corrected by that workstream. Exact heads, failures and subsequent results are recorded below and externally. Business acceptance and an unmerged branch are not declared complete. Local Python checks passed; exact local runtime pins are unavailable (Node 24.19.0/npm 11.9.0 versus 24.20.0/11.19.0), so pinned disposable CI is the runtime evidence environment.

Final publication requires accepted P09/main reconciliation, successful combined checks, review/control assessment, normal expected-head merge and actual merged-main workflow verification. The I2 starter is prepared only. D-013/D-025/full AT-25 and account parity remain unresolved. No operational Pipedrive read expansion/mutation/import, integration, communication, Finance, hosting or access-setting change.

## First disposable CI disposition

Head `2da09aa2f2b5f5e5312a203315aa888e03f2ece0`, tree `3d018bb89e0e771259c5f59a1a25ea8aa1ad921c`: documentation run 34018514075 and CRM design run 34018514058 passed. Application run 34018514060 attempt 1 failed at TypeScript before database/HTTP/browser execution: six implicit-any callbacks resulted from the untyped opportunity context projection. Added an explicit Opportunity row and typed context projection; rerun pending. No runtime acceptance is claimed for this head.

Application run 34019598311 attempt 1, source head `5739e6745622ae76891fbdf5dcfc058013f77982`, tree `f173c8e9cf16a0965b73ebc7e608aa66780dc232`, failed because temporary pinned formatting-review TypeScript copies were generated inside the broad compiler include path. No source-file TypeScript error was reported; no database/HTTP/browser gate ran. Adopted the unchanged-head pinned Prettier 3.9.6 output from artifact 9985047820 (SHA-256 `6bf3f8fec168272d252f78a3aabd36bff42b755519b3ac9c1b340e358f10c7d5`) and removed the temporary workflow step. Compiler configuration and runtime gates remain unchanged. Documentation 34019598302 and CRM design 34019598288 passed.

Application run 34019207722 attempt 1, source head `97d698c68f80a2174f91970b9c420363bfd87f40`, tree `fe04603391d480f40c1227ed012e230815a1574c`, passed lint/type/build, all 9 unit tests, fresh migration/seed and guarded reset. PostgreSQL executed 184 cases: 59 passed, 125 failed. The new all-target visibility SQL reused `co` for both the Opportunity and inner Organisation alias, producing a parse error even in earlier Activity flows. Renamed the inner alias; all-target filtering remains. Four old upgrade assertions expected eight migration rows; changed only those to assert the exact expanded sequence `[1,2,3,4,5,6,7,8,10]`, retaining the old record/byte/hash/revocation assertions. No database acceptance or downstream HTTP/browser/restart proof is claimed for this failed run. Full rerun required.

## Implemented files and contracts

`src/crm/context.ts`, `validation.ts`, `opportunities.ts`, `reads.ts` and `receipt-authority.ts` contain the domain boundary. Three commands reuse the existing shared operation/workspace transaction, registry/reference allocator, audit, receipt and minimal outbox. `src/app/api/v1/crm/` contains thin routes; `src/app/(business)/crm/` and `src/components/crm-screens.tsx`/`crm-state.ts` provide the online worklist/create/detail/qualification/action journey. Canonical Organisation/Person/Site and existing Activity detail/completion screens remain in use.

Migration `0010-crm-opportunities.sql` adds the immutable fictional pipeline/two stages, typed Opportunity and append-only event records, real Opportunity ActivityLink FK and aggregate/context guards. Seed `seed-crm-i1.sql` under receipt 10 adds only configuration and explicit eligible synthetic CRM grants. It creates no opportunities/actions. `scripts/database.ts` selects the actual numeric filename prefix and executes 0001–0010 in order, including P09's unchanged 0009, seed receipt 9 and exact report template. Old migration/seed bytes and dependency pins are unchanged.

Necessary shared changes are limited to capabilities, Activity target parsing/visibility/labels, field/report follow-up all-target visibility, current-permission receipt dispatch, canonical RecordLink, navigation, sensitive-context clearing and CSS. No existing Activity kind, accepted normalisation/hash or start/complete/cancel rule changes. CRM actions use existing CustomerContact/RelationshipReview kinds and Internal class. P09's report projection receives the same optional Opportunity predicate within its existing transaction; its renderer, templates, customer-response and service-review domain contracts remain unchanged. Shared ordering and integration coordination: [initial P09 comment](https://github.com/deanrfiedler-gif/powerplants-one/pull/37#issuecomment-5557819554), [combined verification](https://github.com/deanrfiedler-gif/powerplants-one/pull/37#issuecomment-5558558410).

The [API amendment](../contracts/service-api.md#bp-03-i1-opportunity-implementation-amendment), [dictionary amendment](../contracts/service-data-dictionary.md#bp-03-i1-physical-opportunity-extension), BP-02 and ADR-0015 document exact routes, fields, types, limits, FKs, scopes and recovery. CRM names are BP-03-local; no PP-01 API-C/API-R number or parent requirement is allocated. Broader screen wireframes remain illustrative design, not runtime evidence.

## Reproducible setup and recovery

Use pinned Node 24.20.0, npm 11.19.0 and PostgreSQL 16.15; all existing exact package pins remain. Local scratch is Node 24.19.0/npm 11.9.0, so runtime verification uses disposable CI only. Do not override engines. Create ignored `.env.local` from `.env.example` for an owned loopback database and preserve the existing private document byte directory. The maintained P08/P09 setup governs existing services.

```sh
npm ci
npx playwright install --with-deps chromium
npm run db:migrate
npm run db:seed
npm run db:health
npm run dev
```

Select the synthetic coordinator at `http://127.0.0.1:3000/crm/opportunities`, create against an existing permitted organisation, complete its initial Activity, record qualification, then deliberately plan its successor. Unknown site/contact need explicit reasons. A due-needed action does not receive an invented date. Completion does not change stage. No external worker/send or Pipedrive connection is needed.

For an uncertain save keep the original page open and use **Confirm original save outcome**. This reads `/api/v1/operations/:operation_id` under current authority before retrying only the same original payload. Do not start a replacement intent merely because the response was lost. A stale version retains proposed input; load the current permitted version, compare, then deliberately adopt its version and retry. Unsaved/pending online form memory is not durable offline evidence. Accepted rows/actions/events/audits/receipts persist in PostgreSQL.

Preserve the database and original private bytes on an application problem. Migration is forward-only; a compatible application may be restored without deleting CRM records or rewinding references. No down migration or destructive rollback is supplied. The following reset is only for the expressly disposable `ppo_synthetic_test` database; normal prototype data must be retained:

```sh
PPO_ALLOW_RESET=dispose-synthetic PPO_RESET_DATABASE=ppo_synthetic_test npm run db:reset
```

## Verification procedure and evidence limits

```sh
python3 scripts/check_foundation.py
python3 scripts/check_prototype.py
python3 scripts/check_naming.py
npm run check
npm run test:db
npm run test:http
npm run test:browser
```

HTTP checks require the actual loopback launcher. Browser CI starts its own actual launcher. `tests/database/crm.test.ts` challenges atomic final-write rollback, original hashes/retry, real backend lock waits, competing qualification/action/owner commands, exact terminal outcomes, explicit successor/identification/due semantics, cross-company/person/site isolation, direct FKs/deferred graph, scoped selectors, revoked owners and all-target receipts. `tests/http/crm.test.ts` uses real routes/sessions including accepted-response loss, changed content, strict/body-size errors and revocation. `tests/browser/crm.spec.ts` exercises the UI, reload, retained conflict input, response abort after real commit, original operation lookup, identity clearing, keyboard and responsive/error states. Existing suites remain gates.

The workflow separately runs `scripts/crm-restart-proof.ts write` and `verify`, with actual application/browser process termination and a PostgreSQL container restart between them. The write phase creates an opportunity, completes its first action, qualifies and plans a successor. Verify reads the same opportunity, two actions, exact completed outcome, events and four operation receipts/payload hashes through PostgreSQL and real HTTP, then replays creation and proves one effect. It also records engine, process, source-head, executed checkout/tree, run/attempt and original viewport/hash metadata. This is distinct from screenshots and from P08 offline proof.

Original desktop/phone/320px screenshots have been inspected as recorded below; final-head and actual merged-main results are recorded externally after execution. No real mobile hardware, CRM offline support, full AT-25, owner-accepted parity, independent approval or operational transition is claimed. D-013/D-025 and account licence/pipeline/field/automation/visibility evidence remain open. Unknown account settings do not block this fictional first slice and are never invented from its configuration.

## Next bounded preparation

The broader discovery I2 theme is pipeline and relationship management. Its maintained [next starter](crm-i2-starter.md) now starts with a scoped Board/Grid worklist based on the newly accepted PR #41 brand/UI handover and I1’s two-stage/Open/no-money contracts. Owner transfer, close/reopen, arbitrary stages, reassociation, separate leads and account-plan builders remain later work. The earlier in-progress transfer proposal was superseded before any I2 implementation. The starter requires verified actual I1 publication and a new invocation before implementation. I2 has not begun.

## Traceability and declared component scope

| CRM case / parent link | I1 component actually subject to verification | Outside I1 |
|---|---|---|
| CA-01 / CRM-01/02, AT-02/25 | Atomic existing-context create, initial Activity, permanent identity/reference, strict input and final-write rollback | Lead conversion and imported identities |
| CA-02 / CRM-02, AT-25 | Competing expected-version qualification/action changes; browser comparison retains safe proposal | Arbitrary stage policy and board drag |
| CA-03 / CRM-03/08, AT-24/25 | Identical replay, changed-content conflict, lost response, current-authority lookup, accepted-main upgrade/reseed and real process restart | Separate lead conversion and source migration |
| CA-04 / CRM-03, AT-25 | Exact completed outcome, no automatic stage movement, explicit next-action-needed and chosen successor | Provider send/sync and automation |
| CA-05 / CRM-01, AT-02/25/30 | Existing UUID/site-party/affiliation boundaries, explicit unknowns and no name merge | Reassociation, stakeholder/role successor and import |
| CA-06 / CRM-08, AT-01/25/34 | Company/site/person scope, Systems/unassigned and revoked users; direct/list/filter/selector/operation access | Operational visibility-policy parity |
| CA-07 / CRM-02/03, AT-25 | Immutable stage-entry basis, due-needed/overdue/terminal/unavailable groups | Forecasting, ageing policy, reopening |
| CA-08 / CRM-03, AT-25/34 | Existing Activity reassignment/completion race, eligible active owner and terminal outcome preservation | Opportunity owner transfer (later bounded work) |
| CA-10 / CRM-08, AT-01/25/34 | Every Activity target controls content and current receipt/replay; denied browser identity clears context | CRM files/exports/Finance/service handoff |
| CA-13 / CRM-07, AT-23/25 | Original desktop 1440×1000, phone 390×844, 320px reflow, keyboard/focus/error/conflict/denied/unavailable/loading | Real mobile hardware and offline acceptance |

The included components in this table executed successfully at combined source `c1268fe5`: 29 CRM PostgreSQL cases (including parameterised invalid inputs), 3 CRM HTTP cases, 8 desktop/phone browser cases and the separate actual restart proof. CA-13 is browser-emulated responsive scope only. This is component evidence, not a full catalogue or AT/PT pass promotion; the combined run's two P09 failures still required correction. Exact executed layers/results and final publication are governed by the external record and retained originals. All 78 parent requirement identities, all four issued source byte hashes and existing full AT/PT statuses remain unchanged by I1; P09's separate delivered traceability is preserved. CRM I1 is not P13 and does not close PPO-009 / #9.

Application run 34019900073 attempt 1 at source head `6706c3407709b566b4455d1e4ede73952323f9fe`, tree `2261613ccbc0a4e23816300ab9cff3de66f6fd67`, passed lint/type/build, 9 unit, all 186 PostgreSQL cases, fresh migration/seed/reset and existing PostgreSQL persistence proof. HTTP passed 15/16; the new oversized-body test incorrectly expected 413 while the maintained streaming body guard returns 422 `PayloadTooLarge`. Corrected the assertion to the exact status/code and added zero-opportunity-effect proof; the guard and size limit are unchanged. Restart/browser stages after HTTP did not run. A subsequent shared UI extension removes previously loaded Activity content when its command/selector/read is denied, without changing Activity payload/lifecycle/hash semantics; a real DB revocation browser case exercises it.

Final review also tightened shared read refresh handling: 401/403/404 removes previously loaded data, while ordinary conflicts/outages keep their existing recovery behaviour. CRM creation validates visible company/organisation controls before dependent unknown reasons. Scoped creation selectors now expose only a permitted company/site path; a site-limited creator must choose that site and cannot create an unknown-site or other-site opportunity. A positive site-scoped owner journey supplements the negative isolation cases. These changes need the final full rerun; no new fixture grants are installed outside the disposable tests.

The CRM restart proof additionally compares `pg_postmaster_start_time()` and the separately spawned application PID across phases; a screenshot or a reused running server cannot satisfy the process-restart assertion. Within the disposable CI job only, the exact sequence is:

```sh
node --env-file=.env.local --import tsx scripts/crm-restart-proof.ts write
docker restart "$PPO_POSTGRES_CONTAINER"
docker exec "$PPO_POSTGRES_CONTAINER" pg_isready -U ppo_local -d ppo_synthetic_test
node --env-file=.env.local --import tsx scripts/crm-restart-proof.ts verify
```

The maintained workflow retries readiness for at most 30 one-second attempts. `PPO_POSTGRES_CONTAINER` comes from that job's own PostgreSQL service identity; it is not an operational database selector.

Application run 34020608252 attempt 1 at source head `67ef7d00b25465181aed70c93f1337dfc17d72e5`, tree `5c95ffeb694d27931dc07e64e4bcd9e12cbfe273`, passed static/unit/build, all 186 DB and 16 HTTP cases, P08 restart and the CRM accepted-journey restart. Browser passed 56/62, including all 54 baseline cases and both actual permission-revocation cases. Six new CRM cases hit a strict locator collision between the real application error and Next.js route announcer. The CRM tests now select the existing `.business-error[role=alert]` element, retaining exact focus/text checks and avoiding false-positive denied-page evidence. No production validation or baseline assertion was weakened. Original artifact 9985761197 has SHA-256 `1c96b3d377487e3bd82de0999066b696f0ef55c59f995cde64761ec460ada35b`; later complete journey/conflict/loading evidence still requires execution.

Visual review of run 34020608252 inspected the original 1440×1000 restart verification viewport: the loaded Qualified/Open version-three record, owner, canonical context and due-needed successor were readable. The original 390×844 empty-view screenshot remained above the actual empty result because the shared navigation/identity section occupied the viewport. It is retained as limited layout evidence, not a loaded-list proof. CRM captures now scroll the asserted scenario element into the original viewport, including errors, conflict comparison, successor action and loading. A focused CRM browser step runs before the unchanged database/full-browser regression gates to resolve these new UI cases promptly. Multi-command cases have a two-minute overall budget and a 15-second per-control timeout; exact behavioural assertions remain.

Historical selected originals from run 34020608252 are retained in [PPO-CRM-I1-review-originals.zip](https://chatgpt.com/api/library/files/libfile_9764e3e96ae8819187e5616fc8ab3422/download): 26 unchanged PNG/JSON files plus manifest, 1,117,984 bytes, SHA-256 `a07c3cbc6ad9e095d135481b3bf2f49c60897399ee6fbd12d33621849d2c27dd`. The manifest records source head/tree, actual PR checkout `72c5753967cec1e49908d2dfa2394eebaedb2373`, run/attempt, hashes, failures and visual limits. Original phone revoked-Activity and desktop validation views were also inspected: sensitive content was cleared, the unavailable/validation error and focus were visible. Raw traces, session profiles and credentials are excluded. These originals do not assert final-head or merged-main acceptance.

Application run 34022133008 attempt 1 at source head `15d79db02ca64378c871e31f3655b29380b39fb5`, tree `cba006bc23469f0c050eb9f5a0a2f847b988b35b`, passed static/unit/build, fresh migration/reset and 6/8 focused browser cases: full sales UI/reload, conflict/uncertain-original recovery and denied/empty/320/keyboard/unavailable/loading on desktop and phone. The two new site-scoped fixtures failed before UI setup because `SELECT DISTINCT` inferred uncast parameters as text. Explicit UUID casts fix only that test insert. Remaining DB/HTTP/restart/full-browser stages correctly did not run. Original artifact 9985887998 (SHA-256 `cb4cbec37771966368bab0a5469ae5945c5c0e876b95ec91930abae233735439`) retains the successful component captures and fixture failures.

Original loaded screenshots from run 34022133008 were inspected at 1440×1000 and 390×844: long record titles/needs wrap; canonical context and Open/stage/owner/source details remain readable; the filtered loaded worklist exposes its one permitted record; conflict comparison preserves the proposed need/note and requires deliberate retry; uncertain-save controls prevent replacement intent. Phone next-action-needed and explicit due-needed successor views, focused validation and 320px keyboard reflow were also inspected. Two wording refinements follow that review: empty required selectors now ask for an existing permitted company/organisation/owner, and untouched detail command forms say **No unsaved changes** after reload. Uncertain captures now include the status text above their recovery button. Exact assertions verify these refinements on the next full run.

New main PR #41 merged at `8bc0d903cb9da100136a8a5430bac9e6883bb27d`, tree `191b7f86c790201850240a55b3f81b683a52c38c`, while I1 was being verified. Its 23 exact blobs, source tree and signed main commit were retrieved through connected GitHub and verified locally. Reconciled STATUS/sequence/register conflicts by field, preserving the complete UI specification, original logo/captures, standalone design checks and guidance. Head `b270f8b70470ccf11fe3747d5a9b658f9200f48b` had no Actions runs because the new main caused PR conflicts; no test result is claimed for it. The I2 starter now consumes that accepted visual guidance as a bounded Board/Grid worklist; no I2 code was started.

The reattached logo and brand PDF exactly match the accepted manifest: SHA-256 `8d12f0ecc394950cd9eb66964c588efb71c8e6f445390336c245c3f6712b9694` (315,536 bytes) and `335d3049bc72783799315d4790bd56b04e928acd0e1c2c81d8099cec2f2b8fd3` (27,591,152 bytes). Reviewed PDF text for pages 7–9/15–19/22–24 and original logo plus rendered pages 7/17/18. Brand-derived typography/colour/logo treatment matches the maintained design handover; the full company PDF is not added to Git. Shared brand adoption and Board/Grid remain I2 preparation, while I1 retains the existing synthetic application styling and navigation.

Additional original run 34022133008 inspection covered desktop/phone denied and overdue views, plus the 320px unavailable/loading views. Error focus, cleared sensitive context and overdue details were readable. The unavailable view still showed an earlier empty result/count; the worklist now suppresses those stale results during a failed refresh, and retained detail explicitly identifies its previously loaded state. Captures restore each project's original viewport after the separate 320px check. A security review also found overlapping focus/poll responses could restore stale content after a newer denial. CRM refreshes now accept only the latest request; the actual-grant-revocation browser scenario holds a real authorised response, observes the newer denial, then releases the old response and proves title/action/proposal stay cleared. These refinements require the next exact-head run.

Historical runs 34021214186 (head `7e1e4f05e6efe5a6dd63ed0f4eb64472f84de999`) and 34021829610 (head `e590a901fcec39f38225c55b7af08943197ed003`) passed their database/HTTP/restart stages and failed new browser setup/locator cases. The former passed 56/62 browser cases with the already-corrected alert collision. The latter passed all 54 baseline browser cases; new cases still hit that collision plus the already-corrected UUID fixture insert. Run 34021829610 also passed the strengthened differing-application-PID/PostgreSQL-start-time CRM restart assertion. Neither failed run is final acceptance.

Run 34023194493 attempt 1 at source head `98e489bab34173f902e0e0900441ee7b8fa2f498`, tree `535930fd223466f5435376cec322553e37f5c64d`, passed static/unit/build, fresh migration/seed/reset and 7/8 focused browser cases. The phone revocation case removed its form before the click completed because a concurrent selector read correctly observed the deleted grant. The test now pauses the actual completion request, revokes the fixture grant, then forwards that request to the unchanged server guard; it asserts exactly one refused request and no completed outcome. This removes the setup race without weakening automatic context clearing. Downstream gates were skipped. Original artifact 9986238168: 4,400,349 bytes, SHA-256 `b506eead7ebf169be8d75001a1bfe287c23754aea34dc045bcf68ec24129d84d`.

The historical browser originals from runs 34022133008 and 34023194493 are retained in [PPO-CRM-I1-browser-review-originals.zip](https://chatgpt.com/api/library/files/libfile_d2b425e4253481918e81d4ba73ca3a4a/download): 129 unchanged PNG/JSON files plus manifest, 4,392,808 bytes, SHA-256 `491dc3e3cc483ee4a31713a80d8b4c59340b2b40163704a9bace73c8093474e0`. The manifest records actual checkouts `d73544d7d85417d4700137e9f537f74acfeba582` / `429a6073b3bc23dbe628650727f277e1725b357d`, source trees, viewports, hashes, individual original inspections and limitations. Additional head-98e489b original inspections confirmed desktop/phone site-scoped selectors and the complete uncertain-save status/recovery control, and the phone failure image showed the correctly cleared Activity form. Raw traces/profiles/logs are excluded. These are historical component proofs, not final merged-main acceptance.

Direct-SQL review tightened deferred history integrity before release: every changed Opportunity version must have its exact event and actor, including multiple updates in one transaction. An event cannot describe a future unaccepted version, skip its predecessor or disagree with the preceding stage. The database challenge attempts an omitted intermediate event and a standalone future event and requires atomic refusal. This is an I1 migration refinement in disposable verification, not an edit to any accepted migration.

## Combined regression disposition — 6 September, 10:10 UTC

Pre-P09 source head `9c08b26679cab58fbc6907bf9aaa0417a7e34a91`, tree `26972e5dbb6efc5b5ce43998533a457112f64cfa`, passed application run [34023538509](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34023538509) attempt 1: 9 unit, 187 PostgreSQL, 16 HTTP and all 62 browser cases, plus fresh migration, guarded reset and PostgreSQL/P08/CRM application-browser restart checks. The eight focused CRM repeats are not counted twice. Artifact 9986693183 preserves the originals, SHA-256 `8af0e7495c64470ce03b0853a85a3a6ad012019408dbc507dc584b337f672705`. This was not a P09-inclusive or merged-main result.

Combined source head `c1268fe50c24993855f04aeb22879be256044b60`, tree `cb2f864a18de9d2ea76639ff28028bdfeb92b293`, included exact P09 draft `814fd7c374718c5915be3bec1f050994406affd6`. Application run [34024503776](https://github.com/deanrfiedler-gif/powerplants-one/actions/runs/34024503776), attempt 1, passed 12 unit, all 234 PostgreSQL and 17 HTTP cases, fresh migration/guarded reset, accepted P09 record/receipt/document upgrade preservation, deferred event-chain SQL challenges, report/field all-target projection denial and all PostgreSQL/P08/P09/CRM restart phases. Browser result was 64/66: all eight CRM cases passed; both P09 complete flows failed at a selector that chose the last date-sorted appointment rather than the saved proposal. Artifact 9987047019, SHA-256 `955a8fb1d822d505cf177795e0de0ea67ceaa76fa126b337dbf9f3205fc5766b`, preserves this failed-run evidence. No all-green combined or merged-main result is claimed.

Reconciled P09's own correction at `8de9bce54c2f5ee4e9bcd2905a6f83034a9cb433`: exact receipt ID and persisted Proposed/no-crew/Unknown-agreement checks replace the wrong-card/transient-message assumptions. Its focused P09 gate, handover and disabled-control brand correction are retained unchanged. No P09 branch is changed or declared complete. Normal I1 merge still waits for accepted P09 main and successful final combined checks.

## Durable combined originals and inspected UI scope

[PPO-CRM-I1-combined-review-originals.zip](https://chatgpt.com/api/library/files/libfile_ce57095befc88191acb5cf01e7082c7d/download) preserves 152 unchanged CRM PNG/JSON files plus manifest from runs 34023538509 and 34024503776: 5,663,929 bytes, SHA-256 `d6c0e9f1af0da70a4cd547523c29bf05e7a47199b1ca712163879a25fb0cc501`. Raw traces, sessions and logs are excluded. Each original PNG has source head, executed checkout/tree, run/attempt, viewport, scenario, bytes and SHA-256 in its paired metadata. The manifest marks the 24 exact combined-head images visually inspected on 6 September 2026; other originals are retained without an inspection claim.

The combined screenshots use source `c1268fe50c24993855f04aeb22879be256044b60`, actual PR checkout `a92538c869940665bd02a7eff61475ae900c1bcf`, tree `cb2f864a18de9d2ea76639ff28028bdfeb92b293`, run 34024503776 attempt 1. Original 1440×1000 and 390×844 views show loaded worklist, long title/customer context, Enquiry/Qualified/Open/owner/source, next-action-needed, due-needed successor, overdue, focused labelled validation, conflict comparison retaining the proposal, uncertain-save recovery, unavailable/loading and sensitive context cleared after actual revocation plus a late previously authorised response. Two original 320×844 captures show keyboard focus and contained reflow. All inspected content was readable; long pages use ordinary vertical scrolling. Phone detail captures show the upper customer context; separate action and list captures establish the next-action presentation. No real mobile hardware or CRM offline acceptance is claimed.

The combined restart metadata records application PID 22014 → 22412 and PostgreSQL start time `2026-09-06T09:41:57.094Z` → `2026-09-06T09:42:17.927Z` under Node v24.20.0/PostgreSQL 16.15. Both phases identify opportunity `efa83215-5421-4f3a-a6a8-042c48c2ae34` and the same four operations. The executable proof re-reads Qualified/Open version 3, the two actions and exact completion outcome, three events and original receipts/hashes, then replays original creation and verifies one effect. This persistence conclusion comes from executed DB/HTTP assertions and different process identities, not screenshots.

The upgrade challenge creates accepted P09 issue/response evidence before applying 0010, then compares all original rows in its 14 report tables, exact HTML/PDF/manifest bytes, shared audit/outbox/receipt/history, Activity links, reference counters and identity rows. Replaying original P09 and Activity operations keeps accepted payload hashes. A second seed after CRM qualification and revoked edit grant does not reset the record/reference, duplicate an action or revive authority. Fresh migrations and upgrade paths are both exercised in the combined PostgreSQL run.

## Repository controls and review limits

Connected GitHub identity was rechecked on 6 September 2026 as `deanrfiedler-gif` (231005545). Repository metadata remained private with pull/push/maintain/admin capability and ordinary merge commits enabled. The accessible main branch metadata reported unprotected with empty required contexts; detailed branch protection returned 403 “Resource not accessible by integration”, and rulesets returned 403 for the private-repository plan. These are explicit inspection limits, not proof that every rule was inspected. No visibility, membership, permission or branch-setting change was made. PR #40's submitted reviews and review threads were empty at this checkpoint. Review is the implementer's self-review, not independent approval; CONTRIBUTING's actual sole-developer rule is retained.

Final premerge evidence must reread current main/head/diff/checks/review requests/reviews/threads and repository controls. Normal merge uses the exact expected head, and the external publication records the resulting main SHA/tree/parents, merged PR state and actual main workflow results. That record remains authoritative after this file's commit, avoiding a circular self-SHA claim.

P09's own source `8de9bce54c2f5ee4e9bcd2905a6f83034a9cb433` subsequently passed application run 34025595161 attempt 1: 12 unit, 205 PostgreSQL, 14 HTTP, 58 browser and all restart checks; four focused P09 repeats are not double-counted. Its actual checkout was `3a4740d` (full identity in P09's original metadata). This confirms the independently maintained correction, but P09 remained unmerged when this checkpoint was written. I1's normal merge remains conditional on accepted P09 main and actual current checks.

Self-review examined the three explicit CRM command schemas, server/transaction permissions, original receipt dispatch, closed stage/configuration SQL, deferred event/Activity graph, unknown relationships/due semantics and client recovery/denial handling. The concrete issues found during implementation and their corrections are retained above. Compared with exact P09 `8de9bce5`, 22 preceding migration/seed/runtime-pin files were byte-identical; the only change in `src/reports` is the shared all-target Activity visibility call. No P09 offline/renderer/template/response implementation was replaced. Maintained Python checks after the handover revision passed: four issued sources, 78 requirements, 29 decisions, 38 acceptance scenarios, 30 prototype procedures, 45 document records and 995 local links. These documentation checks do not replace runtime evidence.

## Reconciled newer design main — PR #42

PR #42 merged at `6afdb9d59c05f530b01f63929e97a8a58edd3093`, tree `5003c0ddd93b2fd34b882c892c2e7ccff6de67aa`, while review head `9ffc1fe2aa046e39796a4ce4f6764b863abfd131` was being published. That head had no Actions runs because the new main conflicted in shared documentation. No result is claimed for it. The 19 exact accepted blobs, original added PNGs, source commit and signed main commit/tree were retrieved and verified through connected GitHub. Reconciled STATUS, screen-specification and document-register fields preserve all new audit findings, revised standalone checks/captures, I1 component distinctions and P09 delivery content. The [external design publication](https://github.com/deanrfiedler-gif/powerplants-one/issues/9#issuecomment-5558601112) identifies this newer baseline.

Read the complete revised UI handover/I2 guidance and exact specification changes. I2 starter r03 now carries continuous stage navigation, phone stage selection, contained frozen Grid identity/headings, full action/owner and compact first-record goals into its separately invoked two-stage/no-money scope. No I1 runtime or P09 behaviour is changed by this design reconciliation.
