# Job Pack title, readiness and history refinement

<!-- versioning: git; committed history is authoritative -->

Owner: Dean Fiedler. SV-05 / SC-06; SVC-03, SVC-06, OUT-09. Implements adopted D7, D8-A, D9 and D10 after I6 PR #306. D7 is grouped here under Dean's current programme instruction. Review: implementation for review; no owner acceptance or deployment claim.

## Result

Verified saved scope supplies the title/work kind while references remain visible. Service is the in-module breadcrumb root, retaining the accepted D8-A shell exception. Current server readiness is grouped by stage, action-required rows visible, satisfied rows collapsible. Crew role/progress and explicit responses remain separate. Output recovery and follow-up use names and readable states/errors. Staff can inspect all exact issue acknowledgements and richer service-history provenance. Recipients receive no added staff metadata. The original history hash basis, saved snapshot, renderer and command authority are unchanged.

The actual synthetic schema was inspected before the read additions. No migration, capability or new command is needed. Tests distinguish old-issue responses from successor authority and verify metadata is not included in recipient projections.

## Verification

Eighteen unit cases, three focused database cases and 30 compiled browser cases passed; six browser skips avoid duplicate mobile-width coverage. The browser suite includes I4 source/print choices, I6 structured text, r03 geometry and the new I7 title/readiness/history cases, including first-preparation history in the appointment’s Perth timezone. Typecheck, changed-file lint, production build, studio, foundation and naming checks passed. The local database proof uses the already documented temporary 60-second query timeout wrapper; application timeout is unchanged. Standard CI remains a separate result. [Retained captures and inspection](../testing/evidence/job-pack-i7/README.md) record six widths and a phone shell overlap carried into I5.

I5 entry links, print CSS, legacy cleanup, paired conformance and final integration remain. Physical-device/owner review and deployment are separate.
