"use client";
// Placeholder replaced by the real full record during the EN-08 build.
export function RecordView({ id }: { id: string }) {
  return <div className="em-empty" data-record={id}><p>Loading…</p></div>;
}
