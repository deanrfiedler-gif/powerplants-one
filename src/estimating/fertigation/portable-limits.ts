/** Browser-safe transport bound for portable files carrying retained calculation results.
 * The editable proposal still has its independent 2 MiB validation limit. */
export const PORTABLE_BYTES = 8 * 1024 * 1024;
