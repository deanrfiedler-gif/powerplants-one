-- P11 additive successor output templates. The existing seed transaction calls
-- seedP11Templates to bind exact source/font/logo hashes and moves only the
-- checked version-1 template policies. All old definitions and outputs remain.
SELECT 1;
